# Story 2.5.2: Event Management Frontend

## Status
Draft

## Story

**As a** user (Organizer),
**I want** a React frontend interface for event management with dashboard, CRUD forms, workflow visualization, and timeline views,
**so that** I can efficiently manage events through an intuitive user interface.

## Domain Context

### Primary Domain
Event Management Domain - Frontend React application consuming Event Management Service APIs

### Involved Services
- **Frontend**: React application (TypeScript, Material-UI, Zustand, React Query)
- **Backend**: Event Management Service (REST APIs from Story 2.2)
- **Backend**: Speaker Coordination Service (speaker assignment data)
- **Backend**: Venue Management (venue booking and logistics)
- **Backend**: Company Management Service (company data for speakers)
- **Backend**: User Management Service (team assignments, organizer roles)
- **CDN**: CloudFront for event images and materials
- **Cache**: React Query for client-side caching (5-15min TTL)

### Cross-Domain Dependencies

**Foundation Dependencies (MUST be complete):**
- **Depends on**: Story 1.17 (React Frontend Foundation) - **DONE** ✅
  - Provides: BaseLayout, AppHeader, NavigationMenu, NotificationDropdown, UserMenuDropdown, LanguageSwitcher, ErrorBoundary
  - Provides: Shared components (`components/shared/`), hooks (`useAuth`, `useNotifications`, `useBreakpoints`)
  - Provides: i18n infrastructure (react-i18next, translation files in `public/locales/de/` and `public/locales/en/`)
  - Provides: Zustand stores (`authStore`, `uiStore`), React Query configuration, Material-UI theme
  - Provides: Routing with ProtectedRoute, responsive breakpoints, accessibility (WCAG 2.1 AA)
  - **Location**: Components in `web-frontend/src/components/shared/`, hooks in `web-frontend/src/hooks/`

- **Depends on**: Story 1.9 (Error Handling Essentials) - **DONE** ✅
  - Provides: Standardized error response format `{ timestamp, path, status, error, message, correlationId }`
  - Provides: Exception hierarchy (ValidationException, NotFoundException, UnauthorizedException, ServiceException)
  - Provides: Error codes (ERR_VALIDATION, ERR_NOT_FOUND, ERR_UNAUTHORIZED, ERR_SERVICE)
  - Provides: Correlation ID propagation via `X-Correlation-ID` header and MDC
  - **Frontend Impact**: Use error response format in API client error interceptor, display correlation IDs in error messages

**Backend Dependencies:**
- **Depends on**: Story 1.11 (Security & Compliance Essentials) for JWT authentication and input validation
- **Depends on**: Story 1.16.2 (Eliminate UUIDs from API) for meaningful ID pattern (eventCode, username, companyName)
- **Depends on**: Story 2.2 (Event Management Service Core + API Consolidation) - **PENDING**
  - Provides: Event CRUD APIs, workflow management, slot assignment, overflow management
  - Provides: Consolidated API with `?include=workflow,speakers,sessions,venue` resource expansion

**Service Integration:**
- **Consumes**: Event Management Service consolidated APIs with `?include=workflow,speakers,sessions,venue` resource expansion
- **Consumes**: Speaker Coordination Service for speaker details
- **Consumes**: Venue Management for venue selection
- **Consumes**: User Management Service for team assignments
- **Integrates with**: API Gateway for authentication and routing

**Important**: This story uses **meaningful IDs only** (no UUIDs) as defined in Story 1.16.2:
- Events identified by `eventCode` (e.g., "BATbern56")
- Users identified by `username` (e.g., "john.doe")
- Companies identified by `name` (e.g., "GoogleZH")
- UUIDs remain internal to database layer only

**Reusable Foundation from Story 1.17:**
- ✅ BaseLayout component (already implemented)
- ✅ AppHeader with role-adaptive navigation (already implemented)
- ✅ NavigationMenu with organizer menu items (already implemented)
- ✅ i18n with react-i18next (German/English) (already implemented)
- ✅ API client with auth interceptors (already implemented)
- ✅ ErrorBoundary for component errors (already implemented)
- ✅ Zustand stores for UI state (already implemented)
- ✅ React Query for server state (already implemented)

## Requirements Context

### Related Functional Requirements
- **FR2**: 16-step workflow management with intelligent task sequencing
- **FR5**: Progressive event publishing with automatic content updates
- **FR12**: Multi-year venue reservations, catering coordination, logistics management
- **FR17**: Intelligent speaker matching and assignment tracking
- **FR19**: Progressive publishing engine with content validation
- **FR20**: Intelligent notification system with role-based alerts
- **NFR-Performance**: Page load <2s, interactions <200ms
- **NFR-UX**: WCAG 2.1 AA accessibility compliance
- **NFR-Responsive**: Mobile, tablet, desktop support

### Workflow Steps (if applicable)
Event Management 16-Step Workflow (FR2): All 16 steps visualized and managed through this frontend

### Acceptance Criteria Source
Epic 2, Story 2.5.2 (derived from wireframes `docs/wireframes/story-1.16-*.md`)

## Architecture Context

### Architecture Patterns
[Source: architecture/05-frontend-architecture.md]
- **Component Architecture**: Atomic design with presentational/container separation
- **State Management**:
  - Zustand for UI state (filters, modals, view mode, selected event)
  - React Query for server state (events, workflow, speakers, cache management)
- **Routing**: React Router v6 with lazy loading
- **Form Management**: React Hook Form with Zod validation
- **Data Visualization**: Recharts for workflow progress, analytics charts

[Source: architecture/04-api-event-management.md]
- **API Client**: Axios with interceptors for auth/error handling
- **Resource Expansion**: Uses `?include=workflow,speakers,sessions,venue,registrations` to reduce API calls (90% reduction)
- **Filtering**: JSON filter syntax from Story 1.15a.1
- **Caching**: React Query with 5-15min staleTime for event data based on volatility

[Source: architecture/03-data-architecture.md#event-data-model]
- **Event Entity**: UUID identifier, title, description, eventDate, eventType, status, workflowState
- **TypeScript Types**: Generated from OpenAPI specifications

### Infrastructure Components
[Source: architecture/02-infrastructure-deployment.md]

**Frontend Deployment:**
- **S3 + CloudFront**: Static React app hosting
- **Environment Config**: Runtime environment variables via window.__ENV__
- **API Base URL**: Configured per environment (local, staging, production)

**CDN Integration:**
- **Event Images**: Served via CloudFront with 1-year max-age
- **Assets**: Vite build with content hashing for cache busting

## Wireframe Context

### Wireframe References
**Primary Wireframes**:
1. `docs/wireframes/story-1.16-event-management-dashboard.md` v1.0 (Event Dashboard)
2. `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Event Detail/Edit Screen)
3. `docs/wireframes/story-1.16-event-settings.md` (Event Settings)
4. `docs/wireframes/story-1.16-workflow-visualization.md` (Workflow Progress)

**Screens Implemented:**
1. **Event Management Dashboard** (main organizer screen)
   - Active events pipeline with progress bars
   - Critical tasks list with action buttons
   - Team activity feed
   - Quick action sidebar

2. **Event Detail/Edit Screen**
   - Event information form (title, description, date, type, theme)
   - Workflow progress and key metrics
   - Venue & logistics management
   - Assigned topics list
   - Speakers & sessions table
   - Team assignments
   - Publishing configuration
   - Notifications & reminders
   - Action buttons (save, draft, delete)

3. **Event Settings Modal**
   - Advanced event configuration
   - Integration settings
   - Access permissions

4. **Workflow Visualization Screen**
   - 16-step workflow progress
   - Step-by-step details
   - Bottleneck identification

### UI Components

**Page Components:**
- `EventManagementDashboard.tsx` - Main organizer dashboard with event pipeline
- `EventDetailEdit.tsx` - Comprehensive event editing interface
- `EventSettingsModal.tsx` - Event settings configuration
- `WorkflowVisualization.tsx` - 16-step workflow view

**Feature Components:**
- `EventList.tsx` - Active events pipeline with cards
- `EventCard.tsx` - Individual event card with progress bar
- `EventForm.tsx` - Create/edit event form
- `EventSearch.tsx` - Event search and filters
- `WorkflowProgressBar.tsx` - Visual workflow progress with percentage
- `CriticalTasksList.tsx` - Prioritized critical tasks with actions
- `TeamActivityFeed.tsx` - Real-time team activity
- `VenueSelector.tsx` - Venue selection dropdown with booking
- `TopicsList.tsx` - Assigned topics with management
- `SpeakersSessionsTable.tsx` - Speakers and sessions grid
- `TeamAssignments.tsx` - Team member role assignments
- `PublishingConfig.tsx` - Progressive publishing settings
- `NotificationRules.tsx` - Notification automation rules

**Shared Components (from Story 1.17 - React Frontend Foundation):**
- `FormField`, `SelectField`, `TextAreaField` (form inputs)
- `LoadingSpinner`, `ErrorMessage` (feedback)
- `ConfirmDialog`, `ToastNotification` (user feedback)
- `ResponsiveGrid`, `ResponsiveCard` (layout)

**Material-UI Components Used:**
- TextField, Select, Checkbox, Switch, Button, DatePicker
- Card, CardContent, CardActions, CardHeader
- Grid, Stack, Box, Container
- Dialog, DialogTitle, DialogContent, DialogActions
- Tabs, Tab, Chip, Badge, Avatar, LinearProgress
- Stepper, Step, StepLabel (workflow visualization)
- Skeleton (loading states)

**State Management:**
- **Zustand Store**: `useEventStore` (filters, view mode, selected event, modal state)
- **React Query**:
  - `useEvents` (list with filters, cache 5min)
  - `useEvent` (detail with includes, cache 15min - full data with workflow/speakers)
  - `useEventWorkflow` (workflow state, cache 10min)
  - `useCriticalTasks` (tasks list, cache 3min)
  - `useTeamActivity` (activity feed, cache 2min)
  - `useCreateEvent`, `useUpdateEvent`, `useDeleteEvent` (mutations)

## Acceptance Criteria

1. **Event Dashboard Display** (Main Screen)
   - Display active events pipeline with progress bars (65%, 15%, etc.)
   - Show workflow step (Step 7/16, Step 2/16) for each event
   - Display critical tasks count with priority indicators (⚠️, 🔴)
   - Implement team activity feed with real-time updates (manual reload)
   - Quick actions sidebar ([+ New Event], [📊 Analytics], [👥 Speakers], etc.)

2. **Event List & Filters**
   - Filter events by status (active, published, completed, archived)
   - Filter by year and event type
   - Search events by title
   - Sort by date, status, workflow progress
   - URL persistence for filters (shareable links)

3. **Create Event Form**
   - Modal form with event fields (title, description, date, type, theme, capacity)
   - Event type selection (Full Day, Afternoon, Evening)
   - Date picker with validation (min 30 days in future)
   - Registration deadline picker (min 7 days before event)
   - Venue selection dropdown
   - "Save Draft" button (allows incomplete data)
   - "Save & Create" button (full validation)

4. **Edit Event Form**
   - Pre-fill form with existing event data
   - Inline editing for title, description, theme
   - Auto-save enabled (5-second debounce) - NOT configurable
   - Partial update support (only changed fields sent to backend)
   - Unsaved changes warning on modal close
   - Role-based access (Organizers only)

5. **Workflow Progress Display**
   - Progress bar showing completion percentage
   - Current step indicator (Step X/16: Step Name)
   - Clickable progress bar navigates to Workflow Visualization
   - Warning indicators for blockers (⚠️)
   - [View Workflow Details] button

6. **Venue & Logistics Management**
   - Venue selection dropdown with details (address, capacity, amenities)
   - Booking status display (✓ Confirmed, Pending, Not Booked)
   - [Change Venue] button opens venue selector
   - Catering configuration section
   - [Configure Menu] button opens catering form

7. **Topic Management**
   - Display assigned topics (3 topics with cards)
   - Show topic usage history (Last used: BATbern #48)
   - Partner votes count display
   - [View] button shows topic details modal
   - [Remove] button unassigns topic
   - [+ Add Topic from Backlog] navigates to topic backlog

8. **Speakers & Sessions Display**
   - Table/grid showing speakers and sessions (12 slots)
   - Slot time display (09:00-10:00, 10:15-11:15, etc.)
   - Speaker name and company
   - Session title
   - Materials status (Complete ✓, Pending ⚠️, Missing ❌)
   - [View Details], [Edit Slot], [Materials] action buttons
   - [View Full Agenda] button
   - [Manage Speaker Assignments] button
   - [Auto-Assign Speakers] AI algorithm button

9. **Team Assignments**
   - Lead Organizer display (read-only)
   - Co-Organizers list
   - Moderator dropdown assignment
   - Content Reviewer dropdown assignment
   - [Manage Team Assignments] button

10. **Publishing Configuration**
    - Publishing strategy display (Progressive, Immediate)
    - Current phase indicator
    - Publishing timeline with checkpoints (✓ completed, ○ pending)
    - Quality checkpoints list
    - [Configure Publishing], [Republish Event], [Preview Public Page] buttons

11. **Notifications & Reminders**
    - Active automations count
    - Notification rules list with status
    - [Manage Notifications], [Send Manual Notification] buttons

12. **Critical Tasks**
    - Display prioritized tasks (overdue materials, venue confirmation, abstracts moderation)
    - Urgency indicators (⚠️ warning, 🔴 critical)
    - Inline action buttons ([Contact], [Extend Deadline], [Confirm], [Assign to Team])
    - Task completion updates task count

13. **Team Activity Feed**
    - Display team actions with timestamps
    - User attribution (Mark assigned, Anna completed, System auto-sent)
    - Activity type icons
    - Filter by user, event, action type
    - Manual reload to see new activity

14. **Performance**
    - Initial page load <2s (First Contentful Paint)
    - Event detail with all includes <500ms (Caffeine-cached <50ms)
    - Form submission feedback <200ms
    - Skeleton loading states for all data fetching
    - React Query caching reduces redundant API calls (90% reduction via `?include=`)

15. **Responsive Design**
    - Mobile-first design with breakpoints (xs: 0px, sm: 600px, md: 900px, lg: 1200px, xl: 1536px)
    - Collapsible sections on mobile (workflow, venue, topics, speakers)
    - Stacked layout for forms on mobile/tablet
    - Touch-friendly buttons (min 44x44px)
    - Bottom sheet for action menus on mobile

16. **Accessibility (WCAG 2.1 AA)**
    - All interactive elements keyboard accessible (Tab navigation)
    - ARIA labels for all buttons and inputs
    - Focus indicators (2px solid border)
    - Screen reader support (form labels, error announcements)
    - Color contrast ratio 4.5:1 minimum
    - Status indicators use both color and icon

17. **Error Handling**
    - Display user-friendly error messages for all API failures
    - Retry buttons for failed requests
    - Offline mode with cached data display
    - Validation errors shown inline with red text
    - Network error banner at top of screen
    - Concurrent edit conflict dialog (version mismatch detection)

18. **State Management**
    - Zustand for UI state (filters persist across navigation)
    - React Query for server state (automatic refetch on window focus)
    - Optimistic updates for mutations (immediate UI feedback)
    - Cache invalidation on event create/update/delete
    - Filter/sort state persists in URL query parameters

19. **Integration with Services**
    - Call Event Management Service for CRUD operations
    - Call Speaker Coordination Service for speaker details
    - Call Venue Management for venue selection
    - Call User Management for team assignments
    - Use consolidated APIs with `?include=workflow,speakers,sessions,venue` to reduce API calls
    - Handle 401 (unauthorized), 403 (forbidden), 404 (not found), 500 (server error)

20. **Auto-Save Functionality**
    - Auto-save always enabled for all organizers (5-second debounce)
    - NOT configurable per user (stakeholder decision from wireframe v1.1)
    - Visual indicator showing "Saving..." and "Last saved at [timestamp]"
    - Error banner if auto-save fails
    - Auto-save conflict detection (concurrent edit warning)

21. **Delete Event**
    - [Delete Event] button with organizer-only access
    - Deletion impact dialog shows counts (registrations, speakers, materials)
    - Validation: Cannot delete if registrations exist (must cancel event instead)
    - Organizer confirmation required (no admin approval needed per wireframe v1.1)
    - On success: Navigate back to dashboard with success message
    - On validation failure: Show "Cannot delete - cancel event instead" error

22. **Internationalization (i18n) - German/English** (Foundation from Story 1.17)
    - All UI labels, buttons, headings, and help text fully translated using react-i18next
    - Translation namespace: `events` (separate from `common`, `auth`, `validation`)
    - Translation files: `public/locales/de/events.json` and `public/locales/en/events.json`
    - Language switcher in AppHeader from Story 1.17 changes all event UI text
    - Type-safe translation keys with TypeScript autocomplete (generated from JSON files)
    - Date/time formatting with locale support (date-fns with de/enUS locales)
    - Number formatting with Swiss locale (CHF currency, 1'500.50 format)
    - Error messages translated (validation errors, API errors, confirmation dialogs)
    - No hardcoded strings in any component - all text via `t('events.key')`
    - Language preference persists via Story 1.17's language switcher (LocalStorage + User Profile API)

23. **Navigation Integration** (Foundation from Story 1.17)
    - Event Management menu items added to NavigationMenu from Story 1.17
    - Navigation items: "Events", "Create Event", "Event Timeline" (child items)
    - Menu items visible only for Organizer role (role-adaptive from Story 1.17)
    - Active route highlighting when on event pages
    - Navigation labels fully translated (German/English)
    - Breadcrumbs show current event context (e.g., "Events > BATbern56 > Edit")
    - Mobile hamburger menu includes event navigation (responsive from Story 1.17)

24. **Error Handling Integration** (Foundation from Story 1.9)
    - API errors display correlation ID from `X-Correlation-ID` response header
    - Error response format: `{ timestamp, path, status, error, message, correlationId }`
    - Error codes mapped to user-friendly messages:
      - ERR_VALIDATION → "Invalid input. Please check your data."
      - ERR_NOT_FOUND → "Event not found."
      - ERR_UNAUTHORIZED → "You don't have permission to access this event."
      - ERR_SERVICE → "An unexpected error occurred. Please try again."
    - Global error toast from Story 1.17 shows translated error messages
    - ErrorBoundary from Story 1.17 catches component-level errors
    - Retry buttons for failed API requests
    - Correlation ID included in error logs for debugging

## Test Specifications (TDD)

### Test Scenarios by Acceptance Criteria

**AC1 Tests: Event Dashboard Display**
- Test 1.1: should_renderEventDashboard_when_eventsLoaded
- Test 1.2: should_displayProgressBars_when_eventsHaveWorkflow
- Test 1.3: should_displayCriticalTasksCount_when_tasksExist
- Test 1.4: should_displayTeamActivityFeed_when_activityExists
- Test 1.5: should_renderQuickActions_when_dashboardLoaded

**AC2 Tests: Event List & Filters**
- Test 2.1: should_filterEventsByStatus_when_statusFilterSelected
- Test 2.2: should_filterEventsByYear_when_yearFilterSelected
- Test 2.3: should_searchEventsByTitle_when_searchInputTyped
- Test 2.4: should_sortEventsByDate_when_sortSelected
- Test 2.5: should_persistFiltersInURL_when_filtersChanged

**AC3 Tests: Create Event Form**
- Test 3.1: should_openModal_when_createEventButtonClicked
- Test 3.2: should_validateRequiredFields_when_formSubmitted
- Test 3.3: should_validateEventDate_when_dateTooSoon
- Test 3.4: should_validateRegistrationDeadline_when_deadlineTooClose
- Test 3.5: should_allowSaveDraft_when_incompleteDataProvided
- Test 3.6: should_callCreateAPI_when_saveButtonClicked

**AC4 Tests: Edit Event Form**
- Test 4.1: should_prefillForm_when_editButtonClicked
- Test 4.2: should_enableAutoSave_when_fieldChanged (5s debounce, not configurable)
- Test 4.3: should_sendPartialUpdate_when_onlyTitleChanged
- Test 4.4: should_showUnsavedChangesWarning_when_modalClosedWithChanges
- Test 4.5: should_restrictAccess_when_nonOrganizerEdits

**AC5 Tests: Workflow Progress Display**
- Test 5.1: should_displayProgressBar_when_workflowLoaded
- Test 5.2: should_showCurrentStep_when_workflowInProgress
- Test 5.3: should_navigateToWorkflow_when_progressBarClicked
- Test 5.4: should_displayWarningIndicators_when_blockersExist

**AC6 Tests: Venue & Logistics Management**
- Test 6.1: should_displayVenueDetails_when_venueSelected
- Test 6.2: should_showBookingStatus_when_venueBooked
- Test 6.3: should_openVenueSelector_when_changeVenueClicked
- Test 6.4: should_openCateringConfig_when_configureMenuClicked

**AC7 Tests: Topic Management**
- Test 7.1: should_displayAssignedTopics_when_topicsExist
- Test 7.2: should_showTopicHistory_when_topicViewed
- Test 7.3: should_unassignTopic_when_removeClicked
- Test 7.4: should_navigateToBacklog_when_addTopicClicked

**AC8 Tests: Speakers & Sessions Display**
- Test 8.1: should_displaySpeakersTable_when_speakersLoaded
- Test 8.2: should_showMaterialsStatus_when_statusAvailable
- Test 8.3: should_openSessionDetails_when_viewDetailsClicked
- Test 8.4: should_openSlotEditor_when_editSlotClicked
- Test 8.5: should_triggerAutoAssign_when_autoAssignClicked

**AC9 Tests: Team Assignments**
- Test 9.1: should_displayLeadOrganizer_when_teamLoaded
- Test 9.2: should_allowModeratorAssignment_when_organizerRole
- Test 9.3: should_callAssignmentAPI_when_moderatorSelected
- Test 9.4: should_navigateToTeamManagement_when_manageClicked

**AC10 Tests: Publishing Configuration**
- Test 10.1: should_displayPublishingStrategy_when_configLoaded
- Test 10.2: should_showPublishingTimeline_when_phasesExist
- Test 10.3: should_openPublishingConfig_when_configureClicked
- Test 10.4: should_triggerRepublish_when_republishClicked

**AC11 Tests: Notifications & Reminders**
- Test 11.1: should_displayAutomationCount_when_rulesExist
- Test 11.2: should_listNotificationRules_when_rulesLoaded
- Test 11.3: should_navigateToNotifications_when_manageClicked

**AC12 Tests: Critical Tasks**
- Test 12.1: should_displayPrioritizedTasks_when_tasksLoaded
- Test 12.2: should_showUrgencyIndicators_when_criticalTasks
- Test 12.3: should_executeInlineAction_when_contactClicked
- Test 12.4: should_updateTaskCount_when_taskCompleted

**AC13 Tests: Team Activity Feed**
- Test 13.1: should_displayTeamActions_when_activityLoaded
- Test 13.2: should_showUserAttribution_when_actionsExist
- Test 13.3: should_filterActivity_when_filterApplied
- Test 13.4: should_reloadActivity_when_manualRefresh

**AC14 Tests: Performance**
- Test 14.1: should_loadDashboardUnder2Seconds_when_initialLoad
- Test 14.2: should_loadEventDetailUnder500ms_when_uncached
- Test 14.3: should_loadEventDetailUnder50ms_when_cached
- Test 14.4: should_showSkeletonLoader_when_dataFetching
- Test 14.5: should_useCachedData_when_reactQueryCacheHit

**AC15 Tests: Responsive Design**
- Test 15.1: should_stackSections_when_mobileViewport
- Test 15.2: should_collapseFilters_when_mobileViewport
- Test 15.3: should_showBottomSheet_when_touchDevice

**AC16 Tests: Accessibility**
- Test 16.1: should_navigateWithKeyboard_when_tabPressed
- Test 16.2: should_announceErrors_when_validationFails
- Test 16.3: should_haveFocusIndicators_when_elementFocused
- Test 16.4: should_useColorAndIcon_when_statusDisplayed

**AC17 Tests: Error Handling**
- Test 17.1: should_displayErrorMessage_when_apiFails
- Test 17.2: should_showRetryButton_when_networkError
- Test 17.3: should_displayOfflineBanner_when_networkUnavailable
- Test 17.4: should_showConcurrentEditDialog_when_versionMismatch

**AC18 Tests: State Management**
- Test 18.1: should_persistFiltersInStore_when_navigatingAway
- Test 18.2: should_invalidateCache_when_eventCreated
- Test 18.3: should_updateUIOptimistically_when_eventUpdated
- Test 18.4: should_persistStateInURL_when_filtersChanged

**AC19 Tests: Service Integration**
- Test 19.1: should_callEventService_when_fetchingEvents
- Test 19.2: should_callSpeakerService_when_loadingSpeakers
- Test 19.3: should_callVenueService_when_loadingVenues
- Test 19.4: should_useResourceExpansion_when_loadingDetail (?include= parameter)

**AC20 Tests: Auto-Save Functionality**
- Test 20.1: should_autoSaveAfter5Seconds_when_fieldChanged
- Test 20.2: should_notAllowDisablingAutoSave_when_userPreference (always enabled)
- Test 20.3: should_showSavingIndicator_when_autoSaveTriggered
- Test 20.4: should_showLastSavedTimestamp_when_autoSaveCompletes
- Test 20.5: should_detectConflict_when_concurrentEdit

**AC21 Tests: Delete Event**
- Test 21.1: should_showDeleteButton_when_organizerRole
- Test 21.2: should_displayImpactDialog_when_deleteClicked
- Test 21.3: should_blockDeletion_when_registrationsExist
- Test 21.4: should_requireConfirmation_when_deleteConfirmed (no admin approval)
- Test 21.5: should_navigateToDashboard_when_deleteSuccessful

**AC22 Tests: Internationalization (i18n)**
- Test 22.1: should_renderGermanText_when_localeIsDe
- Test 22.2: should_renderEnglishText_when_localeIsEn
- Test 22.3: should_updateUIText_when_languageSwitcherUsed
- Test 22.4: should_formatDate_when_localeDe (15. März 2024, 14:30)
- Test 22.5: should_formatDate_when_localeEn (March 15, 2024, 2:30 PM)
- Test 22.6: should_formatCurrency_when_localeDe (CHF 1'500.50)
- Test 22.7: should_translateErrorMessages_when_validationFails
- Test 22.8: should_haveNoHardcodedStrings_when_componentsRendered
- Test 22.9: should_haveTypeaheadForTranslationKeys_when_typingInIDE
- Test 22.10: should_persistLanguagePreference_when_languageChanged

**AC23 Tests: Navigation Integration**
- Test 23.1: should_displayEventsMenuItem_when_organizerRole
- Test 23.2: should_hideEventsMenuItem_when_speakerRole
- Test 23.3: should_highlightActiveRoute_when_onEventPage
- Test 23.4: should_showTranslatedLabels_when_languageChanges
- Test 23.5: should_displayBreadcrumbs_when_onEventDetailPage
- Test 23.6: should_includeEventsInHamburger_when_mobileView
- Test 23.7: should_navigateToEventDashboard_when_eventsMenuClicked

**AC24 Tests: Error Handling Integration**
- Test 24.1: should_displayCorrelationId_when_apiErrorOccurs
- Test 24.2: should_showUserFriendlyMessage_when_validationError (ERR_VALIDATION)
- Test 24.3: should_showUserFriendlyMessage_when_notFoundError (ERR_NOT_FOUND)
- Test 24.4: should_showUserFriendlyMessage_when_unauthorizedError (ERR_UNAUTHORIZED)
- Test 24.5: should_showUserFriendlyMessage_when_serviceError (ERR_SERVICE)
- Test 24.6: should_displayErrorToast_when_apiCallFails
- Test 24.7: should_catchComponentError_when_errorBoundaryUsed
- Test 24.8: should_showRetryButton_when_requestFails
- Test 24.9: should_logCorrelationId_when_errorLogged

### Test File Locations

**Component Tests:**
- `web-frontend/src/components/organizer/EventManagement/__tests__/EventManagementDashboard.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/EventList.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/EventCard.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/EventForm.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/EventDetailEdit.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/WorkflowProgressBar.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/VenueSelector.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/TopicsList.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/SpeakersSessionsTable.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/CriticalTasksList.test.tsx`
- `web-frontend/src/components/organizer/EventManagement/__tests__/TeamActivityFeed.test.tsx`

**Hook Tests:**
- `web-frontend/src/hooks/__tests__/useEvents.test.ts`
- `web-frontend/src/hooks/__tests__/useEvent.test.ts`
- `web-frontend/src/hooks/__tests__/useEventWorkflow.test.ts`
- `web-frontend/src/hooks/__tests__/useCriticalTasks.test.ts`
- `web-frontend/src/hooks/__tests__/useTeamActivity.test.ts`
- `web-frontend/src/hooks/__tests__/useEventMutations.test.ts`

**Store Tests:**
- `web-frontend/src/stores/__tests__/eventStore.test.ts`

**Service Tests:**
- `web-frontend/src/services/__tests__/eventService.test.ts`
- `web-frontend/src/services/__tests__/eventApiClient.test.ts`

**E2E Tests:**
- `web-frontend/e2e/workflows/event-management/event-dashboard.spec.ts`
- `web-frontend/e2e/workflows/event-management/event-create.spec.ts`
- `web-frontend/e2e/workflows/event-management/event-edit.spec.ts`
- `web-frontend/e2e/workflows/event-management/event-detail.spec.ts`
- `web-frontend/e2e/workflows/event-management/workflow-visualization.spec.ts`

### Test Data & Mocks

**Test Data Builders:**
- EventTestDataBuilder for creating test events
- WorkflowTestDataBuilder for workflow state data
- SpeakerTestDataBuilder for speaker/session data
- VenueTestDataBuilder for venue data

**Mock Services:**
- Mock Event Management Service API responses (GET, POST, PUT, DELETE, PATCH)
- Mock Speaker Coordination Service API responses
- Mock Venue Management Service API responses
- Mock User Management Service API responses
- Mock workflow state transitions
- Mock critical tasks data

**Mock Server:**
- MSW (Mock Service Worker) for API mocking in tests
- Handlers for all Event/Speaker/Venue/User endpoints

**Test Fixtures:**
- events.fixture.ts (sample event data with workflow states)
- speakers.fixture.ts (sample speaker and session data)
- venues.fixture.ts (sample venue data)
- tasks.fixture.ts (sample critical tasks)
- activity.fixture.ts (sample team activity feed)

## Tasks / Subtasks (TDD Workflow)

**IMPORTANT**: Tasks are ordered to respect dependencies. Infrastructure (API client, hooks, stores, i18n) must be built BEFORE components that depend on them.

- [ ] **Task 1: Write E2E Tests for Event Management (RED Phase)**
  - [ ] Write failing E2E test for event dashboard display
  - [ ] Write failing E2E test for event creation workflow
  - [ ] Write failing E2E test for event edit workflow with auto-save
  - [ ] Write failing E2E test for workflow visualization
  - [ ] Verify tests fail with meaningful error messages

- [ ] **Task 2: Set Up Project Structure (Preparation)**
  - [ ] Create organizer component directory at `web-frontend/src/components/organizer/EventManagement/`
  - [ ] Set up TypeScript types at `web-frontend/src/types/event.types.ts`
  - [ ] Generate OpenAPI types at `web-frontend/src/types/generated/events-api.types.ts` using `openapi-typescript`
  - [ ] Create event hooks directory at `web-frontend/src/hooks/` (placeholder for Tasks 7)
  - [ ] Create event store file at `web-frontend/src/stores/eventStore.ts` (placeholder for Task 6)
  - [ ] Create event services files at `web-frontend/src/services/eventService.ts` and `eventApiClient.ts` (placeholder for Task 5)

- [ ] **Task 3: i18n Translation Files (AC: 22)** ⬆️ MOVED UP - Required by all components
  - [ ] Create `public/locales/de/events.json` with all German translations
  - [ ] Create `public/locales/en/events.json` with all English translations
  - [ ] Include translations for: navigation, dashboard, forms, buttons, labels, validation errors, API errors, confirmation dialogs, workflow, autoSave
  - [ ] Update `src/i18n/config.ts` to include 'events' namespace in namespace list
  - [ ] Generate TypeScript types from translation JSON files (per Story 1.17 pattern)
  - [ ] Test type-safe translation keys in IDE (autocomplete should work)
  - [ ] **DO NOT test components yet** - just verify i18n infrastructure works

- [ ] **Task 4: Navigation Integration & Routing (AC: 23)** ⬆️ MOVED UP - Required for routing
  - [ ] Update `src/config/navigationConfig.ts` to add Event Management menu items
  - [ ] Add "Events" main menu item with EventIcon and path `/organizer/events`
  - [ ] Add child menu items: "Event Dashboard" (/organizer/events), "Create Event" (/organizer/events/create), "Event Timeline" (/organizer/events/timeline)
  - [ ] Use i18n translation keys for menu labels (e.g., `t('events.navigation.dashboard')`)
  - [ ] Add route definitions to React Router configuration
  - [ ] Wrap event routes with ProtectedRoute from Story 1.17 (Organizer role only)
  - [ ] Test role-adaptive navigation (Events menu only shows for Organizer)
  - [ ] Test navigation label translation (German/English)
  - [ ] Integrate with BaseLayout from Story 1.17

- [ ] **Task 5a: API Client TDD (RED Phase) (AC: 19)** ⬆️ MOVED UP - Required by hooks
  - [ ] Write failing tests for eventApiClient methods
  - [ ] Write failing tests for error handling (401, 403, 404, 500)
  - [ ] Write failing tests for auth token injection
  - [ ] Write failing tests for resource expansion (?include= parameter)
  - [ ] Write failing tests for partial update (PATCH)

- [ ] **Task 5b: API Client Implementation (GREEN Phase) (AC: 19)** ⬆️ MOVED UP - Required by hooks
  - [ ] Implement eventApiClient with Axios
  - [ ] Implement request/response interceptors
  - [ ] Implement error handling and transformation (use Story 1.9 error format)
  - [ ] Implement resource expansion query builder
  - [ ] Implement PATCH support for partial updates

- [ ] **Task 6a: Zustand Store TDD (RED Phase) (AC: 18)** ⬆️ MOVED UP - Required by components
  - [ ] Write failing tests for eventStore (filters, view mode, selected event)
  - [ ] Write failing tests for filter persistence
  - [ ] Write failing tests for modal state management

- [ ] **Task 6b: Zustand Store Implementation (GREEN Phase) (AC: 18)** ⬆️ MOVED UP - Required by components
  - [ ] Implement eventStore with filter state
  - [ ] Implement localStorage persistence (filters and viewMode only)
  - [ ] Implement modal open/close state

- [ ] **Task 7a: React Query Hooks TDD (RED Phase) (AC: 14, 19)** ⬆️ MOVED UP - Required by components
  - [ ] Write failing tests for useEvents hook
  - [ ] Write failing tests for useEvent hook (with includes)
  - [ ] Write failing tests for useEventWorkflow hook
  - [ ] Write failing tests for useCriticalTasks hook
  - [ ] Write failing tests for useTeamActivity hook
  - [ ] Write failing tests for useCreateEvent, useUpdateEvent, useDeleteEvent
  - [ ] Write failing tests for cache invalidation

- [ ] **Task 7b: React Query Hooks Implementation (GREEN Phase) (AC: 14, 19)** ⬆️ MOVED UP - Required by components
  - [ ] Implement useEvents with filter/sort/pagination (cache 5min)
  - [ ] Implement useEvent with resource expansion (?include=workflow,speakers,sessions,venue,registrations) (cache 15min)
  - [ ] Implement useEventWorkflow (cache 10min)
  - [ ] Implement useCriticalTasks (cache 3min)
  - [ ] Implement useTeamActivity (cache 2min)
  - [ ] Implement mutation hooks with optimistic updates
  - [ ] Implement cache invalidation strategies

---
**NOW INFRASTRUCTURE IS READY → Start Building Components** (Tasks 8-23)
---

- [ ] **Task 8a: Event Dashboard Components TDD (RED Phase) (AC: 1, 2)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-management-dashboard.md` v1.0
  - **Implements**: Main organizer dashboard with active events pipeline, critical tasks, team activity
  - [ ] Write failing tests for EventManagementDashboard component
  - [ ] Write failing tests for EventList component (active events pipeline)
  - [ ] Write failing tests for EventCard component (progress bar, workflow step)
  - [ ] Write failing tests for EventSearch component (filters)
  - [ ] Write failing tests for CriticalTasksList component
  - [ ] Write failing tests for TeamActivityFeed component

- [ ] **Task 8b: Event Dashboard Components Implementation (GREEN Phase) (AC: 1, 2)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-management-dashboard.md` v1.0
  - [ ] Implement EventManagementDashboard routing and layout
  - [ ] Implement EventList with event cards
  - [ ] Implement EventCard with progress bar and workflow step
  - [ ] Implement EventSearch with filters and URL persistence
  - [ ] Implement CriticalTasksList with inline actions
  - [ ] Implement TeamActivityFeed with manual reload

- [ ] **Task 9a: Event Form Components TDD (RED Phase) (AC: 3, 4)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Event Information section)
  - **Implements**: Event create/edit modal form with validation and auto-save
  - [ ] Write failing tests for EventForm component (create/edit modes)
  - [ ] Write failing tests for form validation (date, deadline, capacity)
  - [ ] Write failing tests for auto-save functionality (5s debounce, always enabled)
  - [ ] Write failing tests for unsaved changes warning
  - [ ] Write failing tests for partial update (PATCH)

- [ ] **Task 9b: Event Form Components Implementation (GREEN Phase) (AC: 3, 4)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Event Information section)
  - [ ] Implement EventForm with React Hook Form
  - [ ] Implement date validation with Zod schema (min 30 days future)
  - [ ] Implement auto-save with 5-second debounce (not configurable)
  - [ ] Implement modal state management (create/edit)
  - [ ] Implement role-based access control
  - [ ] Implement partial update support (PATCH for changed fields only)

- [ ] **Task 10a: Workflow & Metrics Components TDD (RED Phase) (AC: 5)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-workflow-visualization.md` + `story-1.16-event-detail-edit.md` v1.1 (Workflow Progress section)
  - **Implements**: 16-step workflow progress bar with step indicators and warnings
  - [ ] Write failing tests for WorkflowProgressBar component
  - [ ] Write failing tests for workflow step indicator
  - [ ] Write failing tests for progress bar click navigation
  - [ ] Write failing tests for warning indicators (⚠️)

- [ ] **Task 10b: Workflow & Metrics Components Implementation (GREEN Phase) (AC: 5)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-workflow-visualization.md` + `story-1.16-event-detail-edit.md` v1.1
  - [ ] Implement WorkflowProgressBar with percentage calculation
  - [ ] Implement current step indicator (Step X/16: Name)
  - [ ] Implement clickable progress bar navigation
  - [ ] Implement warning indicators for blockers

- [ ] **Task 11a: Venue & Logistics Components TDD (RED Phase) (AC: 6)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Venue & Logistics section)
  - **Implements**: Venue selection dropdown, booking status, catering configuration
  - [ ] Write failing tests for VenueSelector component
  - [ ] Write failing tests for venue booking status display
  - [ ] Write failing tests for catering configuration

- [ ] **Task 11b: Venue & Logistics Components Implementation (GREEN Phase) (AC: 6)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Venue & Logistics section)
  - [ ] Implement VenueSelector with dropdown and details
  - [ ] Implement booking status display (✓ Confirmed, Pending)
  - [ ] Implement catering configuration modal

- [ ] **Task 12a: Topic Management Components TDD (RED Phase) (AC: 7)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Assigned Topics section)
  - **Implements**: Topic cards with usage history, partner votes, add/remove actions
  - [ ] Write failing tests for TopicsList component
  - [ ] Write failing tests for topic cards with history
  - [ ] Write failing tests for topic removal
  - [ ] Write failing tests for add topic navigation

- [ ] **Task 12b: Topic Management Components Implementation (GREEN Phase) (AC: 7)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Assigned Topics section)
  - [ ] Implement TopicsList with cards
  - [ ] Implement topic usage history display
  - [ ] Implement topic removal with confirmation
  - [ ] Implement add topic backlog navigation

- [ ] **Task 13a: Speakers & Sessions Components TDD (RED Phase) (AC: 8)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Speakers & Sessions section)
  - **Implements**: 12-slot speakers/sessions table with materials status and auto-assign
  - [ ] Write failing tests for SpeakersSessionsTable component
  - [ ] Write failing tests for session details modal
  - [ ] Write failing tests for slot editor
  - [ ] Write failing tests for materials status
  - [ ] Write failing tests for auto-assign speakers

- [ ] **Task 13b: Speakers & Sessions Components Implementation (GREEN Phase) (AC: 8)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Speakers & Sessions section)
  - [ ] Implement SpeakersSessionsTable with grid view
  - [ ] Implement session details modal
  - [ ] Implement slot editor with time picker
  - [ ] Implement materials status indicators (Complete ✓, Pending ⚠️)
  - [ ] Implement auto-assign speakers AI integration

- [ ] **Task 14a: Team Assignments Components TDD (RED Phase) (AC: 9)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Team Assignments section)
  - **Implements**: Lead organizer display, co-organizers list, moderator/reviewer dropdowns
  - [ ] Write failing tests for TeamAssignments component
  - [ ] Write failing tests for moderator assignment
  - [ ] Write failing tests for reviewer assignment

- [ ] **Task 14b: Team Assignments Components Implementation (GREEN Phase) (AC: 9)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Team Assignments section)
  - [ ] Implement TeamAssignments with dropdowns
  - [ ] Implement moderator assignment with API call
  - [ ] Implement reviewer assignment with API call

- [ ] **Task 15a: Publishing & Notifications Components TDD (RED Phase) (AC: 10, 11)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Publishing Config + Notifications sections)
  - **Implements**: Progressive publishing timeline, notification rules, manual notifications
  - [ ] Write failing tests for PublishingConfig component
  - [ ] Write failing tests for NotificationRules component
  - [ ] Write failing tests for manual notification

- [ ] **Task 15b: Publishing & Notifications Components Implementation (GREEN Phase) (AC: 10, 11)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Publishing Config + Notifications sections)
  - [ ] Implement PublishingConfig with timeline
  - [ ] Implement NotificationRules with automation list
  - [ ] Implement manual notification composer

- [ ] **Task 16a: Auto-Save Implementation TDD (RED Phase) (AC: 20)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Auto-save indicator at bottom)
  - **Implements**: 5-second auto-save with "Saving..." and "Last saved at" indicators (always enabled)
  - [ ] Write failing tests for auto-save trigger (5s debounce)
  - [ ] Write failing tests for "Saving..." indicator
  - [ ] Write failing tests for "Last saved at" timestamp
  - [ ] Write failing tests for auto-save error handling
  - [ ] Write failing tests for concurrent edit conflict

- [ ] **Task 16b: Auto-Save Implementation (GREEN Phase) (AC: 20)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Auto-save indicator at bottom)
  - [ ] Implement auto-save debounce logic (5 seconds, always enabled)
  - [ ] Implement saving indicator UI
  - [ ] Implement last saved timestamp display
  - [ ] Implement auto-save error banner
  - [ ] Implement concurrent edit conflict dialog

- [ ] **Task 17a: Delete Event Implementation TDD (RED Phase) (AC: 21)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Delete button + confirmation dialog)
  - **Implements**: Delete button with impact analysis dialog and organizer-only confirmation
  - [ ] Write failing tests for delete button visibility (organizer-only)
  - [ ] Write failing tests for deletion impact dialog
  - [ ] Write failing tests for registration validation (cannot delete)
  - [ ] Write failing tests for organizer confirmation (no admin approval)
  - [ ] Write failing tests for success navigation

- [ ] **Task 17b: Delete Event Implementation (GREEN Phase) (AC: 21)**
  - **📐 Wireframe**: `docs/wireframes/story-1.16-event-detail-edit.md` v1.1 (Delete button + confirmation dialog)
  - [ ] Implement delete button with RBAC
  - [ ] Implement deletion impact API call
  - [ ] Implement registration validation
  - [ ] Implement confirmation dialog (organizer-only)
  - [ ] Implement success navigation to dashboard

- [ ] **Task 18: Responsive Design Implementation (AC: 15)**
  - **📐 Wireframes**: ALL wireframes (dashboard, detail/edit, workflow visualization)
  - **Implements**: Mobile-first responsive design with collapsible sections and touch-friendly UI
  - [ ] Implement mobile breakpoints with Material-UI sx prop
  - [ ] Implement collapsible sections for mobile
  - [ ] Implement bottom sheet for actions on mobile
  - [ ] Implement touch-friendly buttons (min 44x44px)
  - [ ] Test on mobile/tablet/desktop viewports

- [ ] **Task 19: Accessibility Implementation (AC: 16)**
  - **📐 Wireframes**: ALL wireframes (applies to all components)
  - **Implements**: WCAG 2.1 AA compliance with keyboard navigation and screen reader support
  - [ ] Implement keyboard navigation (Tab, Enter, Escape)
  - [ ] Add ARIA labels to all interactive elements
  - [ ] Implement focus indicators (2px solid border)
  - [ ] Test with screen reader (NVDA/VoiceOver)
  - [ ] Verify color contrast ratios (WCAG 2.1 AA)
  - [ ] Implement status indicators with color + icon

- [ ] **Task 20: Error Handling & Loading States (AC: 17, 24)**
  - **📐 Wireframes**: ALL wireframes (applies to all components)
  - **Implements**: Skeleton loaders, user-friendly error messages with correlation IDs, retry buttons
  - [ ] Implement skeleton loaders for all data fetching
  - [ ] Implement user-friendly error messages (use Story 1.9 error format)
  - [ ] Display correlation IDs from X-Correlation-ID header
  - [ ] Implement retry buttons for failed requests
  - [ ] Implement offline mode with cached data display
  - [ ] Implement network error banner
  - [ ] Implement concurrent edit conflict dialog

- [ ] **Task 21: Performance Optimization (AC: 14)**
  - **📐 Wireframes**: ALL wireframes (applies to all components)
  - **Implements**: Code splitting, bundle optimization, lazy loading, Core Web Vitals monitoring
  - [ ] Implement React.lazy for code splitting
  - [ ] Optimize bundle size (analyze with webpack-bundle-analyzer)
  - [ ] Implement image lazy loading
  - [ ] Add performance monitoring (Core Web Vitals)
  - [ ] Verify <2s initial load, <200ms interactions
  - [ ] Verify <500ms event detail load (uncached), <50ms (cached)

- [ ] **Task 22: Integration Testing**
  - **📐 Wireframes**: ALL wireframes (E2E workflows across all screens)
  - **Tests**: Full integration with Event/Speaker/Venue/User Management Services
  - [ ] Test Event Management Service API integration end-to-end
  - [ ] Test Speaker Coordination Service API integration
  - [ ] Test Venue Management Service API integration
  - [ ] Test User Management Service API integration
  - [ ] Test resource expansion (?include=) performance
  - [ ] Test error scenarios (API failures, network errors)

- [ ] **Task 23: E2E Testing and Bug Fixes (REFACTOR)**
  - **📐 Wireframes**: ALL wireframes (complete user journeys from Task 1)
  - **Tests**: Verify all E2E tests from Task 1 pass (GREEN phase)
  - [ ] Run all E2E tests from Task 1 (verify GREEN)
  - [ ] Fix any failing tests
  - [ ] Refactor components for maintainability
  - [ ] Optimize performance (React.memo, useMemo, useCallback)
  - [ ] Final code review and cleanup

## Dev Notes - Implementation Guide

### i18n Translation File Structure (AC: 22)

**File: public/locales/de/events.json**
```json
{
  "navigation": {
    "dashboard": "Veranstaltungen",
    "createEvent": "Neue Veranstaltung",
    "eventTimeline": "Zeitachse"
  },
  "dashboard": {
    "title": "Veranstaltungsverwaltung",
    "activeEvents": "Aktive Veranstaltungen",
    "criticalTasks": "Kritische Aufgaben",
    "teamActivity": "Teamaktivität",
    "quickActions": "Schnellzugriff"
  },
  "form": {
    "createEvent": "Neue Veranstaltung erstellen",
    "editEvent": "Veranstaltung bearbeiten",
    "title": "Titel",
    "description": "Beschreibung",
    "eventDate": "Veranstaltungsdatum",
    "eventType": "Veranstaltungstyp",
    "capacity": "Kapazität",
    "save": "Speichern",
    "cancel": "Abbrechen",
    "delete": "Löschen"
  },
  "validation": {
    "titleRequired": "Titel ist erforderlich",
    "titleMinLength": "Titel muss mindestens 10 Zeichen lang sein",
    "descriptionRequired": "Beschreibung ist erforderlich",
    "eventDateFuture": "Veranstaltungsdatum muss mindestens 30 Tage in der Zukunft liegen",
    "registrationDeadline": "Anmeldefrist muss mindestens 7 Tage vor der Veranstaltung sein"
  },
  "errors": {
    "loadFailed": "Fehler beim Laden der Veranstaltungen",
    "saveFailed": "Fehler beim Speichern der Veranstaltung",
    "deleteFailed": "Fehler beim Löschen der Veranstaltung",
    "notFound": "Veranstaltung nicht gefunden",
    "unauthorized": "Sie haben keine Berechtigung für diese Veranstaltung",
    "validationError": "Ungültige Eingabe. Bitte überprüfen Sie Ihre Daten.",
    "serviceError": "Ein unerwarteter Fehler ist aufgetreten. Bitte versuchen Sie es erneut.",
    "correlationId": "Korrelations-ID"
  },
  "confirmations": {
    "deleteTitle": "Veranstaltung löschen?",
    "deleteMessage": "Möchten Sie diese Veranstaltung wirklich löschen?",
    "deleteImpact": "{{registrations}} Anmeldungen, {{speakers}} Referenten, {{materials}} Materialien",
    "cannotDelete": "Veranstaltung kann nicht gelöscht werden - bitte stornieren Sie stattdessen",
    "unsavedChanges": "Sie haben ungespeicherte Änderungen. Möchten Sie wirklich fortfahren?",
    "confirm": "Bestätigen",
    "cancel": "Abbrechen"
  },
  "workflow": {
    "progress": "Fortschritt",
    "step": "Schritt {{current}} von {{total}}",
    "viewDetails": "Details anzeigen",
    "warning": "Warnung",
    "blocker": "Blocker"
  },
  "autoSave": {
    "saving": "Speichern...",
    "saved": "Zuletzt gespeichert um {{time}}",
    "failed": "Automatisches Speichern fehlgeschlagen",
    "conflict": "Gleichzeitige Bearbeitung erkannt. Bitte laden Sie neu und versuchen Sie es erneut."
  }
}
```

**File: public/locales/en/events.json**
```json
{
  "navigation": {
    "dashboard": "Events",
    "createEvent": "Create Event",
    "eventTimeline": "Timeline"
  },
  "dashboard": {
    "title": "Event Management",
    "activeEvents": "Active Events",
    "criticalTasks": "Critical Tasks",
    "teamActivity": "Team Activity",
    "quickActions": "Quick Actions"
  },
  "form": {
    "createEvent": "Create New Event",
    "editEvent": "Edit Event",
    "title": "Title",
    "description": "Description",
    "eventDate": "Event Date",
    "eventType": "Event Type",
    "capacity": "Capacity",
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete"
  },
  "validation": {
    "titleRequired": "Title is required",
    "titleMinLength": "Title must be at least 10 characters",
    "descriptionRequired": "Description is required",
    "eventDateFuture": "Event date must be at least 30 days in the future",
    "registrationDeadline": "Registration deadline must be at least 7 days before event"
  },
  "errors": {
    "loadFailed": "Failed to load events",
    "saveFailed": "Failed to save event",
    "deleteFailed": "Failed to delete event",
    "notFound": "Event not found",
    "unauthorized": "You don't have permission to access this event",
    "validationError": "Invalid input. Please check your data.",
    "serviceError": "An unexpected error occurred. Please try again.",
    "correlationId": "Correlation ID"
  },
  "confirmations": {
    "deleteTitle": "Delete Event?",
    "deleteMessage": "Are you sure you want to delete this event?",
    "deleteImpact": "{{registrations}} registrations, {{speakers}} speakers, {{materials}} materials",
    "cannotDelete": "Cannot delete event - please cancel event instead",
    "unsavedChanges": "You have unsaved changes. Are you sure you want to continue?",
    "confirm": "Confirm",
    "cancel": "Cancel"
  },
  "workflow": {
    "progress": "Progress",
    "step": "Step {{current}} of {{total}}",
    "viewDetails": "View Details",
    "warning": "Warning",
    "blocker": "Blocker"
  },
  "autoSave": {
    "saving": "Saving...",
    "saved": "Last saved at {{time}}",
    "failed": "Auto-save failed",
    "conflict": "Concurrent edit detected. Please reload and try again."
  }
}
```

**Usage in Components:**
```typescript
import { useTranslation } from 'react-i18next';

const EventManagementDashboard: React.FC = () => {
  const { t } = useTranslation('events');

  return (
    <Container>
      <Typography variant="h4">{t('dashboard.title')}</Typography>
      <Button>{t('form.createEvent')}</Button>
    </Container>
  );
};
```

### Foundational Story Integration

**This story builds directly on the following completed foundation stories:**

1. **Story 1.9 (Error Handling Essentials) - DONE**
   - Use `ErrorResponse` format from shared-kernel: `{ timestamp, path, status, error, message, correlationId }`
   - Display correlation IDs from `X-Correlation-ID` response header in error messages
   - Map error codes (ERR_VALIDATION, ERR_NOT_FOUND, ERR_UNAUTHORIZED, ERR_SERVICE) to user-friendly messages
   - Use `GlobalExceptionHandler` error response structure in API client error interceptor

2. **Story 1.11 (Security & Compliance Essentials) - READY FOR REVIEW**
   - JWT tokens automatically added to requests via axios interceptor (from API Gateway SecurityConfig)
   - Security headers (CSP, HSTS, X-Frame-Options) applied by API Gateway SecurityHeadersFilter
   - Input validation uses backend @Valid annotations with 400 error responses
   - Rate limiting handled by API Gateway RateLimitingFilter (60 req/min)
   - All form inputs sanitized and validated client-side before submission

3. **Story 2.2 (Event Management Service Core + API Consolidation) - PENDING**
   - Backend REST APIs: `GET/POST/PUT/PATCH/DELETE /api/v1/events`
   - Event workflow endpoint: `GET/PUT /api/v1/events/{id}/workflow`
   - Slot management endpoints: `GET/POST /api/v1/events/{id}/slots`
   - Overflow management: `GET/POST /api/v1/events/{id}/overflow`
   - Topic backlog: `GET/POST /api/v1/topics/backlog`
   - Consolidated API with `?include=workflow,speakers,sessions,venue,registrations` (90% API call reduction)
   - Caffeine-cached responses (5-15min TTL) for <50ms cached, <500ms uncached response times

4. **Story 1.17 (React Frontend Foundation) - DONE**
   - React 19+ with TypeScript, Material-UI 7.x, Vite 7.x
   - Shared components: FormField, LoadingSpinner, ErrorMessage, ConfirmDialog, ToastNotification
   - React Router v6 with role-based route guards
   - Axios client with auth interceptor and error handling
   - React Query for server state management
   - Zustand for UI state (filters, modals)
   - i18next for internationalization (German/English)

### Service Setup

**Frontend Project Structure:**
```
web-frontend/src/
├── components/
│   ├── organizer/
│   │   └── EventManagement/             # Organizer-specific event components
│   │       ├── EventManagementDashboard.tsx    # Main organizer dashboard
│   │       ├── EventList.tsx                   # Active events pipeline
│   │       ├── EventCard.tsx                   # Individual event card
│   │       ├── EventForm.tsx                   # Create/edit modal form
│   │       ├── EventDetailEdit.tsx             # Comprehensive detail view
│   │       ├── EventSearch.tsx                 # Search and filters
│   │       ├── WorkflowProgressBar.tsx         # Progress visualization
│   │       ├── CriticalTasksList.tsx           # Prioritized tasks
│   │       ├── TeamActivityFeed.tsx            # Team activity
│   │       ├── VenueSelector.tsx               # Venue dropdown
│   │       ├── TopicsList.tsx                  # Assigned topics
│   │       ├── SpeakersSessionsTable.tsx       # Speakers grid
│   │       ├── TeamAssignments.tsx             # Team roles
│   │       ├── PublishingConfig.tsx            # Publishing settings
│   │       └── NotificationRules.tsx           # Notification automation
├── hooks/
│   ├── useEvents.ts                     # List query hook
│   ├── useEvent.ts                      # Detail query hook (with includes)
│   ├── useEventWorkflow.ts              # Workflow query hook
│   ├── useCriticalTasks.ts              # Tasks query hook
│   ├── useTeamActivity.ts               # Activity feed hook
│   └── useEventMutations.ts             # CRUD mutations
├── stores/
│   └── eventStore.ts                    # Zustand store (UI state)
├── services/
│   ├── eventService.ts                  # Business logic
│   └── eventApiClient.ts                # API client
├── types/
│   ├── event.types.ts                   # TypeScript interfaces
│   └── generated/
│       └── events-api.types.ts          # OpenAPI-generated types
└── utils/
    ├── eventValidation.ts               # Validation helpers
    └── eventHelpers.ts                  # Helper functions
```

**Architecture Notes (per 05-frontend-architecture.md):**
- Components organized by role (`organizer/`, `speaker/`, `partner/`, `attendee/`)
- Event management is organizer-specific, so components go in `components/organizer/EventManagement/`
- Hooks, services, stores, types, and utils are shared across the app (top-level directories)
- TypeScript types generated from OpenAPI specs via `openapi-typescript` in `types/generated/`

### Technical Design Notes

**1. TypeScript Types (types/event.types.ts)**

**Type Generation Strategy (per 05-frontend-architecture.md):**
- Use `openapi-typescript` to generate types from OpenAPI specs
- Generated types are committed to repository and kept in sync via git hooks
- Application types derive from generated types

```bash
# Generate types from OpenAPI spec
npm run generate:api-types
# Generates: src/types/generated/events-api.types.ts
```

```typescript
// types/event.types.ts - Derive from OpenAPI-generated types
import type { components } from './generated/events-api.types';

// Derive application types from OpenAPI-generated types
export type Event = components['schemas']['EventResponse'];
export type EventDetail = components['schemas']['EventDetail'];
export type EventWorkflow = components['schemas']['EventWorkflow'];
export type Session = components['schemas']['SessionResponse'];
export type CreateEventRequest = components['schemas']['CreateEventRequest'];
export type UpdateEventRequest = components['schemas']['UpdateEventRequest'];

// Note: All IDs are meaningful identifiers (eventCode, username, companyName) per Story 1.16.2
// UUIDs are internal to database layer only, never exposed in API/frontend

// Extended interfaces for frontend-specific needs
export interface Event {
  id: string; // eventCode (e.g., "BATbern56")
  eventNumber: number;
  title: string;
  description: string;
  eventDate: string; // ISO date string
  eventType: 'full_day' | 'afternoon' | 'evening';
  status: EventStatus;
  workflowState: WorkflowState;
  theme?: string;
  registrationDeadline: string;
  capacity: number;
  currentAttendeeCount: number;
  publishedAt?: string;
  createdAt: string;
  updatedAt: string;
  createdBy: string; // username (e.g., "john.doe")
  version: number; // For concurrent edit detection
}

export interface EventDetail extends Event {
  workflow?: EventWorkflow;
  venue?: Venue;
  catering?: Catering;
  topics?: Topic[];
  sessions?: Session[];
  team?: TeamAssignments;
  publishing?: PublishingConfig;
  notifications?: NotificationRule[];
  analytics?: EventAnalytics;
}

export interface EventWorkflow {
  currentStep: number; // 1-16
  completionPercentage: number;
  stepName: string;
  nextSteps: string[];
  blockers: string[];
  warnings: string[];
}

export interface Session {
  id: string; // sessionSlug (e.g., "microservices-theory-practice")
  eventId: string; // eventCode (e.g., "BATbern56")
  slotNumber: number;
  startTime: string;
  endTime: string;
  speaker?: Speaker;
  title: string;
  materialsStatus: 'complete' | 'pending' | 'missing';
}

export interface EventAnalytics {
  confirmedSpeakersCount: number;
  totalSpeakersCount: number;
  assignedTopicsCount: number;
  pendingMaterialsCount: number;
  registrationCount: number;
  registrationCapacity: number;
  budgetAmount?: number;
}

export enum EventStatus {
  DRAFT = 'draft',
  ACTIVE = 'active',
  PUBLISHED = 'published',
  COMPLETED = 'completed',
  ARCHIVED = 'archived',
  CANCELLED = 'cancelled'
}

export enum WorkflowState {
  TOPIC_SELECTION = 'topic_selection',
  SPEAKER_RESEARCH = 'speaker_research',
  CONTENT_REVIEW = 'content_review',
  // ... 13 more steps
  AGENDA_FINALIZATION = 'agenda_finalization'
}

export interface CreateEventRequest {
  title: string;
  description: string;
  eventDate: string;
  eventType: 'full_day' | 'afternoon' | 'evening';
  theme?: string;
  capacity: number;
  registrationDeadline: string;
  venueId: string; // venueCode or venueName (meaningful ID)
  organizerId?: string; // username (e.g., "john.doe") - auto-set from auth if not provided
}

export interface UpdateEventRequest extends Partial<CreateEventRequest> {
  version: number; // For optimistic concurrency control
}

export interface EventFilters {
  status?: EventStatus;
  year?: number;
  eventType?: string;
  searchQuery?: string;
}

export interface EventStore {
  filters: EventFilters;
  selectedEventCode?: string; // eventCode (e.g., "BATbern56")
  isCreateModalOpen: boolean;
  isEditModalOpen: boolean;
  setFilters: (filters: EventFilters) => void;
  setSelectedEventCode: (eventCode?: string) => void;
  openCreateModal: () => void;
  closeCreateModal: () => void;
  openEditModal: (eventCode: string) => void;
  closeEditModal: () => void;
}
```

**2. React Query Hooks (hooks/useEvents.ts)**

```typescript
import { useQuery } from '@tanstack/react-query';
import { eventApiClient } from '../services/eventApiClient';
import { EventDetail, EventFilters } from '../types/event.types';

export const useEvents = (filters: EventFilters, page: number = 1, limit: number = 20) => {
  return useQuery({
    queryKey: ['events', filters, page, limit],
    queryFn: () => eventApiClient.getEvents(filters, page, limit),
    staleTime: 5 * 60 * 1000, // 5 minutes
    cacheTime: 10 * 60 * 1000, // 10 minutes
    keepPreviousData: true, // For pagination
  });
};

export const useEvent = (eventCode: string, include?: string[]) => {
  return useQuery({
    queryKey: ['event', eventCode, include],
    queryFn: () => eventApiClient.getEvent(eventCode, include),
    staleTime: 15 * 60 * 1000, // 15 minutes (full event with includes cached longer)
    enabled: !!eventCode,
  });
};

export const useEventWorkflow = (eventCode: string) => {
  return useQuery({
    queryKey: ['eventWorkflow', eventCode],
    queryFn: () => eventApiClient.getEventWorkflow(eventCode),
    staleTime: 10 * 60 * 1000, // 10 minutes
    enabled: !!eventCode,
  });
};

export const useCriticalTasks = (organizerId: string) => {
  return useQuery({
    queryKey: ['criticalTasks', organizerId],
    queryFn: () => eventApiClient.getCriticalTasks(organizerId),
    staleTime: 3 * 60 * 1000, // 3 minutes (more volatile)
    enabled: !!organizerId,
  });
};

export const useTeamActivity = (organizerId: string, limit: number = 20) => {
  return useQuery({
    queryKey: ['teamActivity', organizerId, limit],
    queryFn: () => eventApiClient.getTeamActivity(organizerId, limit),
    staleTime: 2 * 60 * 1000, // 2 minutes (very volatile)
    enabled: !!organizerId,
  });
};

export const useCreateEvent = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateEventRequest) => eventApiClient.createEvent(data),
    onSuccess: () => {
      queryClient.invalidateQueries(['events']);
      toast.success('Event created successfully');
    },
    onError: (error: AxiosError) => {
      toast.error(error.response?.data?.message || 'Failed to create event');
    },
  });
};

export const useUpdateEvent = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ eventCode, data }: { eventCode: string; data: UpdateEventRequest }) =>
      eventApiClient.updateEvent(eventCode, data),
    onMutate: async ({ eventCode, data }) => {
      // Optimistic update
      await queryClient.cancelQueries(['event', eventCode]);
      const previousEvent = queryClient.getQueryData(['event', eventCode]);
      queryClient.setQueryData(['event', eventCode], (old: Event) => ({ ...old, ...data }));
      return { previousEvent };
    },
    onSuccess: (_, { eventCode }) => {
      queryClient.invalidateQueries(['events']);
      queryClient.invalidateQueries(['event', eventCode]);
      toast.success('Event updated successfully');
    },
    onError: (error, { eventCode }, context) => {
      queryClient.setQueryData(['event', eventCode], context.previousEvent);
      if (error.response?.status === 409) {
        toast.error('Concurrent edit detected. Please reload and try again.');
      } else {
        toast.error('Failed to update event');
      }
    },
  });
};

export const useDeleteEvent = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (eventCode: string) => eventApiClient.deleteEvent(eventCode),
    onSuccess: () => {
      queryClient.invalidateQueries(['events']);
      toast.success('Event deleted successfully');
    },
    onError: (error: AxiosError) => {
      if (error.response?.status === 400 && error.response?.data?.message.includes('registrations')) {
        toast.error('Cannot delete event with registrations. Please cancel event instead.');
      } else {
        toast.error('Failed to delete event');
      }
    },
  });
};
```

**3. Zustand Store (stores/eventStore.ts)**

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { EventStore, EventFilters } from '../types/event.types';

export const useEventStore = create<EventStore>()(
  persist(
    (set) => ({
      filters: {},
      selectedEventCode: undefined,
      isCreateModalOpen: false,
      isEditModalOpen: false,

      setFilters: (filters) => set({ filters }),
      setSelectedEventCode: (eventCode) => set({ selectedEventCode: eventCode }),
      openCreateModal: () => set({ isCreateModalOpen: true }),
      closeCreateModal: () => set({ isCreateModalOpen: false }),
      openEditModal: (eventCode) => set({ selectedEventCode: eventCode, isEditModalOpen: true }),
      closeEditModal: () => set({ isEditModalOpen: false, selectedEventCode: undefined }),
    }),
    {
      name: 'event-store', // LocalStorage key
      partialPersist: (state) => ({ filters: state.filters }), // Only persist filters
    }
  )
);
```

**4. API Client (services/eventApiClient.ts)**

```typescript
import axios, { AxiosError } from 'axios';
import { Event, EventDetail, CreateEventRequest, UpdateEventRequest, EventFilters, EventWorkflow } from '../types/event.types';

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
});

// Request interceptor for auth token
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('authToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Response interceptor for error handling
apiClient.interceptors.response.use(
  (response) => response,
  (error: AxiosError) => {
    if (error.response?.status === 401) {
      // Redirect to login
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export const eventApiClient = {
  getEvents: async (filters: EventFilters, page: number, limit: number): Promise<{ data: Event[]; pagination: any }> => {
    const params = new URLSearchParams();
    params.append('page', String(page));
    params.append('limit', String(limit));

    if (filters.status) {
      params.append('status', filters.status);
    }
    if (filters.year) {
      params.append('year', String(filters.year));
    }
    if (filters.eventType) {
      params.append('eventType', filters.eventType);
    }
    if (filters.searchQuery) {
      params.append('search', filters.searchQuery);
    }

    const response = await apiClient.get(`/api/v1/events?${params.toString()}`);
    return response.data;
  },

  getEvent: async (eventCode: string, include?: string[]): Promise<EventDetail> => {
    const params = include?.length ? `?include=${include.join(',')}` : '';
    const response = await apiClient.get(`/api/v1/events/${eventCode}${params}`);
    return response.data;
  },

  createEvent: async (data: CreateEventRequest): Promise<Event> => {
    const response = await apiClient.post('/api/v1/events', data);
    return response.data;
  },

  updateEvent: async (eventCode: string, data: UpdateEventRequest): Promise<Event> => {
    const response = await apiClient.patch(`/api/v1/events/${eventCode}`, data);
    return response.data;
  },

  deleteEvent: async (eventCode: string): Promise<void> => {
    await apiClient.delete(`/api/v1/events/${eventCode}`);
  },

  getEventWorkflow: async (eventCode: string): Promise<EventWorkflow> => {
    const response = await apiClient.get(`/api/v1/events/${eventCode}/workflow`);
    return response.data;
  },

  getCriticalTasks: async (username: string, limit: number = 10): Promise<any[]> => {
    // username is the organizer's username (e.g., "john.doe")
    const response = await apiClient.get(`/api/v1/organizers/${username}/tasks/critical?limit=${limit}`);
    return response.data;
  },

  getTeamActivity: async (username: string, limit: number = 20): Promise<any[]> => {
    // username is the organizer's username (e.g., "john.doe")
    const response = await apiClient.get(`/api/v1/organizers/${username}/activity-feed?limit=${limit}`);
    return response.data;
  },

  getDeletionImpact: async (eventCode: string): Promise<any> => {
    const response = await apiClient.get(`/api/v1/events/${eventCode}/deletion-impact`);
    return response.data;
  },
};
```

**5. Event Form Component with Auto-Save (components/EventForm.tsx)**

```typescript
import React, { useEffect, useState } from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
  TextField,
  Button,
  Select,
  MenuItem,
  FormControl,
  InputLabel,
  Grid,
  Typography,
} from '@mui/material';
import { DatePicker } from '@mui/x-date-pickers';
import { CreateEventRequest, UpdateEventRequest, Event } from '../types/event.types';
import { useDebounce } from '../hooks/useDebounce';

const eventSchema = z.object({
  title: z.string().min(10, 'Title must be at least 10 characters').max(200),
  description: z.string().min(50, 'Description must be at least 50 characters').max(2000),
  eventDate: z.date()
    .refine((date) => {
      const minDate = new Date();
      minDate.setDate(minDate.getDate() + 30);
      return date >= minDate;
    }, 'Event date must be at least 30 days in the future'),
  eventType: z.enum(['full_day', 'afternoon', 'evening']),
  theme: z.string().max(100).optional(),
  capacity: z.number().min(20).max(1000),
  registrationDeadline: z.date(),
  venueId: z.string().uuid(),
}).refine((data) => {
  const eventDate = new Date(data.eventDate);
  const deadline = new Date(data.registrationDeadline);
  const minDeadline = new Date(eventDate);
  minDeadline.setDate(minDeadline.getDate() - 7);
  return deadline <= minDeadline;
}, {
  message: 'Registration deadline must be at least 7 days before event date',
  path: ['registrationDeadline'],
});

interface EventFormProps {
  open: boolean;
  mode: 'create' | 'edit';
  initialData?: Event;
  onClose: () => void;
  onSubmit: (data: CreateEventRequest | UpdateEventRequest) => void;
}

export const EventForm: React.FC<EventFormProps> = ({ open, mode, initialData, onClose, onSubmit }) => {
  const [lastSaved, setLastSaved] = useState<Date | null>(null);
  const [isSaving, setIsSaving] = useState(false);

  const { control, handleSubmit, formState: { errors, isDirty }, watch } = useForm({
    resolver: zodResolver(eventSchema),
    defaultValues: initialData || {},
  });

  // Auto-save logic (5-second debounce, always enabled)
  const formValues = watch();
  const debouncedFormValues = useDebounce(formValues, 5000);

  useEffect(() => {
    if (mode === 'edit' && isDirty && debouncedFormValues) {
      setIsSaving(true);
      onSubmit({
        ...debouncedFormValues,
        version: initialData?.version || 0,
      });
      setLastSaved(new Date());
      setIsSaving(false);
    }
  }, [debouncedFormValues, mode, isDirty]);

  const handleClose = () => {
    if (isDirty) {
      const confirmed = window.confirm('You have unsaved changes. Are you sure you want to close?');
      if (!confirmed) return;
    }
    onClose();
  };

  return (
    <Dialog open={open} onClose={handleClose} maxWidth="md" fullWidth>
      <DialogTitle>{mode === 'create' ? 'Create New Event' : 'Edit Event'}</DialogTitle>
      <form onSubmit={handleSubmit(onSubmit)}>
        <DialogContent>
          <Grid container spacing={2}>
            <Grid item xs={12}>
              <Controller
                name="title"
                control={control}
                render={({ field }) => (
                  <TextField
                    {...field}
                    label="Event Title *"
                    fullWidth
                    error={!!errors.title}
                    helperText={errors.title?.message}
                  />
                )}
              />
            </Grid>
            <Grid item xs={12}>
              <Controller
                name="description"
                control={control}
                render={({ field }) => (
                  <TextField
                    {...field}
                    label="Description *"
                    multiline
                    rows={4}
                    fullWidth
                    error={!!errors.description}
                    helperText={errors.description?.message || `${field.value?.length || 0}/2000 characters`}
                  />
                )}
              />
            </Grid>
            <Grid item xs={12} sm={6}>
              <Controller
                name="eventDate"
                control={control}
                render={({ field }) => (
                  <DatePicker
                    {...field}
                    label="Event Date *"
                    slotProps={{
                      textField: {
                        fullWidth: true,
                        error: !!errors.eventDate,
                        helperText: errors.eventDate?.message,
                      },
                    }}
                  />
                )}
              />
            </Grid>
            <Grid item xs={12} sm={6}>
              <Controller
                name="eventType"
                control={control}
                render={({ field }) => (
                  <FormControl fullWidth error={!!errors.eventType}>
                    <InputLabel>Event Type *</InputLabel>
                    <Select {...field} label="Event Type *">
                      <MenuItem value="full_day">Full Day</MenuItem>
                      <MenuItem value="afternoon">Afternoon</MenuItem>
                      <MenuItem value="evening">Evening</MenuItem>
                    </Select>
                  </FormControl>
                )}
              />
            </Grid>
            {/* Additional fields... */}
          </Grid>

          {mode === 'edit' && (
            <Typography variant="caption" color="text.secondary" sx={{ mt: 2, display: 'block' }}>
              {isSaving ? '💾 Saving...' : lastSaved ? `Last saved at ${lastSaved.toLocaleTimeString()}` : 'Auto-save enabled'}
            </Typography>
          )}
        </DialogContent>
        <DialogActions>
          <Button onClick={handleClose}>Cancel</Button>
          <Button type="submit" variant="contained" color="primary">
            {mode === 'create' ? 'Save & Create' : 'Save Changes'}
          </Button>
        </DialogActions>
      </form>
    </Dialog>
  );
};
```

### API Contracts

**OpenAPI Specification (consumed from Event Management Service):**

```yaml
/api/v1/events:
  get:
    summary: List events with filters
    parameters:
      - name: status
        in: query
        schema:
          type: string
          enum: [draft, active, published, completed, archived]
      - name: year
        in: query
        schema:
          type: integer
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: limit
        in: query
        schema:
          type: integer
          default: 20
    responses:
      '200':
        description: Paginated event list
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: array
                  items:
                    $ref: '#/components/schemas/Event'
                pagination:
                  type: object

/api/v1/events/{eventCode}:
  get:
    summary: Get event by eventCode
    parameters:
      - name: eventCode
        in: path
        required: true
        description: Event code identifier (e.g., "BATbern56")
        schema:
          type: string
          pattern: '^BATbern[0-9]+$'
          example: 'BATbern56'
      - name: include
        in: query
        schema:
          type: string
          example: 'workflow,speakers,sessions,venue,registrations'
    responses:
      '200':
        description: Event detail with expanded resources
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventDetail'

  patch:
    summary: Partial update event
    parameters:
      - name: eventCode
        in: path
        required: true
        description: Event code identifier (e.g., "BATbern56")
        schema:
          type: string
          pattern: '^BATbern[0-9]+$'
          example: 'BATbern56'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              title:
                type: string
              description:
                type: string
              version:
                type: integer
                description: For optimistic concurrency control
    responses:
      '200':
        description: Event updated
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Event'
      '409':
        description: Concurrent edit conflict (version mismatch)
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: 'Concurrent edit detected'
                currentVersion:
                  type: integer

  delete:
    summary: Delete event
    parameters:
      - name: eventCode
        in: path
        required: true
        description: Event code identifier (e.g., "BATbern56")
        schema:
          type: string
          pattern: '^BATbern[0-9]+$'
          example: 'BATbern56'
    responses:
      '204':
        description: Event deleted
      '400':
        description: Cannot delete (registrations exist)
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: 'Cannot delete event with registrations'

/api/v1/events/{eventCode}/deletion-impact:
  get:
    summary: Get deletion impact analysis
    parameters:
      - name: eventCode
        in: path
        required: true
        description: Event code identifier (e.g., "BATbern56")
        schema:
          type: string
          pattern: '^BATbern[0-9]+$'
          example: 'BATbern56'
    responses:
      '200':
        description: Deletion impact data
        content:
          application/json:
            schema:
              type: object
              properties:
                registrationCount:
                  type: integer
                confirmedSpeakers:
                  type: integer
                associatedMaterials:
                  type: integer
                canDelete:
                  type: boolean
```

### Testing Requirements

**Unit Test Coverage: >80%**
- All React components with Jest + React Testing Library
- All hooks with @testing-library/react-hooks
- All services and utilities with Jest

**Integration Test Coverage:**
- API client integration with MSW (Mock Service Worker)
- Form submission workflows
- State management integration
- Auto-save functionality with debounce
- Concurrent edit conflict resolution

**E2E Test Coverage:**
- Event dashboard display and filtering
- Event creation workflow
- Event edit workflow with auto-save
- Workflow visualization navigation
- Delete event with validation
- Error scenarios (API failures, validation errors, concurrent edits)

**Performance Requirements:**
- Initial page load: <2s (Lighthouse score >90)
- Event detail with all includes: <500ms uncached, <50ms cached (P95)
- Form submission: <200ms feedback
- Auto-save: 5-second debounce trigger

## Definition of Done Checklist

### Development Complete
- [ ] All tests written BEFORE implementation (TDD followed)
- [ ] All acceptance criteria have corresponding tests
- [ ] All acceptance criteria implemented
- [ ] Unit tests written and passing (>80% coverage)
- [ ] Integration tests cover new functionality
- [ ] E2E tests pass for user journeys
- [ ] Code follows project conventions (ESLint, Prettier)
- [ ] TypeScript types properly defined
- [ ] No TypeScript errors or warnings
- [ ] Components follow atomic design principles

### Frontend Complete
- [ ] React components match wireframe designs (story-1.16-*.md)
- [ ] Responsive design tested (mobile/tablet/desktop)
- [ ] Accessibility requirements met (WCAG 2.1 AA)
- [ ] Performance metrics achieved (Core Web Vitals, Lighthouse >90)
- [ ] Error handling and loading states implemented
- [ ] All user interactions provide feedback
- [ ] Forms have proper validation and error messages
- [ ] Auto-save functionality working (5s debounce, always enabled)

### Integration Complete
- [ ] API Gateway routes tested end-to-end
- [ ] Authentication working with token refresh
- [ ] Event Management Service integration verified
- [ ] Speaker Coordination Service integration verified (read-only)
- [ ] Venue Management Service integration verified
- [ ] User Management Service integration verified
- [ ] React Query caching working correctly (5-15min TTL)
- [ ] Resource expansion (?include=) reducing API calls (90% reduction)
- [ ] Error scenarios handled gracefully
- [ ] Concurrent edit conflict resolution working

### Review Ready
- [ ] PR created with detailed description
- [ ] Code review completed by team
- [ ] UI/UX review completed
- [ ] Accessibility review passed
- [ ] Documentation updated (README, component docs)
- [ ] CLAUDE.md updated if needed
- [ ] Deployment instructions documented

### i18n & Navigation Integration Complete (NEW)
- [ ] Translation files created (public/locales/de/events.json, public/locales/en/events.json)
- [ ] All UI text uses translation keys (no hardcoded strings)
- [ ] Language switcher changes all event UI text (German/English)
- [ ] Date/time formatting uses locale-specific formats (de, enUS)
- [ ] Number/currency formatting uses Swiss locale (CHF 1'500.50)
- [ ] TypeScript autocomplete works for translation keys
- [ ] Navigation menu includes Event Management items (Organizer role only)
- [ ] Navigation labels translated (German/English)
- [ ] Active route highlighting works on event pages
- [ ] Breadcrumbs display event context
- [ ] Mobile hamburger menu includes event navigation

### Error Handling Integration Complete (NEW)
- [ ] API errors display correlation IDs from X-Correlation-ID header
- [ ] Error codes mapped to user-friendly messages (ERR_VALIDATION, ERR_NOT_FOUND, ERR_UNAUTHORIZED, ERR_SERVICE)
- [ ] Global error toast displays translated error messages
- [ ] ErrorBoundary from Story 1.17 catches component errors
- [ ] Retry buttons work for failed API requests
- [ ] Correlation IDs logged for debugging

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-19 | 1.0 | Initial story creation for Event Management Frontend based on wireframes story-1.16-*.md v1.0/v1.1 and Story 2.2 backend APIs | Bob (Scrum Master) |
| 2025-10-19 | 1.1 | Updated to comply with Story 1.16.2 meaningful ID pattern - use eventCode, username, companyName instead of UUIDs in all API calls, TypeScript types, and UI components | Bob (Scrum Master) |
| 2025-10-19 | 1.2 | Updated project structure to align with 05-frontend-architecture.md - components in `components/organizer/EventManagement/`, hooks/services/stores/types at top level, added OpenAPI type generation pattern | Bob (Scrum Master) |
| 2025-10-19 | 1.3 | **Major Update**: Integrated Story 1.17 (React Frontend Foundation) and Story 1.9 (Error Handling Essentials). Added comprehensive i18n support (AC22), navigation integration (AC23), error handling integration (AC24). Added Tasks 22-23 for i18n translation files and navigation config. Added sample translation files (de/en). Story now fully uses foundation from completed stories. | Bob (Scrum Master) |
| 2025-10-19 | 1.4 | **Critical Fix**: Reordered all tasks to respect logical dependencies. Moved i18n (Task 3), Navigation (Task 4), API Client (Task 5), Store (Task 6), and Hooks (Task 7) BEFORE component tasks (Tasks 8-15). Infrastructure must be built before components that depend on it. Added clear section marker "NOW INFRASTRUCTURE IS READY" at Task 8. Total: 23 tasks properly ordered. | Bob (Scrum Master) |
| 2025-10-19 | 1.5 | Added explicit wireframe references to ALL component tasks (Tasks 8-23). Each task now shows which wireframe document to implement (e.g., story-1.16-event-management-dashboard.md, story-1.16-event-detail-edit.md v1.1, etc.) with specific section references. Makes it crystal clear what UI each task implements. | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
_To be populated by Dev Agent during implementation_

### Implementation Approach
_To be documented by Dev Agent_

### Debug Log References
_To be populated by Dev Agent_

### Completion Notes
_To be populated by Dev Agent_

### Files Changed
_To be populated by Dev Agent_

### Deployment Notes
_Special deployment considerations for frontend deployment_

## QA Results
_To be populated by QA Agent after implementation_
