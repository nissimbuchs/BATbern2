# Story 2.7: Partner Coordination Service Foundation

## Status
Approved

## Story

**As a** partner company representative,
**I want** the foundational Partner Coordination Service with CRUD operations through RESTful APIs,
**so that** I can manage partnership relationships, contacts, topic votes, and meeting attendance efficiently.

## Domain Context

### Primary Domain
Partner Coordination Domain - Partnership lifecycle management, topic voting, meeting coordination

### Involved Services
- **Partner Coordination Service** (primary - `services/partner-coordination-service/`)
- **API Gateway** (routing, authentication middleware)
- **Shared Kernel** (exception hierarchy, domain events, query utilities)
- **OpenAPI Code Generation** (generate API interfaces from partners-api.openapi.yml)
- **Company Management Service** (HTTP API calls for company data)
- **User Management Service** (HTTP API calls for user data)

**Shared-Kernel Foundation** (automatically included):
- Exception hierarchy (NotFoundException, ValidationException, BusinessException)
- ErrorResponse with correlationId, path, severity
- DomainEvent & DomainEventPublisher
- Query utilities (FilterParser, SortParser, PaginationUtils, FieldSelector, IncludeParser)

### Cross-Domain Dependencies
- **Depends on:** Story 2.1 (Company Management Service) - Partner stores `companyName` (meaningful ID via HTTP API)
- **Depends on:** Story 2.1b (User Management Service) - PartnerContact stores `username` (meaningful ID via HTTP API)
- **Depends on:** Story 1.15a (API Consolidation Foundation) - Uses FilterParser, SortParser utilities
- **Integration:** EventBridge for domain events (PartnerCreatedEvent, PartnerContactAddedEvent)

**Microservices Architecture (ADR-003):**
- NO direct database access to Company/User tables
- NO JPQL joins across service boundaries
- Store meaningful IDs only: `companyName`, `username`
- HTTP calls to `GET /api/v1/companies/{companyName}` and `GET /api/v1/users/{username}` for full data

## Requirements Context

### Related Functional Requirements
- **FR26**: Partner strategic input through topic voting and suggestions
- **FR28**: Partner meeting coordination and attendance tracking
- **FR4** (partial): Partner access to company-specific data (full analytics deferred to Epic 8)

### Acceptance Criteria Source
Epic 2 Entity CRUD pattern (following Stories 2.1, 2.1b, 2.2, 2.3)

**Database schema from:** docs/architecture/03-data-architecture.md, Section "Partner Coordination Service Database Schema" (lines 1676-1754)

**API design from:** docs/api/partners-api.openapi.yml + docs/architecture/04-api-partner-coordination.md

**Microservices pattern from:** docs/architecture/ADR-003-meaningful-identifiers-public-apis.md

**OpenAPI code generation from:** docs/architecture/ADR-006-openapi-contract-first-code-generation.md

## Architecture Context

### Architecture Patterns

**Data Models** (03-data-architecture.md, lines 1676-1754):
- **Partner aggregate** (company-centric, stores `companyName` String)
- **PartnerContact** (ADR-003/004: stores `username` String, NO User table FK)
- **TopicVote** (partner topic voting with weighted votes)
- **TopicSuggestion** (partner-submitted topic ideas)
- **PartnerMeeting** (spring/autumn meetings)
- **PartnerMeetingAttendance** (RSVP and attendance tracking)

**API Design** (04-api-partner-coordination.md + partners-api.openapi.yml):
- RESTful endpoints: `/api/v1/partners`
- Meaningful IDs: `GET /api/v1/partners/{companyName}` (ADR-003)
- PartnerContact enrichment via HTTP calls to User API (NOT JPQL joins)

**Backend Patterns** (06-backend-architecture.md):
- Spring Boot 3.2 + Java 21
- DDD layered architecture (Controller → Service → Repository)
- JPA entities with PostgreSQL
- Domain events to EventBridge
- OpenAPI code generation (importMappings for shared-kernel types)
- **HTTP clients for inter-service communication (RestTemplate or WebClient)**

**Microservices Data Access (ADR-003):**
- Store meaningful IDs: `companyName`, `username` (NOT UUIDs)
- Call Company API: `GET /api/v1/companies/{companyName}` for full company data
- Call User API: `GET /api/v1/users/{username}` for full user data
- NO cross-service JPQL joins (each service owns its data)

### Database Schema Reference

**Architecture Schema (Source of Truth):**
- Doc: docs/architecture/03-data-architecture.md, Section "Partner Coordination Service Database Schema" (lines 1676-1754)
- Tables: partners, partner_contacts, topic_votes, topic_suggestions, partner_meetings, partner_meeting_attendance

**Migration Check (Dev Agent completes in Task 0):**
- Existing Migration: ❌ No previous migrations existed
- Status: ✅ Created new migrations aligned with architecture
- Action: ✅ Completed
  - V1__Initial_baseline.sql: Copied from shared-kernel (UUID extension, updated_at trigger)
  - V2__create_partner_coordination_schema.sql: Partner domain schema with ADR-003 compliance
    - Uses company_name VARCHAR(12) (meaningful ID, NOT UUID FK)
    - Uses username VARCHAR(100) (meaningful ID, NOT UUID FK)
    - All 6 tables created with proper indexes and constraints
    - NO foreign keys to companies/users tables (microservices boundary)

### Infrastructure Components

**Database:**
- PostgreSQL tables: partners, partner_contacts, topic_votes, topic_suggestions, partner_meetings, partner_meeting_attendance
- Indexes: company_name, username, active status, partnership level, meeting dates
- **NO foreign keys to Company/User tables** (cross-service via HTTP)

**HTTP Clients:**
- Company Service client: `GET /api/v1/companies/{companyName}`
- User Service client: `GET /api/v1/users/{username}`
- RestTemplate with circuit breaker (Resilience4j)

**EventBridge:**
- Domain events: PartnerCreatedEvent, PartnerUpdatedEvent, PartnerContactAddedEvent, TopicVoteSubmittedEvent

**API Gateway:**
- Routes: `/api/v1/partners/*`
- Auth: JWT validation via Cognito
- CORS: Configured for frontend access

**Cache:**
- Caffeine in-memory cache for partner lookups (10min TTL)
- Cache Company/User API responses (5min TTL to reduce HTTP calls)
- No Redis/ElastiCache (cost-optimized per ADR-001)

## Wireframe Context

### Wireframe References

**No wireframes for this story** - Backend CRUD foundation only

Frontend wireframes exist for Epic 8 (Partner Portal):
- `story-6.1-partner-analytics-dashboard.md` (deferred to Epic 8)
- `story-6.2-partner-meetings.md` (deferred to Epic 8)
- `story-6.3-partner-directory.md` (deferred to Epic 8)
- `story-6.4-topic-voting.md` (deferred to Epic 8)

**Scope Note:** This story provides backend APIs only. Frontend consumption deferred to Epic 8 Partner Portal stories.

### UI Components

**Not applicable** - Backend service only (no React components in this story)

## Acceptance Criteria

Following Epic 2 CRUD pattern (Stories 2.1-2.3) with microservices architecture (ADR-003):

1. **Partner aggregate with DDD patterns**
   - Partner entity with JPA annotations
   - Partnership tiers: BRONZE, SILVER, GOLD, PLATINUM, STRATEGIC
   - Active status tracking (start/end dates)
   - Stores `companyName` (String) - meaningful ID per ADR-003
   - **NO UUID foreign key to companies table**

2. **REST API implementing basic CRUD**
   - `GET /api/v1/partners` - List all partners with filtering
   - `GET /api/v1/partners/{companyName}` - Get partner by company name (ADR-003)
   - `POST /api/v1/partners` - Create new partnership (organizer-only)
   - `PATCH /api/v1/partners/{companyName}` - Update partnership details
   - `DELETE /api/v1/partners/{companyName}` - Deactivate partnership (soft delete)

3. **Partner Contact Management** (ADR-003/004 pattern)
   - `GET /api/v1/partners/{companyName}/contacts` - List contacts with User data from HTTP API
   - `POST /api/v1/partners/{companyName}/contacts` - Add contact (stores username)
   - `DELETE /api/v1/partners/{companyName}/contacts/{username}` - Remove contact
   - Contact roles: PRIMARY, BILLING, TECHNICAL, MARKETING
   - Business rule: At least one PRIMARY contact required
   - **PartnerContact stores `username` (String), NOT `userId` (UUID)**
   - **User data fetched via HTTP call to User Service API**

4. **Topic Voting**
   - `GET /api/v1/partners/{companyName}/votes` - List partner's topic votes
   - `POST /api/v1/partners/{companyName}/votes` - Cast vote on topic (1-5 scale)
   - Vote weight based on partnership tier (BRONZE=1, STRATEGIC=5)
   - One vote per topic per partner (constraint)

5. **Topic Suggestions**
   - `GET /api/v1/partners/{companyName}/suggestions` - List suggestions
   - `POST /api/v1/partners/{companyName}/suggestions` - Submit new topic suggestion
   - Suggestion status: SUBMITTED, UNDER_REVIEW, ACCEPTED, REJECTED, IMPLEMENTED

6. **OpenAPI documentation**
   - OpenAPI 3.1 spec at `docs/api/partners-api.openapi.yml`
   - ErrorResponse and PaginationMetadata imported from shared-kernel (NOT defined locally)
   - All endpoints documented with request/response schemas

7. **Database schema with Flyway migrations**
   - PostgreSQL schema uses meaningful IDs (companyName, username)
   - Flyway migration in `shared-kernel/src/main/resources/db/migration/`
   - All constraints, indexes (NO foreign keys to Company/User tables)

8. **Domain events publishing**
   - PartnerCreatedEvent when partnership created
   - PartnerUpdatedEvent when partnership modified
   - PartnerContactAddedEvent when contact added
   - TopicVoteSubmittedEvent when vote cast
   - All events include full metadata (correlationId, causationId, userId)

9. **Shared-Kernel integration** (mandatory)
   - Exceptions extend shared-kernel hierarchy (PartnerNotFoundException extends NotFoundException)
   - GlobalExceptionHandler uses `ch.batbern.shared.dto.ErrorResponse`
   - Domain events extend `DomainEvent<UUID>`
   - Inject DomainEventPublisher from shared-kernel

10. **OpenAPI Code Generation - Hybrid Contract-First** (mandatory)
    - OpenAPI spec is source of truth: `docs/api/partners-api.openapi.yml`
    - Generated API interfaces in `build/generated/src/main/java/ch/batbern/partners/api/generated/`
      - `PartnersApi.java` - Partner CRUD operations
      - `PartnerContactsApi.java` - Contact management
      - `TopicVotingApi.java` - Topic voting and suggestions
    - Generated DTOs in `build/generated/src/main/java/ch/batbern/partners/dto/generated/`
      - `PartnerResponse.java`, `UpdatePartnerRequest.java`
      - `PartnerContactResponse.java`, `AddPartnerContactRequest.java`
      - `TopicVote.java`, `CastVoteRequest.java`
      - `TopicSuggestion.java`, `SubmitSuggestionRequest.java`
    - Shared-kernel types imported (NOT generated):
      - `ch.batbern.shared.dto.ErrorResponse`
      - `ch.batbern.shared.api.PaginationMetadata`
      - `ch.batbern.shared.dto.PaginatedResponse<T>`
    - Controllers implement generated interfaces manually
    - Build fails if implementation doesn't match spec
    - Code regenerated on every build (in `build/generated/`, gitignored)

11. **HTTP Client Integration** (ADR-003 microservices pattern)
    - CompanyServiceClient for `GET /api/v1/companies/{companyName}`
    - UserServiceClient for `GET /api/v1/users/{username}`
    - Circuit breaker with Resilience4j (fallback to cached data)
    - HTTP client responses cached (5min TTL)
    - Handle 404 errors gracefully (company/user not found)

12. **Integration tests**
    - All tests extend AbstractIntegrationTest (PostgreSQL via Testcontainers)
    - Test partner CRUD operations
    - Test contact management with mocked User Service HTTP responses
    - Test topic voting with weight calculations
    - Test business rules (minimum primary contact, vote uniqueness)
    - Mock HTTP clients for Company/User Service calls in tests
    - Coverage >80%

13. **Performance**
    - Partner list: <100ms P95
    - Partner detail: <150ms P95
    - Partner with contacts (HTTP enrichment): <300ms P95 (allows for HTTP call overhead)

## Test Specifications (TDD)

⚠️ **CRITICAL:** All tests must be written BEFORE implementation following Red-Green-Refactor cycle.

### Test Scenarios by Acceptance Criteria

**AC1 Tests: Partner Aggregate with DDD Patterns**
- Test 1.1: `should_createPartnerEntity_when_validDataProvided`
- Test 1.2: `should_storeCompanyNameAsString_when_creatingPartner` (ADR-003)
- Test 1.3: `should_calculateIsActive_when_comparingDates`
- Test 1.4: `should_throwValidationException_when_endDateBeforeStartDate`
- Test 1.5: `should_setActiveTrue_when_currentDateBetweenStartAndEnd`

**AC2 Tests: REST API Basic CRUD**
- Test 2.1: `should_returnPartnerList_when_listEndpointCalled`
- Test 2.2: `should_filterByPartnershipLevel_when_levelParameterProvided`
- Test 2.3: `should_filterByActiveStatus_when_isActiveParameterProvided`
- Test 2.4: `should_returnPartnerByCompanyName_when_validCompanyNameProvided` (ADR-003)
- Test 2.5: `should_return404_when_companyNameNotFound`
- Test 2.6: `should_createPartner_when_validRequestProvided`
- Test 2.7: `should_return400_when_partnerAlreadyExistsForCompany`
- Test 2.8: `should_return403_when_nonOrganizerTriesToCreate`
- Test 2.9: `should_updatePartnershipLevel_when_patchRequestValid`
- Test 2.10: `should_softDeletePartner_when_deleteEndpointCalled`

**AC3 Tests: Partner Contact Management (ADR-003/004)**
- Test 3.1: `should_returnContactsWithUserData_when_httpCallSucceeds` (mock User Service)
- Test 3.2: `should_enrichContactWithUserFields_when_userServiceReturnsData` (email, firstName, lastName, profilePictureUrl)
- Test 3.3: `should_addContactByUsername_when_validUsernameProvided` (ADR-003)
- Test 3.4: `should_return404_when_userServiceReturns404`
- Test 3.5: `should_setContactRole_when_addingContact` (PRIMARY, BILLING, TECHNICAL, MARKETING)
- Test 3.6: `should_removeContact_when_deleteEndpointCalled`
- Test 3.7: `should_return400_when_removingLastPrimaryContact`
- Test 3.8: `should_return409_when_addingDuplicateContact`
- Test 3.9: `should_storeUsernameAsString_when_addingContact` (NOT UUID)

**AC4 Tests: Topic Voting**
- Test 4.1: `should_returnPartnerVotes_when_listVotesCalled`
- Test 4.2: `should_castVoteOnTopic_when_validVoteValueProvided` (1-5 scale)
- Test 4.3: `should_applyVoteWeight_when_calculatingVoteValue` (BRONZE=1, GOLD=3, STRATEGIC=5)
- Test 4.4: `should_return400_when_voteValueOutOfRange` (not 1-5)
- Test 4.5: `should_return409_when_partnerAlreadyVotedOnTopic` (uniqueness constraint)
- Test 4.6: `should_updateVoteWeight_when_partnershipLevelChanges`

**AC5 Tests: Topic Suggestions**
- Test 5.1: `should_listPartnerSuggestions_when_getEndpointCalled`
- Test 5.2: `should_submitTopicSuggestion_when_validDataProvided`
- Test 5.3: `should_setSuggestionStatusToSubmitted_when_initiallyCreated`
- Test 5.4: `should_return400_when_suggestionTitleExceedsMaxLength` (500 chars)
- Test 5.5: `should_return400_when_descriptionExceedsMaxLength` (2000 chars)

**AC6 Tests: OpenAPI Documentation**
- Test 6.1: `should_generateValidOpenAPISpec_when_buildCompletes`
- Test 6.2: `should_importErrorResponseFromSharedKernel_when_validatingSpec`
- Test 6.3: `should_importPaginationMetadataFromSharedKernel_when_validatingSpec`
- Test 6.4: `should_matchControllerToGeneratedInterface_when_buildCompletes`

**AC7 Tests: Database Schema with Flyway**
- Test 7.1: `should_createPartnerTable_when_migrationExecuted`
- Test 7.2: `should_createPartnerContactsTable_when_migrationExecuted`
- Test 7.3: `should_createTopicVotesTable_when_migrationExecuted`
- Test 7.4: `should_createTopicSuggestionsTable_when_migrationExecuted`
- Test 7.5: `should_createAllIndexes_when_migrationExecuted`
- Test 7.6: `should_NOT_createForeignKeys_toCompanyUserTables` (microservices pattern)
- Test 7.7: `should_enforceUniqueConstraint_when_duplicateCompanyNameInserted`

**AC8 Tests: Domain Events Publishing**
- Test 8.1: `should_publishPartnerCreatedEvent_when_partnerCreated`
- Test 8.2: `should_publishPartnerUpdatedEvent_when_partnerModified`
- Test 8.3: `should_publishPartnerContactAddedEvent_when_contactAdded`
- Test 8.4: `should_publishTopicVoteSubmittedEvent_when_voteCast`
- Test 8.5: `should_includeCorrelationId_when_publishingEvent`
- Test 8.6: `should_includeCausationId_when_publishingEvent`
- Test 8.7: `should_includeUserId_when_publishingEvent`

**AC9 Tests: Shared-Kernel Integration**
- Test 9.1: `should_throwPartnerNotFoundException_when_partnerNotFound` (extends NotFoundException)
- Test 9.2: `should_returnErrorResponse_when_exceptionThrown` (ErrorResponse structure)
- Test 9.3: `should_includeCorrelationIdInError_when_exceptionOccurs`
- Test 9.4: `should_useDomainEventPublisher_when_publishingEvents` (from shared-kernel)

**AC10 Tests: OpenAPI Code Generation**
- Test 10.1: `should_implementGeneratedInterface_when_controllerCreated` (PartnersApi interface)
- Test 10.2: `should_useGeneratedDTOs_when_handlingRequests` (CreatePartnerRequest, PartnerResponse)
- Test 10.3: `should_failBuild_when_implementationDoesNotMatchSpec`

**AC11 Tests: HTTP Client Integration (ADR-003 microservices)**
- Test 11.1: `should_callCompanyServiceAPI_when_validatingCompanyName`
- Test 11.2: `should_callUserServiceAPI_when_enrichingContacts`
- Test 11.3: `should_handleCompanyNotFound_when_userServiceReturns404`
- Test 11.4: `should_propagateJwtToken_when_callingServices`
- Test 11.5: `should_cacheAPIResponses_when_httpCallSucceeds` (15min TTL)
- Test 11.6: `should_handleNetworkErrors_when_serviceUnavailable` (ResourceAccessException)

**AC12 Tests: Integration Tests**
- Test 12.1: `should_performFullPartnerCRUD_when_integrationTestExecuted`
- Test 12.2: `should_extendSharedKernelAbstractIntegrationTest_when_runningTests` (testFixtures)
- Test 12.3: `should_mockUserServiceHTTP_when_testingContactEnrichment` (@MockBean, not WireMock)
- Test 12.4: `should_enforceMinimumPrimaryContact_when_deletingContact`
- Test 12.5: `should_enforceVoteUniqueness_when_castingDuplicateVote`

**AC13 Tests: Performance**
- Test 13.1: `should_completePartnerList_within100ms_whenP95Measured`
- Test 13.2: `should_completePartnerDetail_within150ms_whenP95Measured`
- Test 13.3: `should_completePartnerWithContactsHTTP_within300ms_whenP95Measured`

### Test File Locations

**Backend Tests:**

**Unit Tests:**
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/domain/PartnerTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/domain/PartnerContactTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/domain/TopicVoteTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/service/PartnerServiceTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/service/PartnerContactServiceTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/service/TopicVotingServiceTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/client/CompanyServiceClientTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/client/UserServiceClientTest.java`

**Integration Tests:**
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/controller/PartnerControllerIntegrationTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/controller/PartnerContactControllerIntegrationTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/controller/TopicVotingControllerIntegrationTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/repository/PartnerRepositoryTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/repository/PartnerContactRepositoryTest.java`
- `services/partner-coordination-service/src/test/java/ch/batbern/partners/validation/SchemaValidationTest.java`

**E2E Tests (Bruno API Contract Tests):**
- `bruno-tests/partners-api/01-list-partners.bru`
- `bruno-tests/partners-api/02-get-partner-by-company.bru`
- `bruno-tests/partners-api/03-update-partner.bru`
- `bruno-tests/partners-api/04-list-partner-contacts.bru`
- `bruno-tests/partners-api/05-add-partner-contact.bru`
- `bruno-tests/partners-api/06-delete-partner-contact.bru`
- `bruno-tests/partners-api/07-list-partner-votes.bru`
- `bruno-tests/partners-api/08-cast-topic-vote.bru`
- `bruno-tests/partners-api/09-list-partner-suggestions.bru`
- `bruno-tests/partners-api/10-submit-topic-suggestion.bru`
- `bruno-tests/partners-api/bruno.json` (collection configuration)
- `bruno-tests/partners-api/environments/staging.bru`
- `bruno-tests/partners-api/environments/local.bru`

### Test Data & Mocks

**Test Data Builders:**
- `PartnerTestDataBuilder` - Create test Partner entities with various partnership levels
- `PartnerContactTestDataBuilder` - Create test contacts with different roles
- `TopicVoteTestDataBuilder` - Create test votes with weighted values
- `CompanyResponseBuilder` - Mock Company Service HTTP responses
- `UserResponseBuilder` - Mock User Service HTTP responses

**Mock HTTP Services:**
- Mock Company Service HTTP responses: `GET /api/v1/companies/{companyName}`
- Mock User Service HTTP responses: `GET /api/v1/users/{username}`
- Spring @MockBean with Mockito for HTTP client mocking (NOT WireMock)
- TestConfiguration classes: TestCompanyServiceClientConfig, TestUserServiceClientConfig
- Mock EventBridge for domain event publishing tests
- Mock AWS Cognito for authentication tests (JWT tokens)

**Test Containers (MANDATORY):**
- **PostgreSQL 16 Alpine** via shared-kernel testFixtures
  - All integration tests MUST extend `ch.batbern.shared.test.AbstractIntegrationTest`
  - Container provided by testFixtures (singleton pattern, automatic cleanup via Ryuk)
  - Enable Flyway migrations for production parity
  - Database dialect: `org.hibernate.dialect.PostgreSQLDialect`
  - **NO need to declare PostgreSQLContainer in service tests**

**Test Configuration:**
- `application-test.properties`:
  ```properties
  # PostgreSQL via Testcontainers (configured dynamically)
  spring.datasource.driver-class-name=org.postgresql.Driver
  spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

  # Let Flyway manage schema - validate only
  spring.jpa.hibernate.ddl-auto=validate

  # Enable Flyway migrations for production parity
  spring.flyway.enabled=true
  spring.flyway.locations=classpath:db/migration
  spring.flyway.baseline-on-migrate=true

  # HTTP service URLs (mocked via TestConfiguration @MockBean)
  services.company-service.url=http://localhost:8081
  services.user-service.url=http://localhost:8082
  ```

**Cross-Service Test Data:**
- Mock Company response: `companyName=GoogleZH`, `displayName=Google Zurich`
- Mock User response: `username=john.doe`, `email=john.doe@example.com`, `firstName=John`, `lastName=Doe`
- HTTP clients mocked via TestConfiguration classes (NOT WireMock)

## Tasks / Subtasks (TDD Workflow)

**⚠️ CRITICAL:** Tests must be written BEFORE implementation code (Red-Green-Refactor).

- [x] **Task 0: Schema Validation & Migration (FIRST)**
  - [x] Read architecture doc schema (docs/architecture/03-data-architecture.md, lines 1676-1754)
  - [x] Create migration directory: `services/partner-coordination-service/src/main/resources/db/migration/`
  - [x] Copy baseline migration: `V1__Initial_baseline.sql` from `shared-kernel/src/main/resources/db/migration/V1__Initial_baseline.sql`
    - **Why copy?** Each service manages its own migrations independently (per Flyway best practices)
    - V1 provides: UUID extension (`uuid-ossp`), `updated_at` trigger function, common indexes
    - This ensures service can run migrations standalone without shared-kernel dependency at runtime
  - [x] **CRITICAL: Adapt schema to use meaningful IDs (companyName, username) instead of UUIDs**
  - [x] Create migration `V2__create_partner_coordination_schema.sql` in `services/partner-coordination-service/src/main/resources/db/migration/` with:
    - [x] partners table (id, **company_name VARCHAR UNIQUE**, partnership_level, start_date, end_date, is_active)
    - [x] partner_contacts table (id, partner_id, **username VARCHAR**, contact_role, is_primary, UNIQUE(partner_id, username))
    - [x] topic_votes table (id, topic_id, partner_id, vote_weight, vote_value, voted_at, UNIQUE(topic_id, partner_id))
    - [x] topic_suggestions table (id, partner_id, suggested_topic, description, business_justification, status, suggested_at, reviewed_at, reviewed_by)
    - [x] partner_meetings table (id, meeting_type, scheduled_date, location, agenda, materials_generated)
    - [x] partner_meeting_attendance table (id, meeting_id, partner_id, rsvp_status, rsvp_at, attended, UNIQUE(meeting_id, partner_id))
    - [x] All indexes from architecture (company_name, username, active, partnership_level, meeting dates)
    - [x] **NO foreign key constraints to companies/users tables** (microservices boundary)
  - [x] Update Database Schema Reference section with migration version and status
  - [x] Test migration with PostgreSQL container (NOT H2) - Will be tested in Task 2a integration tests
  - [x] Verify Flyway runs migration on service startup (spring.flyway.enabled=true) - Configured in application.yml
  - [x] Commit migrations BEFORE creating JPA entities - Committed in 5e23067

**Important**: Each microservice runs its own Flyway migrations on startup (per deploy-production.yml).
Migrations are in `services/partner-coordination-service/src/main/resources/db/migration/`, NOT in shared-kernel.

- [x] **Task 1: Project Setup & OpenAPI Code Generation (AC: 6, 9, 10)**
  - [ ] Create partner-coordination-service module in services/ directory
  - [ ] Configure build.gradle with:
    - [ ] Spring Boot 3.2 + Java 21
    - [ ] shared-kernel dependency
    - [ ] **testFixtures(project(':shared-kernel'))** - provides AbstractIntegrationTest
    - [ ] PostgreSQL driver + Testcontainers
    - [ ] **OpenAPI Generator plugin** (hybrid contract-first approach):
      ```gradle
      plugins {
          id 'org.openapi.generator' version '7.2.0'
      }

      openApiGenerate {
          generatorName = "spring"
          inputSpec = "$rootDir/docs/api/partners-api.openapi.yml".toString()
          outputDir = "$buildDir/generated".toString()
          apiPackage = "ch.batbern.partners.api.generated"
          modelPackage = "ch.batbern.partners.dto.generated"

          // Import shared-kernel types (NOT generated)
          importMappings = [
              'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
              'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
              'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
          ]

          typeMappings = [
              'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
              'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
          ]

          schemaMappings = [
              'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
              'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
          ]

          configOptions = [
              interfaceOnly: "true",              // Only generate interfaces
              useTags: "true",                    // Organize by OpenAPI tags
              useSpringBoot3: "true",             // Spring Boot 3 annotations
              useBeanValidation: "true",          // @Valid, @NotNull
              useJakartaEe: "true",               // Jakarta EE (Spring Boot 3)
              dateLibrary: "java8",               // java.time.* types
              skipDefaultInterface: "true"        // No default methods
          ]
      }

      // Add generated sources to compile
      sourceSets.main.java.srcDirs += "$buildDir/generated/src/main/java"

      // Generate before compile
      compileJava.dependsOn tasks.openApiGenerate
      ```
    - [ ] RestTemplate for HTTP clients
    - [ ] Caffeine cache dependency
  - [ ] Create package structure: ch.batbern.partners.{controller, service, repository, domain, exception, client, config}
  - [ ] Run openApiGenerate: `./gradlew :services:partner-coordination-service:openApiGenerate`
  - [ ] Verify generated interfaces in `build/generated/src/main/java/ch/batbern/partners/api/generated/`
  - [ ] Verify generated DTOs in `build/generated/src/main/java/ch/batbern/partners/dto/generated/`
  - [ ] Create OPENAPI-CODEGEN.md documentation (implementation guide for ADR-006)
  - [ ] Configure application.yml (database, Flyway, HTTP client URLs)
  - [ ] Create CacheConfig class with named caches (partners, companies, users)

- [x] **Task 2a: Partner Domain Model - RED Phase (AC: 1, 7)**
  - [ ] Write SchemaValidationTest (validates JPA entities match Flyway migration)
  - [ ] Write PartnerTest unit tests:
    - [ ] should_createPartnerEntity_when_validDataProvided
    - [ ] should_storeCompanyNameAsString_when_creatingPartner (ADR-003)
    - [ ] should_calculateIsActive_when_comparingDates
    - [ ] should_throwValidationException_when_endDateBeforeStartDate
  - [ ] Write PartnerRepositoryTest integration tests:
    - [ ] should_savePartner_when_validEntityProvided
    - [ ] should_findByCompanyName_when_partnerExists
    - [ ] should_enforceUniqueConstraint_when_duplicateCompanyNameInserted
  - [ ] Verify all tests FAIL (RED)

- [x] **Task 2b: Partner Domain Model - GREEN Phase (AC: 1, 7)**
  - [ ] Create Partner JPA entity (@Entity, @Table("partners"))
    - [ ] id UUID primary key
    - [ ] **companyName String (NOT companyId UUID)** - ADR-003
    - [ ] partnershipLevel enum (BRONZE, SILVER, GOLD, PLATINUM, STRATEGIC)
    - [ ] partnershipStartDate, partnershipEndDate LocalDate
    - [ ] isActive boolean with @Transient calculation
    - [ ] timestamps (createdAt, updatedAt)
  - [ ] Create PartnerRepository extends JpaRepository with findByCompanyName(String)
  - [ ] Create PartnershipTier enum
  - [ ] Verify all Task 2a tests PASS (GREEN)

- [ ] **Task 3a: HTTP Client Layer - RED Phase (AC: 11)**
  - [ ] Write CompanyServiceClientTest:
    - [ ] should_callCompanyServiceAPI_when_validatingCompanyName
    - [ ] should_return404_when_companyNotFound
    - [ ] should_cacheResponse_when_httpCallSucceeds
    - [ ] should_propagateJwtToken_when_callingCompanyService
    - [ ] should_handle5xxErrors_when_serviceUnavailable
    - [ ] should_handleNetworkErrors_when_connectionFails (ResourceAccessException)
  - [ ] Write UserServiceClientTest:
    - [ ] should_callUserServiceAPI_when_enrichingContacts
    - [ ] should_return404_when_userNotFound
    - [ ] should_cacheResponse_when_httpCallSucceeds
    - [ ] should_propagateJwtToken_when_callingUserService
  - [ ] Verify all tests FAIL (RED)

- [ ] **Task 3b: HTTP Client Layer - GREEN Phase (AC: 11)**
  - [ ] Create CompanyServiceClient with RestTemplate
    - [ ] getCompany(String companyName) → CompanyResponse
    - [ ] JWT token propagation via createHeadersWithJwtToken()
    - [ ] Cache responses (15min TTL - @Cacheable)
    - [ ] Error handling: HttpClientErrorException.NotFound, HttpServerErrorException, ResourceAccessException
  - [ ] Create UserServiceClient with RestTemplate
    - [ ] getUser(String username) → UserResponse
    - [ ] JWT token propagation via createHeadersWithJwtToken()
    - [ ] Cache responses (15min TTL - @Cacheable)
    - [ ] Error handling: HttpClientErrorException.NotFound, HttpServerErrorException, ResourceAccessException
  - [ ] Create CompanyResponse DTO (companyName, displayName, logoUrl, etc.)
  - [ ] Create UserResponse DTO (username, email, firstName, lastName, profilePictureUrl)
  - [ ] Create custom exceptions: CompanyNotFoundException, UserNotFoundException, CompanyServiceException, UserServiceException
  - [ ] Verify all Task 3a tests PASS (GREEN)

- [ ] **Task 4a: Partner CRUD API - RED Phase (AC: 2, 6, 8, 9, 10)**
  - [ ] Update partners-api.openapi.yml with ErrorResponse/PaginationMetadata imports
  - [ ] Run openApiGenerate gradle task to generate PartnersApi interface
  - [ ] Write PartnerControllerIntegrationTest (extends AbstractIntegrationTest):
    - [ ] should_returnPartnerList_when_listEndpointCalled
    - [ ] should_filterByPartnershipLevel_when_levelParameterProvided
    - [ ] should_returnPartnerByCompanyName_when_validCompanyNameProvided (ADR-003)
    - [ ] should_return404_when_companyNameNotFound
    - [ ] should_createPartner_when_validRequestProvided
    - [ ] should_return400_when_partnerAlreadyExistsForCompany
    - [ ] should_return403_when_nonOrganizerTriesToCreate
    - [ ] should_updatePartnershipLevel_when_patchRequestValid
    - [ ] should_softDeletePartner_when_deleteEndpointCalled
  - [ ] Write PartnerServiceTest unit tests for business logic
  - [ ] Write GlobalExceptionHandlerTest (verify ErrorResponse from shared-kernel)
  - [ ] Verify all tests FAIL (RED)

- [ ] **Task 4b: Partner CRUD API - GREEN Phase (AC: 2, 6, 8, 9, 10)**
  - [ ] Create PartnerController implements PartnersApi (generated interface)
  - [ ] Create PartnerService with business logic:
    - [ ] listPartners(filter, sort, page) using FilterParser/SortParser from shared-kernel
    - [ ] getPartnerByCompanyName(companyName) - ADR-003 meaningful ID
    - [ ] createPartner(request) with HTTP validation via CompanyServiceClient
    - [ ] updatePartner(companyName, request)
    - [ ] deletePartner(companyName) - soft delete (set isActive=false)
  - [ ] Create custom exceptions extending shared-kernel:
    - [ ] PartnerNotFoundException extends NotFoundException
    - [ ] PartnerAlreadyExistsException extends BusinessException
  - [ ] Create GlobalExceptionHandler using ErrorResponse from shared-kernel
  - [ ] Inject DomainEventPublisher from shared-kernel
  - [ ] Publish PartnerCreatedEvent, PartnerUpdatedEvent with full metadata (correlationId, causationId, userId)
  - [ ] Verify all Task 4a tests PASS (GREEN)

- [ ] **Task 5a: Partner Contact Management - RED Phase (AC: 3, 8)**
  - [ ] Write PartnerContactTest unit tests:
    - [ ] should_createPartnerContact_when_validDataProvided
    - [ ] should_storeUsernameAsString_when_addingContact (ADR-003)
    - [ ] should_setContactRole_when_creating
    - [ ] should_enforcePrimaryContactRule_when_removingLastPrimary
  - [ ] Write PartnerContactControllerIntegrationTest with @MockBean:
    - [ ] should_returnContactsWithUserData_when_httpCallSucceeds (mock User Service)
    - [ ] should_enrichContactWithUserFields_when_userServiceReturnsData
    - [ ] should_addContactByUsername_when_validUsernameProvided (ADR-003)
    - [ ] should_return404_when_userServiceReturns404
    - [ ] should_removeContact_when_deleteEndpointCalled
    - [ ] should_return400_when_removingLastPrimaryContact
    - [ ] should_return409_when_addingDuplicateContact
  - [ ] Verify all tests FAIL (RED)

- [ ] **Task 5b: Partner Contact Management - GREEN Phase (AC: 3, 8)**
  - [ ] Create PartnerContact JPA entity:
    - [ ] id UUID primary key
    - [ ] partnerId UUID (FK to partners)
    - [ ] **username String (NOT userId UUID)** - ADR-003
    - [ ] contactRole enum (PRIMARY, BILLING, TECHNICAL, MARKETING)
    - [ ] isPrimary boolean
    - [ ] UNIQUE constraint (partnerId, username)
  - [ ] Create PartnerContactRepository
  - [ ] Create PartnerContactService with business logic:
    - [ ] getPartnerContacts(companyName) - returns contacts, enriches with UserServiceClient.getUser(username)
    - [ ] addPartnerContact(companyName, username, role, isPrimary) - validates via UserServiceClient
    - [ ] removePartnerContact(companyName, username) with primary contact validation
  - [ ] Create ContactRole enum
  - [ ] Update PartnerController with contact endpoints
  - [ ] Publish PartnerContactAddedEvent, PartnerContactRemovedEvent
  - [ ] Verify all Task 5a tests PASS (GREEN)

- [ ] **Task 6a: Topic Voting - RED Phase (AC: 4, 8)**
  - [ ] Write TopicVoteTest unit tests:
    - [ ] should_createTopicVote_when_validDataProvided
    - [ ] should_applyVoteWeight_when_calculatingVoteValue
    - [ ] should_return400_when_voteValueOutOfRange
  - [ ] Write TopicVotingControllerIntegrationTest:
    - [ ] should_returnPartnerVotes_when_listVotesCalled
    - [ ] should_castVoteOnTopic_when_validVoteValueProvided
    - [ ] should_applyVoteWeight_when_partnershipLevelChanges (BRONZE=1, STRATEGIC=5)
    - [ ] should_return409_when_partnerAlreadyVotedOnTopic
  - [ ] Verify all tests FAIL (RED)

- [ ] **Task 6b: Topic Voting - GREEN Phase (AC: 4, 8)**
  - [ ] Create TopicVote JPA entity:
    - [ ] id UUID primary key
    - [ ] topicId UUID (FK to topics in Event Service)
    - [ ] partnerId UUID (FK to partners)
    - [ ] voteWeight integer (calculated from partnership tier)
    - [ ] voteValue integer (1-5)
    - [ ] votedAt timestamp
    - [ ] UNIQUE constraint (topicId, partnerId)
  - [ ] Create TopicVoteRepository
  - [ ] Create TopicVotingService with business logic:
    - [ ] getPartnerVotes(companyName)
    - [ ] castVote(companyName, topicId, voteValue) with weight calculation
    - [ ] calculateVoteWeight(partnershipLevel) - BRONZE=1, SILVER=2, GOLD=3, PLATINUM=4, STRATEGIC=5
  - [ ] Update PartnerController with voting endpoints
  - [ ] Publish TopicVoteSubmittedEvent
  - [ ] Verify all Task 6a tests PASS (GREEN)

- [ ] **Task 7a: Topic Suggestions - RED Phase (AC: 5)**
  - [ ] Write TopicSuggestionTest unit tests
  - [ ] Write TopicSuggestionControllerIntegrationTest:
    - [ ] should_listPartnerSuggestions_when_getEndpointCalled
    - [ ] should_submitTopicSuggestion_when_validDataProvided
    - [ ] should_setSuggestionStatusToSubmitted_when_initiallyCreated
    - [ ] should_return400_when_suggestionTitleExceedsMaxLength
  - [ ] Verify all tests FAIL (RED)

- [ ] **Task 7b: Topic Suggestions - GREEN Phase (AC: 5)**
  - [ ] Create TopicSuggestion JPA entity:
    - [ ] id UUID primary key
    - [ ] partnerId UUID (FK to partners)
    - [ ] suggestedTopic string (max 500 chars)
    - [ ] description string (max 2000 chars)
    - [ ] businessJustification string (max 1000 chars)
    - [ ] status enum (SUBMITTED, UNDER_REVIEW, ACCEPTED, REJECTED, IMPLEMENTED)
    - [ ] suggestedAt, reviewedAt timestamps
    - [ ] reviewedBy UUID (organizer who reviewed)
  - [ ] Create TopicSuggestionRepository
  - [ ] Create TopicSuggestionService
  - [ ] Update PartnerController with suggestion endpoints
  - [ ] Verify all Task 7a tests PASS (GREEN)

- [ ] **Task 8: Performance Testing & Optimization (AC: 13)**
  - [ ] Add @Timed annotations to service methods
  - [ ] Write PartnerPerformanceTest:
    - [ ] should_completePartnerList_within100ms_whenP95Measured
    - [ ] should_completePartnerDetail_within150ms_whenP95Measured
    - [ ] should_completePartnerWithContactsHTTP_within300ms_whenP95Measured (allows HTTP overhead)
  - [ ] Configure Caffeine cache for partner lookups (10min TTL)
  - [ ] Configure Caffeine cache for HTTP responses (5min TTL)
  - [ ] Optimize database queries with proper indexes
  - [ ] Verify performance targets met

- [ ] **Task 9: Infrastructure Setup (AC: 6)**
  - [ ] Create partner-coordination-service CDK stack in infrastructure/lib/stacks/
  - [ ] Configure API Gateway routes: /api/v1/partners/*
  - [ ] Configure EventBridge rules for domain events
  - [ ] Configure IAM permissions (least privilege)
  - [ ] Configure service discovery for HTTP clients (or hardcode URLs)
  - [ ] Update CloudWatch dashboards with partner service metrics
  - [ ] Deploy to staging environment
  - [ ] Verify all endpoints accessible via API Gateway

- [ ] **Task 10: Backend Refactoring (REFACTOR)**
  - [ ] Verify no mocks used except in tests
  - [ ] Verify all integration tests use PostgreSQL (not H2)
  - [ ] Verify HTTP clients properly mocked with @MockBean (NOT WireMock)
  - [ ] Verify all tests PASS (GREEN)
  - [ ] Refactor service layer (extract methods, improve naming)
  - [ ] Refactor repository queries (optimize performance)
  - [ ] Remove code duplication
  - [ ] Improve error messages
  - [ ] Add JavaDoc comments to public APIs
  - [ ] Run Checkstyle/SpotBugs and fix issues
  - [ ] Verify all tests still PASS after refactoring

- [ ] **Task 11: Documentation & OpenAPI (AC: 6)**
  - [ ] Update partners-api.openapi.yml with all implemented endpoints
  - [ ] Verify ErrorResponse/PaginationMetadata imported from shared-kernel (NOT defined locally)
  - [ ] Generate OpenAPI documentation (Swagger UI)
  - [ ] Update README.md with API usage examples
  - [ ] Document ADR-003 (meaningful IDs) compliance
  - [ ] Document microservices HTTP client pattern
  - [ ] Add code examples for HTTP enrichment (PartnerContact + User Service)

- [ ] **Task 12: Bruno E2E Tests - Partner API Contract Tests (AC: 2, 3, 4, 5)**
  - [ ] Create Bruno collection: `bruno-tests/partners-api/`
  - [ ] Configure environment variables (API_URL, AUTH_TOKEN)
  - [ ] **Partner CRUD Tests:**
    - [ ] `GET /partners` - List all partners
      - [ ] Test with no filters (returns all partners)
      - [ ] Test with `partnershipLevel=gold` filter
      - [ ] Test with `isActive=true` filter
      - [ ] Test with `include=company,contacts` parameter
      - [ ] Verify response schema matches OpenAPI spec
    - [ ] `GET /partners/{companyName}` - Get partner by company name
      - [ ] Test with valid companyName (e.g., "GoogleZH")
      - [ ] Test with `include=company,contacts,votes` parameter
      - [ ] Verify 404 when partner not found
      - [ ] Verify response includes partnership level, dates, benefits
    - [ ] `PATCH /partners/{companyName}` - Update partnership
      - [ ] Test updating partnership level (GOLD → PLATINUM)
      - [ ] Test updating partnership end date
      - [ ] Test updating isActive status
      - [ ] Verify 404 when partner not found
      - [ ] Verify 400 when invalid data provided
  - [ ] **Partner Contact Management Tests:**
    - [ ] `GET /partners/{companyName}/contacts` - List partner contacts
      - [ ] Test returns empty array when no contacts
      - [ ] Test returns contacts with enriched User data (email, firstName, lastName, profilePictureUrl)
      - [ ] Verify 404 when partner not found
    - [ ] `POST /partners/{companyName}/contacts` - Add partner contact
      - [ ] Test adding contact with username, contactRole=primary, isPrimary=true
      - [ ] Test adding contact with contactRole=billing, isPrimary=false
      - [ ] Verify User data enrichment in response (HTTP call to User Service)
      - [ ] Verify 404 when partner not found
      - [ ] Verify 404 when user not found (username doesn't exist)
      - [ ] Verify 409 when adding duplicate contact (same partner + username)
    - [ ] `DELETE /partners/{companyName}/contacts/{username}` - Remove contact
      - [ ] Test removing non-primary contact (should succeed)
      - [ ] Verify 404 when partner not found
      - [ ] Verify 404 when contact not found
      - [ ] Verify 400 when removing last primary contact
  - [ ] **Topic Voting Tests:**
    - [ ] `GET /partners/{companyName}/votes` - List partner votes
      - [ ] Test returns empty array when no votes
      - [ ] Test returns votes with topicId, voteValue, voteWeight, votedAt
      - [ ] Verify 404 when partner not found
    - [ ] `POST /partners/{companyName}/votes` - Cast topic vote
      - [ ] Test casting vote with voteValue=5 (high interest)
      - [ ] Test casting vote with voteValue=1 (low interest)
      - [ ] Verify vote weight calculated based on partnership tier (GOLD=3, PLATINUM=4, etc.)
      - [ ] Verify 404 when partner not found
      - [ ] Verify 400 when voteValue out of range (<1 or >5)
      - [ ] Verify 409 when partner already voted on topic (uniqueness constraint)
  - [ ] **Topic Suggestions Tests:**
    - [ ] `GET /partners/{companyName}/suggestions` - List suggestions
      - [ ] Test returns empty array when no suggestions
      - [ ] Test returns suggestions with status, dates, reviewedBy
      - [ ] Verify 404 when partner not found
    - [ ] `POST /partners/{companyName}/suggestions` - Submit suggestion
      - [ ] Test submitting suggestion with title, description, businessJustification
      - [ ] Verify status=submitted when initially created
      - [ ] Verify suggestedAt timestamp populated
      - [ ] Verify 404 when partner not found
      - [ ] Verify 400 when suggestedTopic exceeds 500 chars
      - [ ] Verify 400 when description exceeds 2000 chars
  - [ ] **Authentication & Authorization Tests:**
    - [ ] Verify 401 when no JWT token provided
    - [ ] Verify 403 when non-organizer tries to update partner (if role-based access control implemented)
  - [ ] **Error Response Schema Tests:**
    - [ ] Verify all error responses match shared-kernel ErrorResponse schema (correlationId, path, severity, message)
    - [ ] Verify 404 responses include appropriate error message
    - [ ] Verify 400 responses include validation details
    - [ ] Verify 409 responses include conflict details
  - [ ] **Test Data Setup (Prerequisites):**
    - [ ] Create test company: `GoogleZH` (companyName) via Company Service API or seed script
      - `POST /api/v1/companies` with: `{ "companyName": "GoogleZH", "displayName": "Google Zurich", ... }`
    - [ ] Create test user: `john.doe` (username) via User Service API or seed script
      - `POST /api/v1/users` with: `{ "username": "john.doe", "email": "john.doe@example.com", "firstName": "John", "lastName": "Doe" }`
    - [ ] Create test partner for GoogleZH: `POST /api/v1/partners` with partnership level GOLD
    - [ ] Create test topic (via Event Service) for voting tests
    - [ ] Document seed script locations or reference existing seed data from Stories 2.1/2.1b
  - [ ] Run all Bruno tests via CI script: `./scripts/ci/run-bruno-tests.sh partners-api`
  - [ ] Verify all tests pass against deployed staging environment

## Implementation References

**📖 READ FIRST:** Implementation details have been extracted to reusable guides. Read the guides below BEFORE starting implementation.

### Required Reading

**1. Service Foundation**
- 📖 **[Service Foundation Pattern Guide](../../docs/guides/service-foundation-pattern.md)** - Standard service structure, package layout, layer responsibilities
- 📖 **[Partner Coordination Service README](../partner-coordination-service/README.md)** - Service overview, quick start, architecture compliance

**2. OpenAPI Code Generation**
- 📖 **[OpenAPI Code Generation Guide](../../docs/guides/openapi-code-generation.md)** - Complete hybrid contract-first pattern
- 📖 **[Partner Service OpenAPI Codegen](../partner-coordination-service/OPENAPI-CODEGEN.md)** - Partner-specific quick reference
- **OpenAPI Spec**: `docs/api/partners-api.openapi.yml`

**3. Microservices HTTP Clients**
- 📖 **[Microservices HTTP Clients Guide](../../docs/guides/microservices-http-clients.md)** - Cross-service communication, JWT propagation, caching, HTTP enrichment
- **Critical**: Partner stores `companyName` (NOT `companyId` UUID), PartnerContact stores `username` (NOT `userId` UUID)
- **HTTP Enrichment**: `CompanyServiceClient.getCompany(companyName)`, `UserServiceClient.getUser(username)`

**4. Database Migrations**
- 📖 **[Flyway Migration Guide](../../docs/guides/flyway-migration-guide.md)** - Service-specific migrations, ADR-003 compliance, PostgreSQL patterns
- **Migrations**: `services/partner-coordination-service/src/main/resources/db/migration/`
  - `V1__Initial_baseline.sql` - Copied from shared-kernel (UUID extension, triggers)
  - `V2__create_partner_coordination_schema.sql` - Partner domain schema

### Architecture Decisions

- **[ADR-003: Meaningful Identifiers in Public APIs](../../docs/architecture/ADR-003-meaningful-identifiers-public-apis.md)** - Microservices isolation, NO cross-service FKs
- **[ADR-004: Factor User Fields from Domain Entities](../../docs/architecture/ADR-004-factor-user-fields-from-domain-entities.md)** - HTTP enrichment pattern
- **[ADR-006: OpenAPI Contract-First Code Generation](../../docs/architecture/ADR-006-openapi-contract-first-code-generation.md)** - Hybrid approach

### Quick Reference: Key Patterns

**Partner Entity (Meaningful ID):**
```java
@Entity
@Table(name = "partners")
public class Partner {
    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    private UUID id;  // ✅ UUID PK for this service

    @Column(name = "company_name", nullable = false, unique = true, length = 12)
    private String companyName;  // ✅ ADR-003: meaningful ID, NOT UUID FK

    // NO: private UUID companyId;  ❌ WRONG
}
```

**HTTP Enrichment Pattern:**
```java
@Service
@RequiredArgsConstructor
public class PartnerService {
    private final PartnerRepository partnerRepository;
    private final CompanyServiceClient companyServiceClient;  // HTTP client

    public PartnerResponse getPartner(String companyName, Set<String> includes) {
        Partner partner = partnerRepository.findByCompanyName(companyName)
            .orElseThrow(() -> new NotFoundException("Partner not found"));

        PartnerResponse response = mapToResponse(partner);

        // HTTP enrichment per ADR-004
        if (includes.contains("company")) {
            CompanyResponse company = companyServiceClient.getCompany(companyName);
            response.setCompany(company);  // Enrich response
        }

        return response;
    }
}
```

**Controller Pattern:**
```java
@RestController
@RequiredArgsConstructor
public class PartnerController implements PartnersApi {  // Generated interface
    private final PartnerService partnerService;
    private final IncludeParser includeParser;

    @Override
    public ResponseEntity<PartnerResponse> getPartnerByCompanyName(
            String companyName,
            Optional<String> include) {

        Set<String> includes = includeParser.parse(include.orElse(null));
        PartnerResponse partner = partnerService.getPartner(companyName, includes);

        return ResponseEntity.ok(partner);
    }
}
```

### Testing Strategy

**Integration Tests:**
```java
@SpringBootTest
@Transactional
class PartnerControllerIntegrationTest extends AbstractIntegrationTest {

    @Autowired private MockMvc mockMvc;

    @MockBean private CompanyServiceClient companyServiceClient;
    @MockBean private UserServiceClient userServiceClient;

    @Test
    void should_getPartner_when_validCompanyName() throws Exception {
        // Mock HTTP enrichment
        when(companyServiceClient.getCompany("GoogleZH"))
            .thenReturn(mockCompanyResponse());

        mockMvc.perform(get("/api/v1/partners/GoogleZH?include=company")
                .header("Authorization", "Bearer " + validToken))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.companyName").value("GoogleZH"))
                .andExpect(jsonPath("$.company").exists());
    }
}
```

### Build Commands

```bash
# Build service (includes OpenAPI generation)
./gradlew :services:partner-coordination-service:build

# Run tests (uses PostgreSQL Testcontainers)
./gradlew :services:partner-coordination-service:test

# Generate OpenAPI code only
./gradlew :services:partner-coordination-service:openApiGenerate
```

### Configuration Files

**build.gradle**: See [OpenAPI Code Generation Guide](../../docs/guides/openapi-code-generation.md) for complete Gradle configuration

**application.yml**: See [Service Foundation Pattern](../../docs/guides/service-foundation-pattern.md) for Spring Boot configuration

---

**⚠️ IMPORTANT:** All implementation patterns, code examples, and detailed instructions are in the guides above. Read them BEFORE starting implementation to avoid mistakes and rework.

---

## Definition of Done Checklist

⚠️ **CRITICAL:** Story cannot be marked complete until ALL items are checked.

### Development Complete

- [ ] All tests written BEFORE implementation (TDD followed)
- [ ] All acceptance criteria have corresponding tests
- [ ] All 13 acceptance criteria implemented
- [ ] Unit tests written and passing (>90% coverage)
- [ ] Integration tests cover new functionality (>80% coverage)
- [ ] Bruno E2E tests pass for all API endpoints (Task 12)
- [ ] Code follows project conventions (Google Java Style Guide)
- [ ] TypeScript types properly defined (N/A - backend only)
- [ ] API documentation updated (OpenAPI spec)
- [ ] Database migrations created and tested

### Schema Validation (For JPA/Database stories)

**Migration Alignment:**
- [ ] Task 0 completed: Migration validated against architecture
- [ ] Migration adapted to use meaningful IDs (companyName, username) instead of UUIDs
- [ ] Migration tested with PostgreSQL (not H2)
- [ ] Migration version: V2__create_partner_coordination_schema.sql in services/partner-coordination-service/src/main/resources/db/migration/
- [ ] V1__Initial_baseline.sql copied from shared-kernel (UUID extension + updated_at trigger)
- [ ] Flyway configured to run migrations on service startup (spring.flyway.enabled=true)

**JPA Entities:**
- [ ] Entities match database schema from migrations (columns, types, constraints)
- [ ] SchemaValidationTest written and passing (validates Flyway + JPA alignment)
- [ ] spring.jpa.hibernate.ddl-auto=validate in application-test.properties
- [ ] Tests run against PostgreSQL Testcontainer (AbstractIntegrationTest)

**OpenAPI Code Generation:**
- [ ] OpenAPI spec created/updated: docs/api/partners-api.openapi.yml
- [ ] Build.gradle configured for openApiGenerate task
- [ ] Shared-kernel types mapped (ErrorResponse, PaginationMetadata)
- [ ] Generated interfaces implemented by controllers
- [ ] Service layer uses generated DTOs

**HTTP Clients for Inter-Service Communication:**
- [ ] CompanyServiceClient implemented with RestTemplate
- [ ] UserServiceClient implemented with RestTemplate
- [ ] JWT token propagation from SecurityContext (ADR-003)
- [ ] HTTP enrichment in service layer (?include parameter)
- [ ] Caffeine cache configured (15-minute TTL)
- [ ] Error handling (404 Not Found, 5xx Server Error, network errors)
- [ ] @MockBean in integration tests (WireMock NOT required)

**Architecture Compliance:**
- [ ] Domain events extend DomainEvent<UUID> with full metadata:
  - [ ] PartnerCreatedEvent includes correlationId, causationId, userId
  - [ ] PartnerUpdatedEvent includes correlationId, causationId, userId
  - [ ] PartnerContactAddedEvent includes correlationId, causationId, userId
  - [ ] TopicVoteSubmittedEvent includes correlationId, causationId, userId
- [ ] Inject DomainEventPublisher from shared-kernel (NO custom publisher classes)
- [ ] Use FilterParser, SortParser, PaginationUtils from shared-kernel

**Database-Centric Architecture (per ADR-001):**
- [ ] PostgreSQL is single source of truth for all partner data
- [ ] NO AWS SDK calls for partner CRUD operations
- [ ] Service operates without AWS IAM credentials (beyond JWT validation)

**DDD Layered Architecture:**
- [ ] Service layer implements business logic (NO repository injection in controllers)
- [ ] Repository pattern for data access (JpaRepository extensions)
- [ ] Domain entities separate from DTOs
- [ ] Controllers delegate to service layer only

**ADR-003 Microservices Compliance:**
- [ ] Partner stores companyName (String), NOT companyId (UUID)
- [ ] PartnerContact stores username (String), NOT userId (UUID)
- [ ] NO JPQL joins to Company/User tables
- [ ] HTTP clients implemented for Company Service and User Service
- [ ] Circuit breaker configured (Resilience4j)
- [ ] HTTP responses cached (5min TTL)
- [ ] Graceful error handling for 404/503 from dependent services

### Testing Complete

**Unit Tests:**
- [ ] PartnerTest (domain logic)
- [ ] PartnerContactTest (domain logic)
- [ ] TopicVoteTest (vote weight calculation)
- [ ] PartnerServiceTest (business logic)
- [ ] PartnerContactServiceTest (HTTP enrichment logic)
- [ ] TopicVotingServiceTest (uniqueness, weight application)
- [ ] CompanyServiceClientTest (HTTP mocking)
- [ ] UserServiceClientTest (HTTP mocking)
- [ ] Coverage >90% for service layer

**Integration Tests:**
- [ ] All integration tests extend AbstractIntegrationTest
- [ ] PostgreSQL Testcontainer used (NOT H2)
- [ ] @MockBean used for Company/User Service HTTP mocking (NOT WireMock)
- [ ] PartnerControllerIntegrationTest (all CRUD endpoints)
- [ ] PartnerContactControllerIntegrationTest (HTTP enrichment)
- [ ] TopicVotingControllerIntegrationTest (vote uniqueness, weight)
- [ ] PartnerRepositoryTest (database constraints)
- [ ] SchemaValidationTest (Flyway + JPA alignment)
- [ ] Coverage >80% for API endpoints

**Performance Tests:**
- [ ] Partner list <100ms P95
- [ ] Partner detail <150ms P95
- [ ] Partner with contacts (HTTP enriched) <300ms P95
- [ ] @Timed annotations on service methods

**Bruno E2E Tests (Task 12):**
- [ ] Bruno collection created: `bruno-tests/partners-api/`
- [ ] Environment configuration complete (staging, local)
- [ ] Partner CRUD tests passing:
  - [ ] GET /partners (list, filtering, include parameter)
  - [ ] GET /partners/{companyName} (retrieve, 404 handling)
  - [ ] PATCH /partners/{companyName} (update, validation)
- [ ] Partner Contact tests passing:
  - [ ] GET /partners/{companyName}/contacts (list, HTTP enrichment)
  - [ ] POST /partners/{companyName}/contacts (add, validation, duplicates)
  - [ ] DELETE /partners/{companyName}/contacts/{username} (remove, business rules)
- [ ] Topic Voting tests passing:
  - [ ] GET /partners/{companyName}/votes (list votes)
  - [ ] POST /partners/{companyName}/votes (cast vote, weight calculation, uniqueness)
- [ ] Topic Suggestions tests passing:
  - [ ] GET /partners/{companyName}/suggestions (list suggestions)
  - [ ] POST /partners/{companyName}/suggestions (submit, validation, status)
- [ ] Authentication tests passing (401 unauthorized, 403 forbidden)
- [ ] Error response schema validation (ErrorResponse from shared-kernel)
- [ ] All tests pass via CI script: `./scripts/ci/run-bruno-tests.sh partners-api`
- [ ] Tests verified against staging environment

### Review Ready

- [ ] PR created with detailed description
- [ ] Code review completed by team
- [ ] Infrastructure review completed
- [ ] Security review passed (no secrets in code, IAM least privilege)
- [ ] Documentation updated:
  - [ ] README.md with API usage examples
  - [ ] OpenAPI spec at docs/api/partners-api.openapi.yml
  - [ ] Architecture compliance documented (ADR-003 microservices pattern)
  - [ ] HTTP client pattern documented
- [ ] CLAUDE.md updated if commands changed (N/A for this story)
- [ ] Deployment instructions documented

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-08 | 1.0 | Initial story creation for Partner Coordination Service Foundation (Epic 2) | Bob (Scrum Master) |
| 2025-01-08 | 1.1 | Updated to ADR-003 microservices pattern (meaningful IDs, HTTP clients, no JPQL joins) | Bob (Scrum Master) |
| 2025-01-08 | 1.2 | Added Task 12: Bruno E2E Tests for all Partner API endpoints (10 endpoints, 40+ test cases) | Bob (Scrum Master) |
| 2025-01-08 | 1.3 | Added OpenAPI Code Generation hybrid contract-first approach (ADR-006) | Bob (Scrum Master) |
| 2025-01-08 | 1.4 | Fixed Flyway migration location: service-specific (V2__*.sql) NOT shared-kernel (V015__*.sql) | Bob (Scrum Master) |
| 2025-01-08 | 1.5 | Fixed validation issues: Clarified V1 baseline copy rationale, added explicit Bruno test data setup, removed WireMock (use @MockBean consistently) | James (Dev Agent) |

## Dev Agent Record

**⚠️ NOTE:** This section is populated by the development agent during implementation.

### Agent Model Used

[To be filled by dev agent]

### Implementation Approach

[To be filled by dev agent - Document actual approach taken, deviations from plan, and rationale]

### Debug Log References

[To be filled by dev agent - Reference any debug logs or traces generated during development]

### Completion Notes

[To be filled by dev agent - Notes about task completion, issues encountered, and resolutions]

### Files Changed

**Created:**
[To be filled by dev agent - List all new files]

**Modified:**
[To be filled by dev agent - List all changed files]

**Deleted:**
[To be filled by dev agent - List all removed files]

### Deployment Notes

[To be filled by dev agent - Special deployment considerations, migration order, feature flags, rollback procedures, monitoring setup]

## QA Results

**⚠️ NOTE:** This section is populated by QA Agent after story completion.

[To be filled by qa-agent - Results from QA review of completed story implementation]
