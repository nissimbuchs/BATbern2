# BATbern E2E Test Environment Configuration
#
# Setup Instructions:
# 1. Copy this file to .env.test
# 2. Update values with your test environment configuration
# 3. DO NOT commit .env.test to version control (add to .gitignore)

# ============================================================================
# Application URLs
# ============================================================================

# Frontend application URL (local dev server)
E2E_BASE_URL=http://localhost:5173

# API Gateway URL
E2E_API_URL=http://localhost:8080

# ============================================================================
# Email Testing (MailHog)
# ============================================================================

# MailHog web UI and API URL
# Install: brew install mailhog (macOS) or docker run mailhog/mailhog
MAILHOG_URL=http://localhost:8025

# SMTP port for email delivery
MAILHOG_SMTP_PORT=1025

# ============================================================================
# Test User Credentials
# ============================================================================

# Test user email (must exist in Cognito User Pool)
E2E_TEST_EMAIL=test@batbern.ch

# Test user password
E2E_TEST_PASSWORD=Test123!@#

# Additional test users for role-based testing
E2E_ORGANIZER_EMAIL=organizer@batbern.ch
E2E_ORGANIZER_PASSWORD=Organizer123!@#

E2E_SPEAKER_EMAIL=speaker@batbern.ch
E2E_SPEAKER_PASSWORD=Speaker123!@#

E2E_PARTNER_EMAIL=partner@batbern.ch
E2E_PARTNER_PASSWORD=Partner123!@#

E2E_ATTENDEE_EMAIL=attendee@batbern.ch
E2E_ATTENDEE_PASSWORD=Attendee123!@#

# ============================================================================
# AWS Configuration (Deployed Environment)
# ============================================================================

# AWS Region
E2E_AWS_REGION=eu-central-1

# Cognito User Pool Configuration
E2E_COGNITO_USER_POOL_ID=eu-central-1_XXXXXXXXX
E2E_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# ============================================================================
# Test Configuration
# ============================================================================

# Test execution timeout (milliseconds)
E2E_TEST_TIMEOUT=30000

# Retry failed tests
E2E_RETRY_COUNT=2

# Headless mode (true for CI, false for local debugging)
E2E_HEADLESS=true

# Slow motion delay (milliseconds, useful for debugging)
E2E_SLOW_MO=0

# ============================================================================
# Feature Flags (Toggle features for testing)
# ============================================================================

# Enable/disable specific test suites
E2E_TEST_AUTH=true
E2E_TEST_EVENTS=false
E2E_TEST_SPEAKERS=false
E2E_TEST_PARTNERS=false
E2E_TEST_ATTENDEES=false

# ============================================================================
# Debugging
# ============================================================================

# Enable verbose logging
E2E_DEBUG=false

# Save screenshots on failure
E2E_SCREENSHOT_ON_FAILURE=true

# Save videos on failure
E2E_VIDEO_ON_FAILURE=true

# Save traces for debugging
E2E_TRACE=on-first-retry
