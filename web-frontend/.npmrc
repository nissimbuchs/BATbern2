# Use legacy peer deps to allow React 19 with packages that haven't updated peer deps yet
# Specifically needed for:
# - react-helmet-async@2.0.5 (supports up to React 18)
# - @xstate/react@3.2.2 (used by @aws-amplify/ui-react, supports up to React 18)
legacy-peer-deps=true
