# Swift OpenAPI Generator Configuration
# See: https://swiftpackageindex.com/apple/swift-openapi-generator/documentation

generate:
  - types        # Generate model types (Event, Session, Speaker, etc.)
  - client       # Generate API client code (optional - we have custom client)

accessModifier: public

# Additional configuration
additionalImports:
  - Foundation
  - SwiftData
