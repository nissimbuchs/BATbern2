meta {
  name: Cleanup - Delete Get-or-Create Test Companies
  type: http
  seq: 17
}

delete {
  url: {{baseUrl}}/companies/{{getOrCreateCompanyName}}
  body: none
  auth: inherit
}

headers {
  X-Correlation-ID: {{$guid}}
  Accept-Language: en-US
  Accept: application/json
}

assert {
  res.status: eq 204
}

tests {
  test("should delete test company (testcobruno)", function() {
    expect(res.getStatus()).to.equal(204);
    console.log("Deleted test company:", bru.getEnvVar("getOrCreateCompanyName"));
  });
}
