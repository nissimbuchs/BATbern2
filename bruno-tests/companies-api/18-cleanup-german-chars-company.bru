meta {
  name: Cleanup - Delete German Chars Test Company
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/companies/{{germanCharsCompanyName}}
  body: none
  auth: inherit
}

headers {
  X-Correlation-ID: {{$guid}}
  Accept-Language: en-US
  Accept: application/json
}

assert {
  res.status: eq 204
}

tests {
  test("should delete German chars test company (muellersoehn)", function() {
    expect(res.getStatus()).to.equal(204);
    console.log("Deleted German chars test company:", bru.getEnvVar("germanCharsCompanyName"));
  });
}
