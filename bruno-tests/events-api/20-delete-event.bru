meta {
  name: Delete Event (Cleanup)
  type: http
  seq: 20
}

delete {
  url: {{baseUrl}}/events/{{createdEventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 204
}

tests {
  test("should return 204 status", function() {
    expect(res.getStatus()).to.equal(204);
  });

  test("cleanup: clear event id", function() {
    bru.setVar("createdEventId", null);
  });
}
