# Deferred Work

## Deferred from: code review of 10-32-additional-user-emails (2026-05-22)

- **AC19 — persistent `user_activity` row for `ADDITIONAL_EMAIL_ADDED` / `ADDITIONAL_EMAIL_REMOVED`** [`services/company-user-management-service/.../service/UserService.java:1382-1407`] — `ActivityHistoryEntity` does not exist in this service yet; landing it mid-review was scope creep. Story 10.32 ships with CloudWatch-only audit logging (masked via `LoggingUtils.maskEmail`). Follow-up story: add a minimal `activity_history` table + JPA entity, then revisit OQ#6 (currently downgraded — masking is intentional because there is no user-visible timeline). [Auditor]

## Deferred from: code review of 11-e-4-epic-11-trailing-cleanup (2026-05-18)

- **Sprint-status YAML comment field has become unmaintainable** [`_bmad-output/implementation-artifacts/sprint-status.yaml:540`] — Multiple multi-paragraph `# ...` trailing comments per story value; `last_updated:` carries narratives from 11.E.4 prepended to 11.E.3 prepended to earlier stories. Reduce to one short sentence per story; truncate `last_updated:` to current update only. Hygiene; longstanding branch pattern. [Blind]
- **5 Bruno tests deleted vs. AC3's authorized 1** [`bruno-tests/speaker-portal-api/12-setup-tentative-speaker.bru`, `13-send-tentative-invitation.bru`, `13b-get-tentative-test-token.bru`, `15-tentative-can-change-to-accept.bru`] — Dev correctly identified the orphan chain after deleting `14-respond-tentative.bru`. However, tests 12 (`POST /speakers/invite` smoke) and 13 (invitation-send + INVITED-state assertion) had standalone coverage value beyond TENTATIVE. Verify whether equivalent assertions live in `bruno-tests/events-api/`; if not, restore those two tests under non-tentative names. [Blind]
- **CUMS `provisionUserWithRole` retains `"Speaker"`/`"Unknown"` fallback** [`services/company-user-management-service/src/main/java/ch/batbern/companyuser/service/UserService.java:677-696`] — AC4 deleted the EMS-side fallback; CUMS still derives placeholder names from email local-part when firstName/lastName arrive null. Today no risk (only EMS-promote calls reach it with `@NotBlank` validation), but any future internal caller can regress the invariant silently. Either tighten CUMS OpenAPI `ProvisionUserRequest` to required firstName+lastName, or add a comment documenting AC4 contract + flag the fallback as defense-in-depth only. Out of scope for Epic-11 cleanup. [Edge Case]
- **`notification.ts` NotificationType still recognises legacy `SLOT_ASSIGNED`/`OVERFLOW`** [`web-frontend/src/types/notification.ts`, `web-frontend/src/components/.../TeamActivityFeed.tsx`] — Separate type system from `SpeakerWorkflowState`; explicitly out of AC1 scope per dev notes. Worth confirming whether backend still emits these notification types after Phase B enum reduction. [Blind]
- **Bruno test `51-speaker-workflow-invalid-transition.bru:41-44` comment pins 422 mapping not in ADR-009** [`bruno-tests/events-api/51-speaker-workflow-invalid-transition.bru`] — Comment locks "(currently 422 via the workflow service's domain exception mapping)" but ADR-009 §0.2 doesn't pin the HTTP status. Either pin the contract in §0.2 or remove the parenthetical to keep implementation-agnostic. [Blind]
- **ADR-009 §0.3 says `magic_link_tokens` is in event-management-service schema** [`docs/architecture/ADR-009-unified-speaker-workflow.md` §0.3 table] — Table header says "all in the event-management-service schema unless noted"; `magic_link_tokens` row has no override note. Add `(in CUMS schema)` parenthetical or footnote the table header. Misleading for readers chasing schema ownership. [Blind]
- **`mode: 'onTouched'` + react-hook-form `isValid` initial state may not gate submit-disabled until first blur** [`web-frontend/src/components/SpeakerBrainstormingPanel/PromoteSpeakerDialog.tsx:107-118`] — With `mode: 'onTouched'` and empty defaults, `isValid` may initially be `true` (no errors computed). Tests pass today (defaults pre-fill from `splitFullName`), but if a future caller passes empty defaults the submit-disabled rule could lag. Either set `mode: 'all'` or explicitly check non-empty in the `disabled` prop. [Blind]
- **MUI testid + `user.click(field); user.tab()` fragility in PromoteSpeakerDialog tests** [`web-frontend/src/components/SpeakerBrainstormingPanel/PromoteSpeakerDialog.test.tsx:1571-1574, 1584-1587`] — MUI `data-testid` typically lands on the outer wrapper, not the inner `input`. Clicking the wrapper may not focus the input → tab does not blur it. Tests pass today (the field may be `Autofocus={false}` and tab still works), but fragile under future MUI version bumps. Use `getByRole('textbox', { name: /first name/i })` or pass testid via `inputProps`. [Blind]
- **`splitFullName("Mononym")` pre-fills empty lastName, no visible error on first render** [`web-frontend/src/components/SpeakerBrainstormingPanel/PromoteSpeakerDialog.tsx:46-55, 107-110`] — Single-word `speakerName` (e.g., "Madonna") produces `firstName: "Madonna", lastName: ""`. With `mode: 'onTouched'`, no helper text fires until user touches the empty lastName field. Submit is correctly disabled but organizer sees no reason. UX papercut; trigger initial validation or force re-entry of both names when single-word. [Edge Case]
- **Identity-rebind test seeds CONTACTED speaker with non-null username — artificial invariant violation** [`services/event-management-service/src/test/java/ch/batbern/events/service/SpeakerWorkflowServiceTest.java:1297-1298`] — Production should never produce a CONTACTED speaker with a username (per ADR-009 §0.1 "No User yet"). The rebind guard's real domain is post-READY rebinding. Either justify with a `// simulates leftover from partial Phase B migration` comment or seed READY state for the guard test. [Blind]

## Deferred from: code review of 11-e-2-cognito-provisioning-at-ready-invitation-email-i18n (2026-05-18)

- **`runInvitedHook` performs Cognito mutation synchronously inside a JPA `@Transactional`** [`services/event-management-service/src/main/java/ch/batbern/events/service/SpeakerWorkflowService.java:296-315`] — If any later step in the transaction throws (statusHistory write, eventPublisher listener), the DB rolls back but Cognito has already been mutated (password rotated, possibly never communicated). The AFTER_COMMIT TransactionalEventListener pattern is the right fix; code comment already flags the email-send variant but not the credential-issuance variant. Bundle with the AFTER_COMMIT design discussion alongside Story 11.F.1. [Blind #1 + Edge #9]
- **AdminCreateUser succeeds → JPA commit fails on the same transaction → orphan Cognito user** [`services/company-user-management-service/.../UserService.java:710-747`] — Sequence: `createNewUser` (persists User) → `roleService.addRole` → `cognitoService.adminCreateUserSilently` SUCCEEDS → commit FAILS (deadlock, connection drop). Transaction rolls back; Cognito user remains. Next retry generates a different username; Cognito's `preferred_username` attribute now drifts from Postgres. Speaker logs in successfully via Cognito but `getCurrentUser` throws UserNotFoundException. Requires either an outbox pattern or a reconciliation job; out of scope for 11.E.2. [Edge #2]
- **AdminSetUserPassword succeeds → `@Async` email send throws → speaker locked out** [`services/event-management-service/.../SpeakerInvitationEmailService.java:113-117`] — Async failures are swallowed at the catch-all; the workflow transition still commits (speaker → INVITED). The new Cognito temp password is in nobody's hands. Operator must manually retry (the issue-invitation-credentials endpoint IS idempotent — but operators don't know it failed). Needs persistent retry queue or pre-send commit gate. [Edge #3]
- **`EXTERNAL_PROVIDER` Cognito status maps to 422 (Unprocessable)** [`services/company-user-management-service/.../UserService.java:797-830`] — Switch covers FORCE_CHANGE_PASSWORD/RESET_REQUIRED/CONFIRMED/UNCONFIRMED/ARCHIVED/COMPROMISED/UNKNOWN_TO_SDK_VERSION explicitly; `EXTERNAL_PROVIDER` (and any other future-added value) falls to `default` → 422. Federated IdP isn't on the near-term roadmap; revisit if Cognito SAML/Google IdP support is added. [Edge #4]
- **Cognito `username` vs `preferred_username` drift on email change** [`services/company-user-management-service/.../CognitoIntegrationServiceImpl.java:920-932`] — `AdminCreateUser` uses email as the Cognito username; later calls use email again as the lookup key. If the speaker changes their email in CUMS post-provision, the Cognito record stays keyed by the old email → `getUserStatus(user.getEmail())` throws UserNotFoundException. Pre-existing Cognito-design pitfall, broader scope than 11.E.2. [Blind #16]
- **`UsernameExistsException` swallow doesn't distinguish race from stale state** [`services/company-user-management-service/.../CognitoIntegrationServiceImpl.java:937-938`] — Swallowing INFO-logs as "idempotent no-op". If Cognito user is `ARCHIVED` (or otherwise unhealthy), the silent-swallow at provision time hides the problem; the issueInvitationCredentials call at INVITED then throws 422 — too late, the BATbern User row already exists. Detect at provision time via a follow-up `AdminGetUser` status check. [Blind #22]
- **Throwaway temp password "discard" is theatrical + AWS SDK exception PII risk** [`services/company-user-management-service/.../UserService.java:1118-1124`] — `String throwawayTempPassword = null;` reassignment doesn't zero memory. More importantly, if `AdminCreateUser` fails with a validation error, some SDK error paths include the request body in serialized exception messages. `CognitoOperationException` wraps the cause and is logged. Needs an AWS SDK v2 exception-message audit to confirm passwords never appear. [Blind #3]
- **`deriveHostBase(loginUrl)` strips path → fragile under sub-path deployments** [`services/event-management-service/.../SpeakerInvitationEmailService.java:2112-2125`] — Currently fine because `app.base-url` is always a bare scheme+host. If a future deploy puts the portal at `https://batbern.ch/portal/`, derived `eventUrl` will silently drop `/portal`. Keep the existing `@Value("${app.base-url}")` field on the email service instead of regex-stripping. [Blind #7]
- **Plain-text temp password in service-to-service JSON; `RestTemplate` DEBUG logging risk** [`services/event-management-service/.../UserApiClientImpl.java:1782-1784`] — The response body contains the fresh temp password. Spring `RestTemplate` at DEBUG level dumps response bodies. Verify staging/prod log configs explicitly suppress this, or wrap the response in a redacting interceptor. [Blind #8]
- **AC10 manual staging smoke + AC13 Bruno CI run punted to post-merge** — Acceptable by design: 11.E.1 must be deployed to staging before the round-trip can be verified. Risk: PR merges without the CloudWatch evidence required by AC10's final clause. PR description must carry the four CloudWatch log lines + the re-invite negative-case observation. [Blind #24]
- **Email format validation gap in `provisionUserWithRole`** [`services/company-user-management-service/.../UserService.java:680-696`] — Email validation is `isBlank()` only. A weird input like "weird-id" (no `@`) derives `firstName="Weird" lastName="Id"` and passes to Cognito as the username — Cognito accepts but downstream attribute filters break. Pre-existing input-validation gap; broader scope than 11.E.2. [Edge #11]

## Deferred from: code review of 11-e-1-cdk-iam-prereq-cognito-admin-flow (2026-05-17)

- **`fromUserPoolId(...) as unknown as cognito.UserPool` cast** [`infrastructure/test/unit/company-management-stack.test.ts:47-51`] — Spec dev-agent record line 558 acknowledges the cast is "outside this story's scope." If `CompanyManagementStack` ever reads anything beyond `.userPoolArn` from `props.userPool` (e.g. `.addClient`, `.userPoolProviderName`), the test stub will throw at synth time. Fix: widen `props.userPool` to `IUserPool` and adjust call sites. [Blind #4 + Edge #7]
- **No IAM `Condition` keys narrowing `AdminSetUserPassword` to specific usernames** [`infrastructure/lib/stacks/company-management-stack.ts:163-176`] — Defense-in-depth hardening; the four-action grant resource-scopes to the pool but does not constrain which users CUMS can reset passwords for. Cognito supports condition keys like `aws:RequestTag/Role` for finer scoping. [Blind #10]
- **`should_configureOAuthFlows_when_appClientCreated` may not pin all four `ExplicitAuthFlows` as a closed set** [`infrastructure/test/unit/cognito-stack.test.ts:~70-88`] — Pre-existing test gap. The new AC6 test asserts presence-only (`Match.arrayWith`) per spec, but a future regression silently removing SRP or USER_PASSWORD_AUTH wouldn't fail the suite. Tighten to a closed set in a separate test-hardening sweep. [Blind #19 + Edge #5]
- **ECS task-role IAM policy picked up at next STS credential refresh (≤6h), not immediately** — Operational note for Story 11.E.2 rollout. IAM-policy-only CDK deploys do NOT trigger ECS task replacement. 11.E.2's first deploy must force a service redeploy (or wait ≤6h) to guarantee task-credential refresh, or `AdminCreateUser` calls fail with AccessDenied. Capture in 11.E.2's "Verification" section. [Edge #8]
- **14-day temp password window — security-tradeoff comment missing** [`infrastructure/lib/stacks/cognito-stack.ts:204`] — The inline comment captures UX rationale (two weekends) but not the threat model (longer email-resident credential validity, increased phishing/forwarding window). Document in 11.E.2's invitation-email rewrite story (the natural seam for inbox-retention assumptions + forgot-password fallback). [Blind #6]
- **Tests not parametrized across `isProduction: true|false`** [`infrastructure/test/unit/company-management-stack.test.ts`] — Coverage enhancement. The new tests only exercise `stagingConfig` (`isProduction: true`). A future config-conditional Cognito grant in `CompanyManagementStack` would not be caught by either positive or negative regression guard. [Edge #6]

## Deferred from: code review of 11-d-3-kanban-column-triage-time-in-state-colour-coding (2026-05-17)

- **`getStatusChangedAt` `updatedAt` fallback inflates time-in-state on note/organizer edits** [`web-frontend/src/components/organizer/SpeakerStatus/kanbanThresholds.ts:79-93`] — pre-existing Resolved Q#2 from 11.D.2; backend `status_changed_at` column is the proper fix, out of scope here.
- **`slotCapacity.reached` derived from in-memory `speakers` array — breaks silently if the kanban query ever paginates** [`SpeakerStatusLanes.tsx:333-345`] — code comment already flags this; switch to a server-side count endpoint if pagination lands.
- **INVITED with `responseDeadline === undefined` silently normal — no UI signal that the workflow has stalled** [`kanbanThresholds.ts:111-113`] — surfacing missing-deadline is a distinct UX concern; address with a dedicated "INVITED without deadline" warning if organisers report this in practice.
- **INVITED `joiner` i18n key has no defensive code fallback** [`SpeakerStatusLanes.tsx:201`] — structural punctuation pushed to locales; missing-key would render the literal i18next key path. Defensive `?? ' · '` would protect against future locale-contributor mistakes.
- **`MS_PER_DAY = 86_400_000` ignores DST/leap-second days — `diffDays` off-by-one on transition days** [`kanbanThresholds.ts:51`] — replace with `date-fns` `differenceInCalendarDays` when next touched.
- **INVITED 1-hour boundary at deadline moment — same root cause as date-only ISO parsing as UTC midnight** [`kanbanThresholds.ts:111-115`] — bundle with the date-only TZ parsing fix.
- **`Math.floor` asymmetry in `diffDays(later, earlier)` vs `-diffDays(earlier, later)` at sub-day deltas** [`kanbanThresholds.ts:51`] — semantically debatable; document in JSDoc when the date-only parsing rework lands.
- **gsw-BE plural grammar — single-form `überfellig` for both 1 and many** [`gsw-BE/organizer.json`] — same family as the wider i18next plural-variant question; tackle alongside the native-speaker review pass on machine-translated baselines.
- **QUALITY_REVIEWED chip flicker while event detail is loading** [`SpeakerStatusLanes.tsx:843-852`] — minimal fix shipped: JSDoc on `eventDate` prop documents the expected "default → warning/error" flip after the event query resolves. A skeleton-chip during loading would be cleaner but requires a design call on the placeholder appearance; revisit when QA reports it.

## Deferred from: code review of 11-d-2-kanban-card-primary-action-button-cleanup (2026-05-17)

- **`i18n.language === 'de'` covers only 2 of 10 supported locales for `date-fns` time-distance formatting** [`web-frontend/src/components/organizer/SpeakerStatus/SpeakerStatusLanes.tsx:516`] — Story-D2 spec AC3 is faithful to the existing `TeamActivityFeed.tsx` pattern (same de/enUS-only handling). Non-de users see English distance strings inside otherwise-translated kanban. Project-wide concern; tackle alongside the wider i18n locale-completeness sweep.
- **`i18n.language === 'de'` strict equality misses `de-CH`/`de-DE`/`gsw-BE`** [`SpeakerStatusLanes.tsx:516`] — Swiss-German users fall to enUS rather than the German `date-fns` locale. Same root cause as above; track with the wider i18n cleanup.
- **`gsw-BE` locale never had `speakers.tentative*` keys to remove** [`web-frontend/public/locales/gsw-BE/organizer.json`] — pre-existing locale gap; AC5(C)'s "removed from all 10 locales" sweep was vacuously satisfied for gsw-BE. Investigate whether other namespaces also lag for gsw-BE.
- **Stale modal state when speaker mutated mid-modal** [`EventSpeakersTab.tsx:763-770`] — `outreachModalState.speaker` / `promoteModalState.speaker` are snapshot copies; a 30 s background refetch changing email/status/assignment is invisible to the open modal. Requires a redesign of modal-state ownership (subscribe to live speaker via React Query rather than snapshot prop).
- **Snackbar state is per-card** [`SpeakerStatusLanes.tsx` SpeakerCard local state] — clicking many Send-invitation buttons quickly fires conflicting/hidden toasts. Pre-existing pattern; hoist to a global snackbar provider in a separate refactor.
- **`SpeakerStatusDashboard` mounts `SpeakerStatusLanes` without the new callbacks** [`SpeakerStatusDashboard.tsx:181`] — the `??` no-op defaults silently swallow clicks. Component is not on any production route today (only referenced from tests). Latent — if any future caller wires `SpeakerStatusDashboard` back in, the kanban's primary-action buttons will be inert.
- **`STATUS_LANES` does not catch-all legacy union members `SLOT_ASSIGNED`, `WITHDREW`, `OVERFLOW`** [`speakerPool.types.ts` `SpeakerWorkflowState` union, `SpeakerStatusLanes.tsx:91-100`] — speakers stuck in those states on brownfield events disappear from the kanban entirely. Bundle with the wider `SpeakerWorkflowState` union cleanup planned after Phase D.
- **`slotCapacity` `useMemo` recomputes on every `speakers` array reference change** [`SpeakerStatusLanes.tsx:165`] — React Query returns fresh arrays every refetch (incl. 30 s background poll), defeating downstream `SpeakerCard` memoization. Performance nit, not correctness; revisit if profiling shows the kanban is a hot spot.
- **Empty 4px organizer-row Box when both `assignedOrg` and `timeInState` are falsy** [`SpeakerStatusLanes.tsx` organizer-row rendering] — cosmetic edge under "no organizer + no timestamps anywhere" data; harmless in practice.
- **Snackbar "Invitation sent" toast can be lost on subsequent refetch re-render** [`SpeakerStatusLanes.tsx:501-503`] — invalidation re-renders the card with status INVITED; per-card snackbar local state may not survive the rerender. Pre-existing pattern; resolve with the snackbar-hoist refactor above.

## Deferred from: code review of 11-d-1-promote-endpoint-brainstorm-tightening-slot-gate (2026-05-16)

- **`lastNameFallback` returns empty string for single-word speaker names** [`services/event-management-service/src/main/java/ch/batbern/events/service/SpeakerWorkflowService.java:471-478`] — pre-existing fallback design from 11.B.2 (already deferred in that review as "`firstNameFallback`/`lastNameFallback` literals 'Speaker' / 'Unknown'"). When `speakerName` has a single word, the User is created with `lastName=""`. Whether `provisionUserWithRole` enforces non-blank lastName is not verified by this story. Address alongside the existing 11.B.2 deferred item, ideally in Story 11.E.2 which owns Cognito welcome-email rendering.
- **maxLength asymmetry — firstName/lastName=100 vs email=320; silent UI truncation via `inputProps.maxLength`** [`services/event-management-service/src/main/java/ch/batbern/events/dto/PromoteSpeakerRequest.java`, `web-frontend/src/components/SpeakerBrainstormingPanel/PromoteSpeakerDialog.tsx`] — `inputProps.maxLength={100}` silently caps user input at 100; API callers that bypass the UI get a generic "size must be between 0 and 100" 400. Bumping to 255 to fit longer composite names ("Maria del Carmen Fernández-Echevarría Smith") is a spec change, not a review patch. Revisit when validation conventions are next reviewed.
- **`usePromoteSpeakerToReady` invalidates `speakerStatusSummary` with a hardcoded query-key string** [`web-frontend/src/hooks/useSpeakerPool.ts:138-140`] — consistent with sibling hooks in the same file. A future consolidation into a `speakerStatusSummaryKeys` factory (analogous to `speakerPoolKeys`) would prevent silent breakage if the key is ever renamed. Out of scope for this story; bundle with a TanStack-Query-keys refactor.
- **Identity-rebind guard reject-path not exercised by tests** [`services/event-management-service/src/test/java/ch/batbern/events/service/SpeakerWorkflowServiceTest.java`] — `provisionUserWithRole` mock always returns the same username, so the rebind guard's reject path (different username returned for the same email) is never exercised. Add a test that stubs a divergent return and asserts `ValidationException` is thrown.
- **`saturate = 24` magic constant in slot-capacity integration test** [`services/event-management-service/src/test/java/ch/batbern/events/controller/SpeakerInvitationControllerIntegrationTest.java:604-617`] — works for all current event types but silently stops actually saturating if any event-type maxSlots is bumped to ≥ 24. Replace with `eventTypeService.getEventType(...).getMaxSlots()` read when next touched.
- **`InOrder` block does not assert `applicationEventPublisher.publishEvent(...)` ordering** [`services/event-management-service/src/test/java/ch/batbern/events/service/SpeakerWorkflowServiceTest.java`] — `applicationEventPublisher` is added to the InOrder mock group but never verified at the end. Would catch a regression where the event publish moves before the DB save.

## Deferred from: code review of 11-c-2-userapiclient-provisioning-contentsubmissionservice-shared (2026-05-16)

- **Same-instant `Instant.now()` rows in `speaker_status_history` may share `changed_at`** [`SpeakerWorkflowService.java:380`] — PostgreSQL TIMESTAMP microsecond precision makes collisions rare in real workflows; only flagged by Edge Case Hunter. Address with a tiebreak column (sequence/serial) or `INSERT ... RETURNING` ordering only if test flakiness emerges.
- **SPEAKER self-patch fails 403 on case-mismatched username in URL** [`UserController.java:723`] — `String.equals` is case-sensitive. Usernames are stored canonical-case from Cognito provisioning, so the mismatch only happens with mistyped URLs. Revisit when Phase E lands Cognito-issued JWTs.
- **`magicLinkService.markTokenAsUsed` is functionally no-op for SUBMIT tokens** [`SpeakerPortalContentController.java:216`] — `MagicLinkService.validateToken` only checks `is_used` for RESPOND tokens. The new call is misleading but harmless. Will be removed in Phase F magic-link teardown.
- **`UserApiClientImpl.provisionUserWithRole` does not error on HTTP 200 with null body** [`UserApiClientImpl.java:1378-1391`] — null-body checks exist for logging only; null is then returned to callers. CUMS controller never returns null and OpenAPI-validated client/server pair enforces non-null response. Address only if observed in production.
- **Async `SpeakerContentSubmittedEvent` listener failure may drop organizer notification** [`ContentSubmissionService.java:2348`] — out-of-scope for this story; standard async event-publisher concern. Confirm retry/DLQ semantics for `SpeakerContentSubmittedEvent` listeners in a separate observability story before MVP.

## Deferred from: code review of 11-c-1-drop-speakers-table-remove-speaker-coordination-refs (2026-05-16)

- **Watch services lose batch user lookup → N+1 HTTP loop** [`services/event-management-service/src/main/java/ch/batbern/events/watch/WatchEventController.java`] — `mapToActiveEventDetail` replaced batch `speakerRepository.findAllByUsernameIn(...)` with per-record `userApiClient.getUserByUsername(...)`. Open Q4 (resolved 2026-05-15) accepted per-record for 5-10 speakers per event. Revisit if smoke shows >500ms latency in local-native.
- **`UserApiClient` retains three dead methods** (`updateUser`, `updateUserProfilePicture`, `getSpeakerUsernames`) [`services/event-management-service/src/main/java/ch/batbern/events/service/UserApiClient.java`] — explicitly deferred to Story 11.C.2 which rewrites `UserApiClient` with `patchUserProfile`. No live callers remain in EMS.
- **`ProfileUpdatePage.tsx` + `speakerPortalService.getProfile/updateProfile` still wired in frontend** [`web-frontend/src/App.tsx:277`, `web-frontend/src/services/speakerPortalService.ts:444-525`] — backend `/speaker-portal/profile*` endpoints deleted (AC4), frontend tear-down scoped to Story 11.F.1. Until then, clicking "Update profile" from `ContentSubmissionPage:358` shows a silent failure UI.
- **Kanban `OUTREACH_LANES` / `POST_ACCEPTANCE_LANES` reorder is only partial** [`web-frontend/src/components/organizer/SpeakerStatus/SpeakerStatusLanes.tsx`] — full ADR-009 §0.1 lane ordering (move `INVITED` to end of outreach; add `ACCEPTED` as first post-acceptance lane) owned by Stories 11.D.2 / 11.D.3 per their scope. This story only removed `'CONFIRMED'` from post-acceptance.
- **19 Bruno test failures across `users-api` / `events-api` / `speaker-portal-api`** [`bruno-tests/events-api/`, `bruno-tests/speaker-portal-api/`] — pre-existing 11.B.* carryover: tests reference removed `TENTATIVE` state, removed `ACCEPTED → IDENTIFIED` transitions, or `send-invitation` endpoints whose validation tightened in 11.B.2/B.3. Phase B owners should sweep the suite.
- **Sprint-status YAML promotes 11.C.1 to `review` while prerequisite 11.B.3 is still `in-progress`** [`_bmad-output/implementation-artifacts/sprint-status.yaml`] — merge-train ordering risk: V94 depends on V93 landing first. Track to ensure correct sequencing at merge time.
- **No `UserApiClient` batch path added** [`services/event-management-service/src/main/java/ch/batbern/events/service/UserApiClient.java`] — Open Q4 explicitly accepted per-record. Listed so a future contributor adding a new N-record fan-out screen doesn't repeat the per-record loop without revisiting the decision.

## Deferred from: code review of 11-b-3-migrate-legacy-status-drop-tentative-cols-derived-flags (2026-05-16)

- **Misleading test semantics in `should_rejectLegacyStatusUpdate_when_constraintsTightened`** [`V93LegacySpeakerStatesMigrationIntegrationTest.java`] — witness row (`slotAssignedSpeakerId`) was already mapped to `accepted` by the migration, so the constraint check is incidental rather than intentional. Test functions correctly but is misleading to future readers. Low priority.
- **`isSlotAssigned` over-reporting in fallback path (`patchEntry`/`assignSpeakerToOrganizer`)** [`SpeakerPoolResponse.java`, `SpeakerPoolService.java`] — when `SpeakerPool.sessionId != null` but the session has no `startTime`, the fallback `fromEntity(SpeakerPool)` returns `isSlotAssigned: true` while `countPublishableByEventId` would exclude that speaker. Inconsistency between PATCH response and GET /pool response. Fix requires session-loading in those call sites. Deferred to Phase D Story 11.D.4 per story scope guard (AC7 item 3, Resolved Decision #7).
- **READY rejection fires before log statement** [`SpeakerStatusController.java`] — the `READY_REQUIRES_PROMOTE_ENDPOINT` check throws before the `log.info` line, so READY requests are never logged. Operational observability gap; not a correctness issue.
- **`MagicLinkService` null `previousResponse` for QUALITY_REVIEWED migrated speakers** [`MagicLinkService.java`] — speakers migrated from `CONFIRMED → QUALITY_REVIEWED` may have null `acceptedAt`, causing `alreadyResponded: true, previousResponse: null` which the speaker portal UI cannot render gracefully. Pre-existing design gap made visible by V93. Phase F magic-link teardown is the appropriate owner.
- **No test for `sessionId == null` derived-flag path** [`SpeakerStatusControllerIntegrationTest.java`] — the `fromEntity(SpeakerPool, null)` else-branch for `sessionId == null` is trivially correct (returns `isSlotAssigned = false`) but has no dedicated assertion. Low priority.

## Deferred from: code review of 11-b-2-speakerworkflowservice-sole-status-writer (2026-05-16)

- **Side-effect hooks fire emails + external HTTP before commit** [`SpeakerWorkflowService.java` `runInvitedHook` / `runAcceptedHook` / `runReadyHook`] — `runInvitedHook` sends the invitation email inside the `@Transactional` boundary; if a downstream operation rolls back, the email is already gone. Initial code-review fix wired `TransactionSynchronizationManager.afterCommit` but had to be reverted: 5 `SpeakerWorkflowServiceIntegrationTest` cases assert on the email/organizer-notify mocks under `@Transactional` rollback semantics, where after-commit callbacks never fire. Follow-up needs: (a) wire AFTER_COMMIT via either a domain-event listener (`SpeakerInvitedEvent` / `SpeakerAcceptedEvent` consumed by `@TransactionalEventListener(phase = AFTER_COMMIT)`) or `TransactionSynchronizationManager` in the service; (b) update the affected integration tests to commit the transaction (`TestTransaction.flagForCommit()` or `@Sql(executionPhase = ...)`); (c) restore the after-commit symmetry for runDeclinedHook's organizer notify.
- **Slot-capacity TOCTOU race + concurrent `sendInvitation` race** [`SpeakerWorkflowService.java:201-209`, `SpeakerInvitationService.java:165-217`] — `enforceSlotCapacity` reads `count(ACCEPTED) + count(INVITED)` without a row/event lock and `SpeakerPool` has no `@Version`. Two concurrent `READY → INVITED` callers can both pass the gate and both commit; two concurrent `sendInvitation` calls on the same speaker can each generate magic-link tokens. Explicitly accepted by PM Resolved Decision #6 ("race window matches current behaviour"). Future story should add `@Lock(LockModeType.PESSIMISTIC_WRITE)` on `speakerPoolRepository.findById(...)` inside `transition()` and a uniqueness constraint or `@Version` on `SpeakerPool`.
- **`runDeclinedHook` deletes session without checking co-presenters** [`SpeakerWorkflowService.java:268-275`] — `sessionRepository.deleteById(sessionId)` runs unconditionally for post-invitation declines. Pre-existing behaviour from the deleted `SpeakerStatusService.updateStatus`. If multiple `SpeakerPool` rows share a `sessionId` (panels), the other speakers end up with orphaned `session_id` FKs. Track in a follow-up story for multi-speaker session handling — at minimum, `findBySessionId(sessionId).size() > 1 ? clearLink : delete` would close the obvious hole.
- **Coverage gaps in new test suite** [`SpeakerInvitationServiceTest`, `SpeakerWorkflowServiceTest`, `SpeakerWorkflowServiceIntegrationTest`] — missing: explicit `RESPOND` + `VIEW` `TokenAction` assertion in invited-hook test; `READY → DECLINED` and `CONTACTED → DECLINED` paths (no reason required, no organizer notify); spec-named `should_notReFireSessionAutoCreation_when_acceptedToAcceptedSameStateTransition()` (covered structurally but not by name); panel-session-delete regression. Coverage target (≥90%) is met (91.8%); these are dark scenarios rather than gaps in line coverage.
- **`firstNameFallback` / `lastNameFallback` literals "Speaker" / "Unknown"** [`SpeakerWorkflowService.java:367-379`] — when payload omits name and `speakerName` is blank, the new User has `firstName=Speaker, lastName=Unknown`. Becomes Cognito user attributes in Phase E (Story 11.E.2 owns the welcome-email rendering and should decide whether to (a) reject the transition at the seam, (b) require name on `TransitionPayload` for `CONTACTED → READY`, or (c) accept the placeholder and let the welcome email render around it).
- **`resolveLocale` accepts arbitrary garbage** [`SpeakerWorkflowService.java:355-365`] — `Locale.forLanguageTag("xyz123")` silently returns an empty-language locale; email template falls back to service default. Pure observability concern (debug-time mystery for translators); not user-facing.
- **`enforceSlotCapacity` N+1 on batch invites** [`SpeakerWorkflowService.java:201-209`] — each `transition` to INVITED issues `eventTypeService.getEventType(...)` + two `countByEventIdAndStatus` queries. `3N` round-trips for batch-invite. Revisit if/when batch-invite becomes a hot path; can be cached per `event.eventType` for the duration of a request or replaced with a single aggregate query.
- **`MagicLinkService.RESPONDED_STATES` interaction with legacy DB rows** [`MagicLinkService.java:65-69`] — new set `{ACCEPTED, DECLINED, CONTENT_SUBMITTED, QUALITY_REVIEWED}` excludes the legacy `CONFIRMED` / `SLOT_ASSIGNED` values. A legacy `CONFIRMED` row would let the magic-link respond form re-open for an already-committed speaker. Gated on Story 11.B.3 Flyway migration; 11.B.3 should also include a post-migration assertion that no row holds a legacy value.

## Deferred from: code review of 11-b-1-reduce-speakerworkflowstate-enum-to-8-states (2026-05-15)

- **No empty-string / whitespace / very-long-value validation for required `String` fields in `SpeakerPromotedToReadyEvent`** [`shared-kernel/src/main/java/ch/batbern/shared/events/SpeakerPromotedToReadyEvent.java:73-104`] — only null is rejected; empty username/email could reach Phase E Cognito mailer. Consistent with sibling events today; revisit as a cross-cutting domain-event validation policy.
- **`Instant.MIN`/`MAX`/stale-date values accepted unchecked for `promotedAt`** [`SpeakerPromotedToReadyEvent.java:102`] — no time-range guard. No sibling event has one either.
- **`Instant` serialisation format not pinned by JSON test** [`SpeakerPromotedToReadyEventTest.java:85`] — test asserts key presence only, not ISO-8601 vs epoch numeric. Test-style concern; downstream consumers will pin when they parse.
- **`eventType` wire-format inconsistency between sibling speaker events** — `SpeakerInvitationSentEvent` uses `"SpeakerInvitationSent"` (no suffix) while `SpeakerResponseReceivedEvent` and the new `SpeakerPromotedToReadyEvent` use the full class name with `Event` suffix. New event matches its cited precedent; outlier is in unmodified code. Sweep cleanup belongs to a future Phase-B/D story before Phase E consumes routing.
- **PII (`email`, `username`) in domain event payload without redaction-on-log policy** [`SpeakerPromotedToReadyEvent.java:62-65`] — cross-cutting policy decision affecting all domain events; not a 11.B.1 regression.
- **JSON serialisation test uses substring-contains rather than structural assertions** [`SpeakerPromotedToReadyEventTest.java:80-87`] — project pattern across all event tests.
- **AC9 self-reference gap: allow-list does not enumerate the story file itself, yet the story file is `MM` in git status (Tasks/Subtasks + Dev Agent Record updates)** — spec template tweak, not a code defect.

## Deferred from: code review of 11-a-1-align-speaker-workflow-documentation-to-adr-009 (2026-05-15)

- **Mermaid `<br/>` inside stateDiagram-v2 edge labels may not render** [`docs/architecture/06a-workflow-state-machines.md:994-1012`] — needs visual smoke test in the docs site; older Mermaid renderers may emit `<br/>` literally inside edge labels. Cosmetic.
- **Speaker-coordination-service ownership ambiguity** — `/api/v1/speaker-portal/**` host vs. `speaker_pool` table vs. workflow ownership is not spelled out in one place across `04-api-design.md`, `04-api-speaker-coordination.md`, and `06b-user-lifecycle-sync.md` (Pattern N). Architectural question to resolve during Phase B implementation.
- **`session_speakers` narrative mixes target-tense and current-tense** [`docs/architecture/03-data-architecture.md:303`, `docs/architecture/06a-workflow-state-machines.md:1112`] — "There is no `session_speakers` junction table" reads as already-deleted, but Phase A is doc-only. Minor cosmetic; revisit when Phase B physically drops the table.
- **26 `ADR-009 §0.x` citations point to nonexistent sections** — ADR-009 has no `§0` numbered subsections (only Decisions 1, 2, 3). The story's AC text introduced the §0.1/§0.2/§0.3/§0.5/§0.7 convention to disambiguate target-model topics (states/auth/etc.), and the dev consistently used these placeholders across 26 hits. Either (a) restructure ADR-009 to add a `§0 Target Model` section with numbered subsections matching the citations, or (b) sweep-replace each citation with actual ADR-009 section references. Follow-up story under Phase A cleanup or as part of Phase B doc tidy.

## Deferred from: code review of 11-d-4-kanban-guided-drag-unified-drawer-onbehalf-content-form (2026-05-17)

- **gsw-BE locale structural divergence under `speakerContent` block** [`web-frontend/public/locales/gsw-BE/organizer.json:113-122`] — Missing `form.*` and most `errors.*` keys vs. the 9 other locales; i18n `fallbackLng: 'de'` resolves missing keys to German. Acceptable per CLAUDE.md §"Localization — Official vs Optional Languages" (de+en first-class, gsw-BE optional).
- **`useSendInvitation` called with `username: speaker.id`** [`EventSpeakersTab.tsx:182`, `SpeakerDetailDrawer.tsx:165`] — Pool entry's `.id` field is documented as the username per the hook's JSDoc (`useSpeakerPool.ts:168`). Convention is project-wide and predates 11.D.4; not a 11.D.4 regression.
- **`profilePictureUrl` write path ambiguity** [`ContentSubmissionSubView.tsx:177-181, 198-203`] — Unclear whether `uploadProfilePictureForUser` already patches `User.profile_picture_url`, or whether the form's `submitContent` body re-patches it via `UserApiClient.patchUserProfile` (per 11.C.2 contract). Likely benign double-write but not user-visible. Worth a code-path audit in a future cleanup story.
- **`presentationUploadId` field shipped dead in the UI but key present in locales** [`ContentSubmissionSubView.tsx` + locale `speakerContent.presentationUploadLabel`] — Spec AC8 step 3 explicitly allows deferring the upload UI when no organizer-side materials endpoint exists. Will be removed/wired when a future story adds the organizer-on-behalf materials endpoint.
- **`WITHDREW` / `OVERFLOW` references survive in `speakerPool.types.ts:15-16`** — Phase B residue from Story 11.B.1 (enum reduction); not introduced by 11.D.4. AC11 regression-guard fails as worded but the dev did not author these.
- **i18next nested-translation substitution not integration-tested for the invalid-drop toast** [`speakerTransitions.test.ts:4316-4322`] — Unit tests assert that keys are looked up; no integration test verifies the rendered toast text in a real i18next environment. Partially covered by `speaker-kanban-guided-drag.spec.ts:358-365` Playwright spec. A follow-up could add a unit-level test with the real i18next instance + minimal resource bundle to catch substitution regressions cheaper.
- **Cognito provisioning race during portrait upload for newly-created speakers** [`ContentSubmissionSubView.tsx:177-181`] — Race window during Story 11.E.2 rollout: READY-hook provisions Cognito; the admin presigned-URL endpoint may 404 in the gap between user-record creation and Cognito-account creation. Out-of-scope for 11.D.4; the upload helper at most surfaces a generic error which the organizer can retry. Owner: Story 11.E.2.
- **`UnifiedHistoryPanel.outreachQuery` always-on adds N+1 traffic for speakers without outreach** [`UnifiedHistoryPanel.tsx:2942`] — Endpoint returns 200 [] cheaply; performance only.
- **History panel `changedAt` truthiness filter accepts malformed timestamps** [`UnifiedHistoryPanel.tsx:2948-2950`] — Data-quality dependent; `new Date(malformed).getTime() === NaN` sorts to position 0 (newest). Backend currently guarantees ISO-8601 strings; concern only surfaces if backend contract drifts.

## Deferred from: code review of 11-e-3-speaker-portal-cognito-auth-frontend-session-multi-role-nav (2026-05-18)

- **Five legacy speaker-portal `*IntegrationTest` classes marked `@Disabled`** — `SpeakerPortalResponseControllerIntegrationTest`, `SpeakerPortalContentControllerIntegrationTest`, `SpeakerPortalDashboardControllerIntegrationTest`, `SpeakerPortalMaterialsIntegrationTest`, `SpeakerPortalTokenControllerIntegrationTest`. Phase F (Story 11.F.1) deletes the disabled files alongside the magic-link teardown. New `SpeakerPortalAuthIntegrationTest` covers the auth matrix; behavioural-state coverage (idempotency, validation errors, conflict states, a11y) is the trade-off documented in Completion Note #13.
- **Four frontend Vitest suites + `speakerPortalService.test.ts` marked `describe.skip`** — `ContentSubmissionPage.test.tsx`, `InvitationResponsePage.test.tsx`, `ProfileUpdatePage.test.tsx`, `SpeakerMagicLoginPage.test.tsx`, plus the service-level test. Phase F deletes; rewrite postponed per Completion Notes #26-27.
- **Three Playwright specs `test.fixme` pending test-speaker seed** [`e2e/speaker/speaker-portal-respond.spec.ts`, `speaker-portal-content-submit.spec.ts`, `speaker-portal-cross-portal-nav.spec.ts`]. Awaiting Story 11.E.2 provisioning a `SPEAKER_AUTH_TOKEN`-yielding test user in staging Cognito. Follow-up dev pass once the seed lands.
- **Native-speaker review of fr/it/rm/es/fi/nl/ja/gsw-BE nav-section translations** — `Relatur` (rm), `Ponente` (es), `Puhuja` (fi), `Spreker` (nl), `講演者` (ja), `Referänt` (gsw-BE). Non-blocking; cosmetic accuracy follow-up.
- **`docs/prd/epic-6-speaker-onboarding-plan.md` retains un-annotated current-behavior magic-link claims (7 lines)** — legacy doc; out-of-scope per Completion Note #36. Add supersession banner in a follow-up doc-sweep.
- **Belt-and-suspenders role check inside `SpeakerResponseService.processResponse`** — service trusts the actor; controller-layer `@PreAuthorize` covers all current callers. Add only if the service is reused outside speaker-portal context.
