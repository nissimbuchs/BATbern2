# Deferred Work

> **Groomed 2026-06-07** — resolved items removed (listed at bottom under "Resolved (removed 2026-06-07)"),
> open items clustered into 4 backlog candidates (A–D). Each cluster is sized to become a backlog story.
> Item text is preserved verbatim (trimmed) with original `file:line` references and `[Edge]`/`[Blind]`/`[Auditor]` attributions.
> `(verify)` markers flag items that could not be confidently confirmed open during grooming.

---

## Cluster A — Auth/SSO hardening (REQUIRED before Apple/generic OIDC, Story 12.10)

These harden the federated-identity surface. Most are low-risk *while Google is the only IdP* (Google always asserts a verified email and the same `sub`), but become exploitable or noisy once a second IdP (Apple/generic OIDC) lands. **Treat this cluster as the precondition for Story 12.10.**

- **Primary-email federated linking has no `email_verified` gate** [`infrastructure/lib/lambda/triggers/pre-signup.ts:128-134`] — `pre-signup.ts` links a federated identity to a matching `user_profiles.email` WITHOUT checking the IdP's `email_verified` attribute (pre-existing Epic 12 behavior; the new verified-additional-email fallback IS gated). Low risk while Google is the only IdP (always verified); MUST be hardened before Apple/generic OIDC lands — an IdP asserting an arbitrary unverified email could link into a victim's account.
- **`AdminLinkProviderForUser` "already linked" not distinguished from real failures** [`pre-signup.ts:131-147, 212-219`] — a concurrent sign-in/retry throws `InvalidParameterException` and pollutes the `PreSignUpFailure` metric. `post-authentication.ts:105` already special-cases this; mirror that in `pre-signup.ts` for both linking paths. Two simultaneous first Google sign-ins for the same email both call link; the second throws already-linked, caught by the federated try/catch (no 503) but emits a misleading `PreSignUpFailure`. No idempotency guard. Low blast radius; revisit if the metric shows noise. [Edge]
- **Canonical-email restore lags one sign-in (`event.userName`-without-underscore fallback hardcodes provider `'Google'` + uses the whole userName as the subject)** [`pre-signup.ts:251-255, 268-270`] — real `PreSignUp_ExternalProvider` events are always `Google_<sub>` so the happy path is correct; the fallback is defensive-only and will mis-attribute the provider once Apple/another IdP lands (Phase 6). **Preferred fix: a PreTokenGen-restore variant** rather than relying on this PreSignUp fallback. Revisit when a second IdP is added. [Blind + Edge]
- **SES IAM grant includes `identity/*` wildcard** [`infrastructure/lib/stacks/partner-coordination-stack.ts:99-103` (origin of the pattern), `infrastructure/lib/stacks/company-management-stack.ts:182-186`] — both stacks carry a third resource ARN `identity/*` that negates the scoped domain ARNs. Tighten both stacks together after verifying which identity ARNs SES sends actually require (test in staging; sending uses From `noreply@batbern.ch` → `identity/batbern.ch` should suffice).
- **No cooldown/rate-limit on `POST /me/additional-emails/{email}/resend-verification`** [`services/company-user-management-service/.../controller/UserController.java:594`] — authenticated user can spam SES sends to their registered addresses. Add a per-row 60s cooldown (compare token issue time or a lightweight in-memory Caffeine cache) if abuse is observed.
- **`cognito-idp:ListUsers` granted but never called** [`infrastructure/lib/constructs/cognito-user-sync-triggers.ts` IAM grant; `infrastructure/lib/lambda/triggers/pre-signup.ts`] — spec-mandated (AC4 requires the grant; AC3 names it as a "MAY-use" destination-resolution fallback) but the handler resolves the destination solely from the DB row, so it is currently dead privilege, in mild tension with AC4's "least-privilege" framing. Implement the `ListUsers` fallback (would also address the anonymous-row + username==sub items) or drop the grant. [Edge + Auditor]
- **Destination correctness relies on the undocumented-in-AWS coupling "native Cognito username == sub"** [`pre-signup.ts:134-138`] — holds for this email-alias pool (Cognito-generated UUID usernames equal their sub) but admin-created/historically-imported rows could diverge, with no fallback (the granted `ListUsers` would be the natural one). Code matches the spec's literal instruction. [Auditor]
- **Turnstile fail-open-on-missing-token needs an explicit test + threat-model comment** (NEW — v1.1.0 release-PR bot review) — the Turnstile check fails open when no token is present; rate limiting is the compensating control. Add an explicit test asserting the fail-open behaviour and a threat-model comment documenting that rate limiting is the intended compensating control.
- **Verification-link replay-idempotency: documented as accepted design** (NEW — v1.1.0 release-PR bot review) — verification tokens are NOT single-use; replaying a verification link grants nothing additional. Accepted by design; capture the rationale as a code/threat-model comment so a future reviewer does not re-flag it.

## Cluster B — Cognito↔DB consistency / outbox

The recurring failure mode: a Cognito mutation succeeds, then the surrounding JPA transaction or an async step fails, leaving Cognito and Postgres divergent (orphan user, drifted username, locked-out speaker). The structural fix is an outbox / AFTER_COMMIT pattern (already spun out as Story 11.F.2 for the invitation path) plus a reconciliation job.

- **`runInvitedHook` performs Cognito mutation synchronously inside a JPA `@Transactional`** [`services/event-management-service/.../service/SpeakerWorkflowService.java:296-315`] — if any later step in the transaction throws (statusHistory write, eventPublisher listener), the DB rolls back but Cognito has already been mutated (password rotated, possibly never communicated). The AFTER_COMMIT TransactionalEventListener pattern is the right fix; code comment already flags the email-send variant but not the credential-issuance variant. [Blind #1 + Edge #9] — *NOTE: spun out as Story 11.F.2 per RD2 (2026-05-25); `SpeakerInvitedEvent` + `SpeakerAcceptedEvent` move Cognito mutation + email send to AFTER_COMMIT listeners. Tracked in sprint-status.yaml backlog.* — CONFIRMED still open 2026-06-07: `SpeakerWorkflowService.java:517,552` still carries the AFTER_COMMIT deferral comments; 11.F.2 has not landed.
- **AdminCreateUser succeeds → JPA commit fails on the same transaction → orphan Cognito user** [`services/company-user-management-service/.../UserService.java:710-747`] — Sequence: `createNewUser` (persists User) → `roleService.addRole` → `cognitoService.adminCreateUserSilently` SUCCEEDS → commit FAILS (deadlock, connection drop). Transaction rolls back; Cognito user remains. Next retry generates a different username; Cognito's `preferred_username` attribute now drifts from Postgres. Speaker logs in successfully via Cognito but `getCurrentUser` throws UserNotFoundException. Requires either an outbox pattern or a reconciliation job. [Edge #2]
- **AdminSetUserPassword succeeds → `@Async` email send throws → speaker locked out** [`services/event-management-service/.../SpeakerInvitationEmailService.java:113-117`] — Async failures are swallowed at the catch-all; the workflow transition still commits (speaker → INVITED). The new Cognito temp password is in nobody's hands. Operator must manually retry (the issue-invitation-credentials endpoint IS idempotent — but operators don't know it failed). Needs persistent retry queue or pre-send commit gate. [Edge #3]
- **`UsernameExistsException` swallow doesn't distinguish race from stale state** [`services/company-user-management-service/.../CognitoIntegrationServiceImpl.java:937-938`] — Swallowing INFO-logs as "idempotent no-op". If Cognito user is `ARCHIVED` (or otherwise unhealthy), the silent-swallow at provision time hides the problem; the issueInvitationCredentials call at INVITED then throws 422 — too late, the BATbern User row already exists. Detect at provision time via a follow-up `AdminGetUser` status check. [Blind #22]
- **Cognito `username` vs `preferred_username` drift on email change** [`services/company-user-management-service/.../CognitoIntegrationServiceImpl.java:920-932`] — `AdminCreateUser` uses email as the Cognito username; later calls use email again as the lookup key. If the speaker changes their email in CUMS post-provision, the Cognito record stays keyed by the old email → `getUserStatus(user.getEmail())` throws UserNotFoundException. Pre-existing Cognito-design pitfall. [Blind #16]
- **`UserReconciliationService.createMissingUser` `@Transactional` is a no-op (private + self-invoked)** [`services/company-user-management-service/.../service/UserReconciliationService.java:307-308`] — Spring AOP ignores `@Transactional` on private and self-invoked methods; the live tx is the outer `reconcileUsers()` (`:95`), so per-user `save()` (`:356`) flushes only at the batch commit → no per-user isolation. One over-length `pref_language` rolls back the entire nightly reconciliation batch and over-reports successes. Fix: make `createMissingUser` package-private + move to a separate bean (or use `TransactionTemplate`/`REQUIRES_NEW` per user) so each user commits independently. [Edge Case]
- **Throwaway temp password "discard" is theatrical + AWS SDK exception PII risk** [`services/company-user-management-service/.../UserService.java:1118-1124`] — `String throwawayTempPassword = null;` reassignment doesn't zero memory. More importantly, if `AdminCreateUser` fails with a validation error, some SDK error paths include the request body in serialized exception messages. `CognitoOperationException` wraps the cause and is logged. Needs an AWS SDK v2 exception-message audit to confirm passwords never appear. [Blind #3]
- **Plain-text temp password in service-to-service JSON; `RestTemplate` DEBUG logging risk** [`services/event-management-service/.../UserApiClientImpl.java:1782-1784`] — The response body contains the fresh temp password. Spring `RestTemplate` at DEBUG level dumps response bodies. Verify staging/prod log configs explicitly suppress this, or wrap the response in a redacting interceptor. [Blind #8]

## Cluster C — i18n & date handling sweep

A coherent locale/date-correctness sweep: `i18n.language === 'de'` strict-equality misses Swiss/regional variants, `date-fns` distance formatting is de/enUS-only, and several date arithmetic helpers ignore DST/calendar-day boundaries.

- **`i18n.language === 'de'` covers only 2 of 10 supported locales for `date-fns` time-distance formatting** [`web-frontend/src/components/organizer/SpeakerStatus/SpeakerStatusLanes.tsx:516`] — Non-de users see English distance strings inside otherwise-translated kanban. Project-wide concern; faithful to the existing `TeamActivityFeed.tsx` pattern.
- **`i18n.language === 'de'` strict equality misses `de-CH`/`de-DE`/`gsw-BE`** [`SpeakerStatusLanes.tsx:516`] — Swiss-German users fall to enUS rather than the German `date-fns` locale. Same root cause as above.
- **`gsw-BE` locale never had `speakers.tentative*` keys to remove** [`web-frontend/public/locales/gsw-BE/organizer.json`] — pre-existing locale gap; the "removed from all 10 locales" sweep was vacuously satisfied for gsw-BE. Investigate whether other namespaces also lag for gsw-BE.
- **gsw-BE plural grammar — single-form `überfellig` for both 1 and many** [`gsw-BE/organizer.json`] — same family as the wider i18next plural-variant question; tackle alongside the native-speaker review pass on machine-translated baselines.
- **gsw-BE locale structural divergence under `speakerContent` block** [`web-frontend/public/locales/gsw-BE/organizer.json:113-122`] — Missing `form.*` and most `errors.*` keys vs. the 9 other locales; i18n `fallbackLng: 'de'` resolves missing keys to German. Acceptable per CLAUDE.md localization rule (de+en first-class).
- **Native-speaker review of fr/it/rm/es/fi/nl/ja/gsw-BE nav-section translations** — `Relatur` (rm), `Ponente` (es), `Puhuja` (fi), `Spreker` (nl), `講演者` (ja), `Referänt` (gsw-BE). Non-blocking; cosmetic accuracy follow-up.
- **`MS_PER_DAY = 86_400_000` ignores DST/leap-second days — `diffDays` off-by-one on transition days** [`web-frontend/src/components/organizer/SpeakerStatus/kanbanThresholds.ts:51`] — replace with `date-fns` `differenceInCalendarDays` when next touched.
- **INVITED 1-hour boundary at deadline moment — same root cause as date-only ISO parsing as UTC midnight** [`kanbanThresholds.ts:111-115`] — bundle with the date-only TZ parsing fix.
- **`Math.floor` asymmetry in `diffDays(later, earlier)` vs `-diffDays(earlier, later)` at sub-day deltas** [`kanbanThresholds.ts:51`] — semantically debatable; document in JSDoc when the date-only parsing rework lands.
- **`resolveLocale` accepts arbitrary garbage** [`services/event-management-service/.../SpeakerWorkflowService.java:355-365`] — `Locale.forLanguageTag("xyz123")` silently returns an empty-language locale; email template falls back to service default. Pure observability concern; not user-facing.
- **History panel `changedAt` truthiness filter accepts malformed timestamps** [`web-frontend/src/components/.../UnifiedHistoryPanel.tsx:2948-2950`] — `new Date(malformed).getTime() === NaN` sorts to position 0 (newest). Backend currently guarantees ISO-8601 strings; concern only surfaces if backend contract drifts.

## Cluster D — test & UX papercuts

Everything else: test-hardening, kanban/modal UX papercuts, sprint-status YAML hygiene, Bruno test restoration questions, the AC19 activity-history table, doc nits, and the new Core Web Vitals monitoring gap.

### Performance monitoring (NEW)
- **Core Web Vitals regression monitoring for the homepage perf work** (NEW — v1.1.0 release-PR bot review) — the homepage PageSpeed improvement (25 → 80+) has NO automated guard. A future bundle/loading regression would silently erode it. Add a CI Lighthouse/PSI budget or Web Vitals check on the homepage (likely against the beta canary).

### Test hardening
- **Integration-test event-count assertion asymmetry** [`services/company-user-management-service/.../integration/JITProvisioningIntegrationTest.java`] — link test counts **all** `UserCreatedEvent`s unfiltered and asserts zero — stricter than its stated intent. Harden by adding the same `source=='JIT_PROVISIONING'` filter. [Blind]
- **Identity-rebind test seeds CONTACTED speaker with non-null username — artificial invariant violation** [`services/event-management-service/.../SpeakerWorkflowServiceTest.java:1297-1298`] — Production should never produce a CONTACTED speaker with a username. Either justify with a comment or seed READY state for the guard test. [Blind]
- **Identity-rebind guard reject-path not exercised by tests** [`services/event-management-service/.../SpeakerWorkflowServiceTest.java`] — `provisionUserWithRole` mock always returns the same username, so the rebind guard's reject path is never exercised. Add a test stubbing a divergent return and asserting `ValidationException`.
- **`InOrder` block does not assert `applicationEventPublisher.publishEvent(...)` ordering** [`services/event-management-service/.../SpeakerWorkflowServiceTest.java`] — `applicationEventPublisher` is in the InOrder group but never verified. Would catch a regression where the event publish moves before the DB save.
- **`saturate = 24` magic constant in slot-capacity integration test** [`services/event-management-service/.../SpeakerInvitationControllerIntegrationTest.java:604-617`] — silently stops saturating if any event-type maxSlots is bumped to ≥ 24. Replace with `eventTypeService.getEventType(...).getMaxSlots()` read when next touched.
- **MUI testid + `user.click(field); user.tab()` fragility in PromoteSpeakerDialog tests** [`web-frontend/src/components/SpeakerBrainstormingPanel/PromoteSpeakerDialog.test.tsx:1571-1574, 1584-1587`] — MUI `data-testid` lands on the outer wrapper, not the inner `input`; fragile under future MUI bumps. Use `getByRole('textbox', { name: /first name/i })` or pass testid via `inputProps`. [Blind]
- **`mode: 'onTouched'` + react-hook-form `isValid` initial state may not gate submit-disabled until first blur** [`PromoteSpeakerDialog.tsx:107-118`] — `isValid` may initially be `true`. Tests pass today (defaults pre-fill); set `mode: 'all'` or explicitly check non-empty in the `disabled` prop. [Blind]
- **i18next nested-translation substitution not integration-tested for the invalid-drop toast** [`web-frontend/src/.../speakerTransitions.test.ts:4316-4322`] — no integration test verifies the rendered toast text in a real i18next environment. Partially covered by Playwright. A unit-level test with the real i18next instance would catch substitution regressions cheaper.
- **Three Playwright specs `test.fixme` pending test-speaker seed** [`e2e/speaker/speaker-portal-respond.spec.ts`, `speaker-portal-content-submit.spec.ts`, `speaker-portal-cross-portal-nav.spec.ts`] — awaiting a staging-Cognito test-speaker seed + `SPEAKER_AUTH_TOKEN` GitHub secret. Tracked as an ops ticket (Story 11.F.1 RD4).
- **`speaker_jwt` Playwright cookie audit `test.skip()`s when `SPEAKER_AUTH_TOKEN` unset** [`web-frontend/e2e/speaker/magic-link-teardown.spec.ts`] — same ops dependency as above; regression net dormant until the test-speaker seed lands.

### Bruno test restoration questions
- **5 Bruno tests deleted vs. AC3's authorized 1** [`bruno-tests/speaker-portal-api/12-setup-tentative-speaker.bru`, `13-send-tentative-invitation.bru`, `13b-get-tentative-test-token.bru`, `15-tentative-can-change-to-accept.bru`] — tests 12 (`POST /speakers/invite` smoke) and 13 (invitation-send + INVITED-state assertion) had standalone coverage value beyond TENTATIVE. Verify whether equivalent assertions live in `bruno-tests/events-api/`; if not, restore those two under non-tentative names. [Blind]
- **AC2 regression test missing coverage for deleted `/api/v1/e2e-test/**` endpoints** [`MagicLinkEndpointsRemovedIntegrationTest.java`] — extending the regression net (one extra anonymous + authenticated test for `/api/v1/e2e-test/tokens/generate-e2e-set`) would prevent silent reintroduction of `E2ETestTokenController`. Nice-to-have.
- **Test coverage regression: 4 new Cognito-side Vitest replacement files have fewer assertions than the deleted suites** [`ContentSubmissionPage.test.tsx`, `InvitationResponsePage.test.tsx`, `ProfileUpdatePage.test.tsx`, `speakerPortalService.test.ts`] — RD5 promised replacements, not parity. Validation paths, error rendering, and presigned-upload flow are thinly covered. Track if Vitest report drops below RD5's 80% threshold.

### Kanban / modal / UX papercuts
- **`splitFullName("Mononym")` pre-fills empty lastName, no visible error on first render** [`PromoteSpeakerDialog.tsx:46-55, 107-110`] — single-word `speakerName` produces `firstName: "Madonna", lastName: ""`. With `mode: 'onTouched'`, no helper text fires; submit is correctly disabled but organizer sees no reason. Trigger initial validation or force re-entry when single-word. [Edge Case]
- **Whitespace/punctuation-only names pass client validation, then trim to empty** [`web-frontend/src/components/auth/RegistrationStep1/RegistrationStep1.tsx:97-159`, `web-frontend/src/hooks/useRegistration/useRegistration.ts:31-32`] — `"  "`/`"--"`/`"''"` pass `required`+`minLength:2`+`pattern`, then `.trim()` to `''`. Pre-existing (same rules as the old single `fullName` field). Cheap hardening: `validate: v => v.trim().length >= 2` per field. Backend JIT blank-name fallback bounds the impact. [Edge + Blind]
- **`?tab=<non-numeric>` deep link yields `Number()=NaN`** [`web-frontend/src/pages/organizer/EventManagementAdminPage.tsx:64`] — no tab content renders and (round 2) the mobile BottomNavigation selects nothing. Guard: `Number.isFinite(n) ? clamp(n,0,8) : 0`.
- **Stale modal state when speaker mutated mid-modal** [`EventSpeakersTab.tsx:763-770`] — `outreachModalState.speaker`/`promoteModalState.speaker` are snapshot copies; a 30s background refetch is invisible to the open modal. Requires modal-state-ownership redesign (subscribe to live speaker via React Query).
- **Snackbar state is per-card** [`SpeakerStatusLanes.tsx` SpeakerCard local state] — clicking many Send-invitation buttons quickly fires conflicting/hidden toasts. Hoist to a global snackbar provider.
- **Snackbar "Invitation sent" toast can be lost on subsequent refetch re-render** [`SpeakerStatusLanes.tsx:501-503`] — invalidation re-renders the card; per-card snackbar local state may not survive. Resolve with the snackbar-hoist refactor.
- **`SpeakerStatusDashboard` mounts `SpeakerStatusLanes` without the new callbacks** [`SpeakerStatusDashboard.tsx:181`] — `??` no-op defaults silently swallow clicks. Not on any production route today; latent if re-wired.
- **`STATUS_LANES` does not catch-all legacy union members `SLOT_ASSIGNED`, `WITHDREW`, `OVERFLOW`** [`speakerPool.types.ts` `SpeakerWorkflowState` union, `SpeakerStatusLanes.tsx:91-100`] — speakers stuck in those states on brownfield events disappear from the kanban. Bundle with the wider `SpeakerWorkflowState` union cleanup.
- **`WITHDREW` / `OVERFLOW` references survive in `speakerPool.types.ts:15-16`** — Phase B residue from enum reduction; not a regression.
- **`notification.ts` NotificationType still recognises legacy `SLOT_ASSIGNED`/`OVERFLOW`** [`web-frontend/src/types/notification.ts`, `.../TeamActivityFeed.tsx`] — confirm whether backend still emits these notification types after Phase B enum reduction. [Blind]
- **`getStatusChangedAt` `updatedAt` fallback inflates time-in-state on note/organizer edits** [`kanbanThresholds.ts:79-93`] — backend `status_changed_at` column is the proper fix.
- **`slotCapacity.reached` derived from in-memory `speakers` array — breaks silently if the kanban query ever paginates** [`SpeakerStatusLanes.tsx:333-345`] — switch to a server-side count endpoint if pagination lands.
- **INVITED with `responseDeadline === undefined` silently normal — no UI signal the workflow has stalled** [`kanbanThresholds.ts:111-113`] — address with a dedicated "INVITED without deadline" warning if organisers report it.
- **INVITED `joiner` i18n key has no defensive code fallback** [`SpeakerStatusLanes.tsx:201`] — missing-key would render the literal key path. Defensive `?? ' · '` would protect against locale-contributor mistakes.
- **QUALITY_REVIEWED chip flicker while event detail is loading** [`SpeakerStatusLanes.tsx:843-852`] — JSDoc shipped; a skeleton-chip during loading would be cleaner but needs a design call.
- **`slotCapacity` `useMemo` recomputes on every `speakers` array reference change** [`SpeakerStatusLanes.tsx:165`] — React Query returns fresh arrays every refetch, defeating downstream memoization. Performance nit.
- **Empty 4px organizer-row Box when both `assignedOrg` and `timeInState` are falsy** [`SpeakerStatusLanes.tsx` organizer-row rendering] — cosmetic edge; harmless.
- **`UnifiedHistoryPanel.outreachQuery` always-on adds N+1 traffic for speakers without outreach** [`UnifiedHistoryPanel.tsx:2942`] — returns 200 [] cheaply; performance only.
- **`presentationUploadId` field shipped dead in the UI but key present in locales** [`ContentSubmissionSubView.tsx` + locale `speakerContent.presentationUploadLabel`] — deferral allowed per spec; wired when an organizer-on-behalf materials endpoint exists.
- **`profilePictureUrl` write path ambiguity** [`ContentSubmissionSubView.tsx:177-181, 198-203`] — likely benign double-write (upload helper + form re-patch). Worth a code-path audit.
- **Cognito provisioning race during portrait upload for newly-created speakers** [`ContentSubmissionSubView.tsx:177-181`] — admin presigned-URL endpoint may 404 in the gap between user-record creation and Cognito-account creation. Owner: Story 11.E.2; upload helper surfaces a generic retriable error.

### Company / profile contract gaps
- **(Medium) Company association can never be cleared via /profile** — `ProfilePage.handleSubmit` sends `companyId: companyId || undefined`; empty string collapses to `undefined` and CUMS `UserService.updateCurrentUser` only writes `companyId` when non-null. Once set, a company cannot be removed via the profile form. Needs an explicit-null/clear contract in `UpdateUserRequest` + backend handling.
- **(Low) Latent /profile↔/dashboard redirect loop for a roleless authenticated user** — consent gate (`termsAcceptedAt === null`) redirects to `/profile`; role check bounces an empty-roles user to `/dashboard`; the gate fires again. Unreachable today (JIT + post-confirmation Lambda always assign ATTENDEE). Guard if roleless states become producible.
- **401 from `GET /users/me` during init/sign-in hydration redirects to `/login`** [`web-frontend/src/contexts/AuthContext.tsx` hydrate calls + `web-frontend/src/services/api/apiClient.ts` global 401 interceptor] — the global interceptor fires `navigateCallback('/login')` on any 401 before `hydrateUserFromDb`'s catch can swallow it, so a transient 401 from `/users/me` bounces a valid session. Pre-existing; 12.1 widened the blast radius. Fix: scope the 401 redirect to exclude the hydration call (or treat `/users/me` 401 as soft failure). Low priority — no confirmed staging repro.

### AC19 activity-history table
- **AC19 — persistent `user_activity` row for `ADDITIONAL_EMAIL_ADDED` / `ADDITIONAL_EMAIL_REMOVED`** [`services/company-user-management-service/.../service/UserService.java:1382-1407`] — `ActivityHistoryEntity` does not exist in this service yet. Story 10.32 ships CloudWatch-only audit logging (masked via `LoggingUtils.maskEmail`). Follow-up: add a minimal `activity_history` table + JPA entity, then revisit OQ#6. [Auditor]

### Backend / structural papercuts
- **CUMS `provisionUserWithRole` retains `"Speaker"`/`"Unknown"` fallback** [`services/company-user-management-service/.../service/UserService.java:677-696`] — CUMS derives placeholder names from email local-part when firstName/lastName arrive null. No risk today (only EMS-promote with `@NotBlank` reaches it), but a future internal caller could regress silently. Tighten the OpenAPI `ProvisionUserRequest` to required firstName+lastName, or document the AC4 contract + flag as defense-in-depth. [Edge Case]
- **`lastNameFallback` returns empty string for single-word speaker names** [`services/event-management-service/.../service/SpeakerWorkflowService.java:471-478`] — single-word `speakerName` creates a User with `lastName=""`. Whether `provisionUserWithRole` enforces non-blank lastName is unverified. Address alongside the placeholder-name fallback above.
- **`firstNameFallback` / `lastNameFallback` literals "Speaker" / "Unknown"** [`SpeakerWorkflowService.java:367-379`] — when payload omits name and `speakerName` is blank, the new User has `firstName=Speaker, lastName=Unknown`, which become Cognito attributes. Decide whether to reject at the seam, require name on the payload, or accept the placeholder.
- **Email format validation gap in `provisionUserWithRole`** [`services/company-user-management-service/.../UserService.java:680-696`] — validation is `isBlank()` only. "weird-id" (no `@`) derives `firstName="Weird" lastName="Id"` and passes to Cognito as username. Pre-existing input-validation gap. [Edge #11]
- **`EXTERNAL_PROVIDER` Cognito status maps to 422 (Unprocessable)** [`services/company-user-management-service/.../UserService.java:797-830`] — the switch falls `EXTERNAL_PROVIDER` (and future values) to `default` → 422. Revisit if Cognito SAML/Google IdP status surfaces here. [Edge #4]
- **Duplicated `extractLanguageFromPreferences` across interceptor + reconciliation** [`JITUserProvisioningInterceptor.java:223` / `UserReconciliationService.java:399`] — byte-parallel parse logic; reintroduces drift risk (AC7 "keep in sync"). Candidate: a shared `CustomPreferencesParser` util in CUMS. [Edge Case + Blind]
- **Watch services lose batch user lookup → N+1 HTTP loop** [`services/event-management-service/.../watch/WatchEventController.java`] — `mapToActiveEventDetail` uses per-record `userApiClient.getUserByUsername(...)`. Accepted per Open Q4 for 5-10 speakers/event. Revisit if smoke shows >500ms latency.
- **No `UserApiClient` batch path added** [`services/event-management-service/.../service/UserApiClient.java`] — Open Q4 accepted per-record. Listed so a future N-record fan-out screen doesn't repeat the loop without revisiting.
- **`UserApiClientImpl.provisionUserWithRole` does not error on HTTP 200 with null body** [`UserApiClientImpl.java:1378-1391`] — null-body checks log only; null returned to callers. CUMS never returns null and OpenAPI enforces non-null. Address only if observed in production.
- **Async `SpeakerContentSubmittedEvent` listener failure may drop organizer notification** [`ContentSubmissionService.java:2348`] — standard async event-publisher concern. Confirm retry/DLQ semantics in a separate observability story.
- **`runDeclinedHook` deletes session without checking co-presenters** [`SpeakerWorkflowService.java:268-275`] — `sessionRepository.deleteById(sessionId)` runs unconditionally; panel sessions (multiple `SpeakerPool` rows sharing a `sessionId`) end up with orphaned FKs. At minimum `findBySessionId(sessionId).size() > 1 ? clearLink : delete`.
- **Slot-capacity TOCTOU race + concurrent `sendInvitation` race** [`SpeakerWorkflowService.java:201-209`, `SpeakerInvitationService.java:165-217`] — `enforceSlotCapacity` reads counts without a row/event lock and `SpeakerPool` has no `@Version`. Accepted by PM RD #6. Future story: `@Lock(PESSIMISTIC_WRITE)` on `findById` inside `transition()` + a uniqueness constraint or `@Version`.
- **`enforceSlotCapacity` N+1 on batch invites** [`SpeakerWorkflowService.java:201-209`] — `3N` round-trips for batch-invite. Cache per `event.eventType` per request or use a single aggregate query if batch-invite becomes hot.
- **Same-instant `Instant.now()` rows in `speaker_status_history` may share `changed_at`** [`SpeakerWorkflowService.java:380`] — collisions rare; add a tiebreak column or `INSERT ... RETURNING` ordering only if flakiness emerges.
- **SPEAKER self-patch fails 403 on case-mismatched username in URL** [`UserController.java:723`] — `String.equals` is case-sensitive; only happens with mistyped URLs. Revisit if a Cognito-JWT username-casing issue surfaces.
- **`GlobalExceptionHandler` `NoResourceFoundException` widens 404 mapping to static-resource misses across all of EMS** [`GlobalExceptionHandler.java:1100-1103`] — clients relying on prior empty 404 body now get `ErrorResponse` JSON. Needs a verification sweep across EMS error consumers.
- **Orphan ShedLock row for deleted `tokenCleanup` job** — `TokenCleanupScheduler` deletion didn't clean its ShedLock row. Cosmetic; `DELETE FROM shedlock WHERE name = 'tokenCleanup'`.
- **READY rejection fires before log statement** [`SpeakerStatusController.java`] — `READY_REQUIRES_PROMOTE_ENDPOINT` check throws before `log.info`, so READY requests are never logged. Observability gap.
- **`isSlotAssigned` over-reporting in fallback path (`patchEntry`/`assignSpeakerToOrganizer`)** [`SpeakerPoolResponse.java`, `SpeakerPoolService.java`] — when `SpeakerPool.sessionId != null` but the session has no `startTime`, the fallback returns `isSlotAssigned: true` while `countPublishableByEventId` excludes it. Fix requires session-loading in those call sites.

### Validation / domain-event policy
- **No empty-string / whitespace / very-long-value validation for required `String` fields in `SpeakerPromotedToReadyEvent`** [`shared-kernel/.../events/SpeakerPromotedToReadyEvent.java:73-104`] — only null is rejected; empty username/email could reach the Cognito mailer. Cross-cutting domain-event validation policy.
- **`Instant.MIN`/`MAX`/stale-date values accepted unchecked for `promotedAt`** [`SpeakerPromotedToReadyEvent.java:102`] — no time-range guard. No sibling event has one.
- **PII (`email`, `username`) in domain event payload without redaction-on-log policy** [`SpeakerPromotedToReadyEvent.java:62-65`] — cross-cutting policy decision affecting all domain events.
- **`maxLength` asymmetry — firstName/lastName=100 vs email=320; silent UI truncation via `inputProps.maxLength`** [`PromoteSpeakerRequest.java`, `PromoteSpeakerDialog.tsx`] — `inputProps.maxLength={100}` silently caps input; API callers get a generic 400. Bumping to 255 is a spec change; revisit when validation conventions are reviewed.
- **`usePromoteSpeakerToReady` invalidates `speakerStatusSummary` with a hardcoded query-key string** [`web-frontend/src/hooks/useSpeakerPool.ts:138-140`] — a `speakerStatusSummaryKeys` factory would prevent silent breakage. Bundle with a TanStack-Query-keys refactor.

### Sprint-status / YAML hygiene
- **Sprint-status YAML comment field has become unmaintainable** [`_bmad-output/implementation-artifacts/sprint-status.yaml:540`] — multi-paragraph `# ...` trailing comments per story; `last_updated:` carries stacked narratives. Reduce to one short sentence per story; truncate `last_updated:` to current update only. [Blind]

### Doc nits
- **Epic 11 PRD §"Phase F" has no section for the spun-out Story 11.F.2** [`docs/prd/epic-11-speaker-workflow-refactor.md` §F + Coverage Map] — whoever runs `/bmad-create-story 11-f-2` updates §F + the Coverage Map. sprint-status.yaml already lists 11-f-2 in backlog. [Doc-drift, low]
- **AC1 sweep clause has 24 narrative-comment hits in EMS main java (Javadoc only)** — AC4 has an explicit narrative-comment exemption; AC1 doesn't. Either copy AC4's exemption into AC1, or sweep the 24 comments to past-tense `formerly...`.
- **OpenAPI changes are narrative-prose only — magic-link DTOs were never in the specs** [`docs/api/events-api.openapi.yml`, `speakers-api.openapi.yml`] — reveals the magic-link surface was undocumented throughout its lifetime. Worth a separate OpenAPI-curation pass.
- **`submit-content-rejects-unknown-fields.bru` deletion not enumerated in AC7 file list** — verified deleted; worth a one-line dev-agent-record note next time AC7 enumerates a Bruno sweep.
- **V104 `CASCADE` rationale prose is technically wrong** [`V104__drop_speaker_invitation_tokens_table.sql` header] — comment claims CASCADE handles "view or index added since V43"; indexes drop with the table regardless, so CASCADE scopes to FK dependents + views. Comment-prose nit.
- **V104 has no Flyway undo migration (U104)** — consistent with EMS convention; BATbern has never used Flyway undo. Pre-existing.
- **Mixed-fleet rolling deploy risk: old pods may INSERT into `speaker_invitation_tokens` while V104 DROPs it** — deploy-time concern; needs deploy guidance, not a code change. RD1 accepted this risk class. Same class: `SpeakerInvitationToken` JPA entity deleted in the same release as V104 (Hibernate validate semantics during mixed-fleet boot undefined).
- **Mermaid `<br/>` inside stateDiagram-v2 edge labels may not render** [`docs/architecture/06a-workflow-state-machines.md:994-1012`] — needs a visual smoke test; older Mermaid renderers may emit `<br/>` literally. Cosmetic.
- **Speaker-coordination-service ownership ambiguity** — `/api/v1/speaker-portal/**` host vs. `speaker_pool` table vs. workflow ownership not spelled out in one place across `04-api-design.md`, `04-api-speaker-coordination.md`, `06b-user-lifecycle-sync.md`. Architectural question.
- **`session_speakers` narrative mixes target-tense and current-tense** [`docs/architecture/03-data-architecture.md:303`, `06a-workflow-state-machines.md:1112`] — "There is no `session_speakers` junction table" reads as already-deleted. Cosmetic.
- **26 `ADR-009 §0.x` citations point to nonexistent sections** — ADR-009 has no `§0` numbered subsections. Either restructure ADR-009 to add a `§0 Target Model` section, or sweep-replace each citation with actual references.
- **Bruno test `51-speaker-workflow-invalid-transition.bru:41-44` comment pins 422 mapping not in ADR-009** [`bruno-tests/events-api/51-speaker-workflow-invalid-transition.bru`] — comment locks "(currently 422 ...)" but ADR-009 §0.2 doesn't pin the status. Either pin the contract in §0.2 or remove the parenthetical. [Blind]
- **`docs/prd/epic-6-speaker-onboarding-plan.md` retains un-annotated current-behavior magic-link claims (7 lines)** — legacy doc; add a supersession banner in a follow-up doc-sweep.
- **`deriveHostBase(loginUrl)` strips path → fragile under sub-path deployments** [`services/event-management-service/.../SpeakerInvitationEmailService.java:2112-2125`] — fine today (bare scheme+host). Keep the existing `@Value("${app.base-url}")` field instead of regex-stripping. [Blind #7]

### Infra test coverage / hardening (Story 11.E.1 carryover)
- **`fromUserPoolId(...) as unknown as cognito.UserPool` cast** [`infrastructure/test/unit/company-management-stack.test.ts:47-51`] — if `CompanyManagementStack` ever reads beyond `.userPoolArn` from `props.userPool`, the stub throws at synth. Fix: widen to `IUserPool` and adjust call sites. [Blind #4 + Edge #7]
- **No IAM `Condition` keys narrowing `AdminSetUserPassword` to specific usernames** [`infrastructure/lib/stacks/company-management-stack.ts:163-176`] — defense-in-depth; Cognito supports `aws:RequestTag/Role`-style scoping. [Blind #10]
- **`should_configureOAuthFlows_when_appClientCreated` may not pin all four `ExplicitAuthFlows` as a closed set** [`infrastructure/test/unit/cognito-stack.test.ts:~70-88`] — asserts presence-only (`Match.arrayWith`); a regression removing SRP/USER_PASSWORD_AUTH wouldn't fail. Tighten to a closed set. [Blind #19 + Edge #5]
- **Tests not parametrized across `isProduction: true|false`** [`infrastructure/test/unit/company-management-stack.test.ts`] — only `stagingConfig` is exercised; a config-conditional Cognito grant wouldn't be caught. [Edge #6]
- **14-day temp password window — security-tradeoff comment missing** [`infrastructure/lib/stacks/cognito-stack.ts:204`] — the comment captures UX rationale, not the threat model (longer email-resident credential validity). Document in the invitation-email rewrite. [Blind #6]
- **ECS task-role IAM policy picked up at next STS credential refresh (≤6h), not immediately** — operational note: IAM-policy-only CDK deploys do NOT trigger ECS task replacement. Force a service redeploy on the first deploy or `AdminCreateUser` calls fail with AccessDenied. [Edge #8]

### Low-priority test-semantics notes
- **Misleading test semantics in `should_rejectLegacyStatusUpdate_when_constraintsTightened`** [`V93LegacySpeakerStatesMigrationIntegrationTest.java`] — witness row was already mapped to `accepted`, so the constraint check is incidental. Functions correctly; misleading to readers. Low priority.
- **No test for `sessionId == null` derived-flag path** [`SpeakerStatusControllerIntegrationTest.java`] — the `fromEntity(SpeakerPool, null)` else-branch is trivially correct but has no dedicated assertion. Low priority.
- **`Instant` serialisation format not pinned by JSON test** [`SpeakerPromotedToReadyEventTest.java:85`] — asserts key presence only, not ISO-8601 vs epoch. Downstream consumers pin when they parse.
- **`eventType` wire-format inconsistency between sibling speaker events** — `SpeakerInvitationSentEvent` uses `"SpeakerInvitationSent"` (no suffix); siblings use the full class name. Sweep cleanup belongs to a future story before Phase E consumes routing.
- **JSON serialisation test uses substring-contains rather than structural assertions** [`SpeakerPromotedToReadyEventTest.java:80-87`] — project pattern across all event tests.
- **AC9 self-reference gap: allow-list does not enumerate the story file itself** — spec template tweak, not a code defect.
- **`useSendInvitation` called with `username: speaker.id`** [`EventSpeakersTab.tsx:182`, `SpeakerDetailDrawer.tsx:165`] — pool entry's `.id` IS the username per the hook's JSDoc; project-wide convention, predates 11.D.4.
- **`MagicLinkService.RESPONDED_STATES` legacy-row interaction / `MagicLinkService` null `previousResponse` / `markTokenAsUsed` no-op** — *all three resolved by Story 11.F.1 (MagicLinkService deleted); see Resolved section.*

---

## Resolved (removed 2026-06-07)

- **JIT `findByEmail` is case-sensitive** (12.6) — RESOLVED by PR #745: `JITUserProvisioningInterceptor.java:147` now uses `findByEmailIgnoreCase`. Verified.
- **Federated linking lookup ignores `user_additional_emails`** (12.6) — RESOLVED by PR #745 (Story B): `pre-signup.ts:137-167` adds a verified-additional-email fallback (gated on `verified_at IS NOT NULL` AND IdP `email_verified='true'`), and `JITUserProvisioningInterceptor.java:170` guards JIT against verified additional emails. Verified.
- **`/auth/callback` may race the Amplify v6 OAuth code-exchange** (12.7) — RESOLVED by PRs #739/#741 (12.8 F7/F8): `AuthContext.completeFederatedSignIn` (`AuthContext.tsx:482-499`) now awaits `authService.waitForFederatedSession()` (bounded Hub-event/token-poll wait for the code exchange to settle) before resolving the session; the callback page additionally gates on runtime config (F8). Verified.
- **"Google SSO login via verified additional email" — Story B queued next** (spec-additional-email-verification, 2026-06-06) — RESOLVED by PR #745: both the `pre-signup.ts` PreSignUp_ExternalProvider fallback and the `JITUserProvisioningInterceptor` fallback are implemented, with the mandatory Lambda handler unit test (`infrastructure/test/unit/lambda/pre-signup.test.ts`) and CUMS integration tests (`JITAdditionalEmailGuardIntegrationTest`, `AdditionalEmailVerificationIntegrationTest`). Verified.
- **`magicLinkService.markTokenAsUsed` functionally no-op for SUBMIT tokens** (11.C.2) — RESOLVED by Story 11.F.1 (2026-05-25): `MagicLinkService` deleted along with the `SpeakerPortalContentController.markTokenAsUsed` call site.
- **`MagicLinkService` null `previousResponse` for QUALITY_REVIEWED migrated speakers** (11.B.3) — RESOLVED by Story 11.F.1: `MagicLinkService` deleted; the Cognito-only flow reads dashboard state from `speaker_pool` + `session_users`.
- **`MagicLinkService.RESPONDED_STATES` interaction with legacy DB rows** (11.B.2) — RESOLVED by Story 11.F.1: `MagicLinkService` deleted entirely; no magic-link respond-form path remains.
- **Five legacy speaker-portal `*IntegrationTest` classes marked `@Disabled`** (11.E.3) — RESOLVED by Story 11.F.1 AC6: all 5 `@Disabled` files deleted.
- **Four frontend Vitest suites + `speakerPortalService.test.ts` marked `describe.skip`** (11.E.3) — RESOLVED by Story 11.F.1 AC6 + RD5: legacy tests deleted; four new Cognito-side replacement test files written (the residual coverage-parity concern is tracked separately in Cluster D).
- **ADR-009 §0.3 says `magic_link_tokens` is in event-management-service schema** (11.E.4) — RESOLVED by Story 11.F.1 RD3: §0.3 row now footnotes that the colloquial `magic_link_tokens` label refers to the EMS table `speaker_invitation_tokens`.

### Cross-references retained (not removed, but tracked under their new owner)
- **`runInvitedHook` Cognito-mutation-inside-`@Transactional`** — kept in Cluster B; spun out as Story 11.F.2 (AFTER_COMMIT listener refactor), still open. The `runInvitedHook` email-only AFTER_COMMIT discussion (11.B.2 deferred item) folds into the same 11.F.2 work.
- **Three Playwright `test.fixme` specs + `speaker_jwt` cookie-audit `test.skip`** — kept in Cluster D; deferred per Story 11.F.1 RD4, awaiting the ops-side staging-Cognito test-speaker seed + `SPEAKER_AUTH_TOKEN` secret.

## 2026-06-07 — from spec-screencast-new-workflow-elevenlabs review
- **i18n: untranslated `speakerDrawer.secondaryActions` siblings in 8 non-DE/EN locales** — `decline`, `reassignOrganizer`, `editDetails`, `overrideState` are English in es/fi/fr/it/ja/nl/rm/gsw-BE while the newly added `acceptOnBehalf` is translated, producing a mixed-language drawer menu. Pre-existing gap (10-locale rule applies to UI keys); translate the siblings.

---

## Deferred from: code review of 7-1-topics-from-the-floor (2026-06-10)

- **Malformed/empty request body → 500 instead of 400** [`services/partner-coordination-service/src/main/java/ch/batbern/partners/exception/GlobalExceptionHandler.java`] — no `@ExceptionHandler(HttpMessageNotReadableException.class)`, so a body-less or garbage-JSON POST falls through to the catch-all `Exception` handler → 500. Pre-existing; affects all POST endpoints in the service, newly inherited by `/api/v1/attendees/topics`. Fix: add a handler returning 400. [Edge]
- **`resolveCallerCompanyNameOrNull()` fail-open on resolution error** [`services/partner-coordination-service/src/main/java/ch/batbern/partners/service/TopicService.java:227`] — swallows all exceptions and returns null, so a partner whose company resolution transiently fails (User Service hiccup) is treated as a null-company organizer and bypasses the ownership guard in `updateTopic`/`deleteTopic` (`callerCompanyName != null && ...` short-circuits). Pre-existing — this commit only flipped the `.equals()` operand order to be null-safe for community topics (correct). Consider distinguishing "organizer (no company)" from "partner whose company could not be resolved." [Edge]
- **`getCurrentUsername()` empty-string trap → blank `suggestedBy`** [`services/partner-coordination-service/src/main/java/ch/batbern/partners/service/TopicService.java:108`] — `suggestCommunityTopic` persists `suggestedBy` with no null/blank guard; per the project's documented Pattern 3b twin, the JWT username claim can be empty in some auth paths, yielding an un-attributable community topic. Dormant in staging (JWT always carries username); a local-dev/edge risk. [Blind]

## Deferred from: quick-dev scoping of Epic 14 (2026-06-13)

`/bmad-quick-dev` was pointed at `docs/prd/epic-14-event-detail-redesign.md` (the full epic = 24
stories, FR1–FR47, 7 ordered phases A–G). Per quick-dev's single-goal scope rule, **Phase A (the
8-tab lifecycle-aware shell — Stories 14.A.1 + 14.A.2 + 14.A.3)** was carved off as the active goal
(spec `spec-14-a-event-detail-shell.md`). The remaining phases are deferred — each is a natural
follow-up quick-dev pass (or per-story `/bmad-create-story` → `/bmad-dev-story`), and all assume the
Phase A shell + the `workflowState → relevance` map already exist:

- **Phase B — Cockpit** (14.B.1–14.B.5): 8-step lifecycle spine, "Needs your attention" task cards,
  the §12.1 per-card completion-signal model (done cards disappear), event-day virtual cards
  (@ AGENDA_PUBLISHED onward), 4 clickable metric tiles, ＋Add task. _(FR7–FR13 · AR3,AR4)_
- **Phase C — Speakers & Agenda** (14.C.1–14.C.5): Pool/Agenda/Slots sub-view toggle, 4-phase
  kanban + workflow-safe drag grammar + collapsible Declined, Agenda session table, **2-column
  in-tab Slots** (rework `DragDropSlotAssignment`, retire the `/slot-assignment` route + `100vh`
  lock + repoint every in-app navigator). _(FR14–FR23 · AR5,AR6)_
- **Phase D — Registrations** (14.D.1–14.D.4): header (count, capacity bar, badge exports, Enrol),
  search + status filters, row actions, **waitlist-as-filter**, pagination/virtualization. _(FR24–FR28 · NFR3)_
- **Phase E — Communications** (14.E.1–14.E.3): 4-audience compose switch, **wire the existing
  `useSendReminder` hook** for speaker bulk comms, Venue & Caterer audience moved from Settings. _(FR29–FR34)_
- **Phase F — Config cluster + remaining tabs** (14.F.1–14.F.5): Publishing & Settings finalization,
  Wrap-up (photos + thank-you notes), Details (identity + topic), the **Topic focused overlay**
  (pick → pin → brainstorm), and removing the interim Overview. _(FR35–FR43 · AR7)_
- **Phase G — Mobile** (14.G.1–14.G.4): bottom nav + ⋯ More sheet, tap-to-assign Slots,
  tables→cards, mobile Cockpit. _(FR44–FR47 · NFR4)_

Cross-cutting constraint on **every** deferred phase: NFR9/AR9 beta-first dual-serve — frontend-only,
additive-and-backward-compatible backend only, recompose-don't-rewrite. (Full detail in the epic.)

## Deferred from: code review of story-7.2 (2026-06-10)

- **`@PreAuthorize("hasRole('ATTENDEE')")` runtime-assumption (verify-in-prod)** [`services/event-management-service/src/main/java/ch/batbern/events/controller/SelfNominationController.java:50`] — the entire self-nomination feature depends on logged-in attendees carrying an explicit `ROLE_ATTENDEE` claim/assignment. `JwtRolesConverter` maps `ROLE_ATTENDEE` from `custom:role` / `role_assignments`; JIT provisioning defaults ATTENDEE and Pattern 3b provides a DB-fallback, so it should hold — but the integration tests use `@WithMockUser(roles={"ATTENDEE"})` and cannot detect a missing real-world claim. If absent, every nomination returns 403 and the feature is dead-on-arrival. Confirm a real registered/federated attendee's token carries ATTENDEE before sign-off. [Auditor]

## Deferred from: code review of spec-14-b-cockpit (Epic 14 Phase B, 2026-06-13)

- **`CustomTaskModal` invalidates the generic `['tasks']` key, not event-scoped keys** [`web-frontend/src/components/organizer/Tasks/CustomTaskModal.tsx:159`] — its create/update mutations invalidate `['tasks']`, which does NOT match `useEventTasks`' `['eventTasks', code]` or `useMyTasks`' `['myTasks']` keys. The Cockpit's `AttentionList` works around this by invalidating the event-task queries itself on modal close, so the Phase B "+Add task" flow is correct — but any OTHER caller of `CustomTaskModal` relies on the 3-min staleTime to refresh. Pre-existing (predates Epic 14); root fix is to make `CustomTaskModal` invalidate event-specific keys. [Edge-case hunter]

## Deferred from: quick-dev scope-split on Epic 14 (2026-06-14) — Phase C selected, D–G deferred

Epic 14 (`docs/prd/epic-14-event-detail-redesign.md`) was scoped into quick-dev as **Phase C — Speakers & Agenda** (stories 14.C.1–C.5). The following later phases are independently-shippable goals deferred to their own runs:

- **Phase D — Registrations** [FR24–28 · NFR3 · UX-DR9] — header (count chip, capacity bar, badge exports, Enrol organizers & partners), search + status segmented control, participant table + row actions, **waitlist-as-filter** (queue-ordered + inline Promote), and **pagination/virtualization** (~200 rows not virtualized today).
- **Phase E — Communications** [FR29–34 · UX-DR8] — one compose surface with a **4-audience switch** (Newsletter · Registrants · Speakers · Venue & Caterer), **wiring the existing-but-unwired `useSendReminder`** for speaker bulk comms, and the Venue & Caterer audience moved here from Settings.
- **Phase F — Config cluster + Topic overlay** [FR35–43 · AR7 · UX-DR10–13] — Publishing, Wrap-up (photos + thank-you notes), Details (identity + topic + when/where), the **Topic focused overlay** (pick → pin → brainstorm reusing `TopicBacklogManager` + `SpeakerBrainstormingPanel`), and Settings (logistics removed).
- **Phase G — Mobile** [FR44–47 · NFR4 · UX-DR14,15] — bottom nav + ⋯ More sheet, tap-to-assign Slots, tables→cards, mobile Cockpit.

All four inherit the epic's cross-cutting DoD (NFR1/5/7/8/9) and the beta-first dual-serve constraint.

## Deferred from: code review of spec-14-c-speakers-agenda (Epic 14 Phase C, 2026-06-14)

- **`hasAssignedSlot` semantics are loose: `isSlotAssigned === true || sessionId != null`** [`web-frontend/src/components/organizer/SpeakerStatus/getPrimaryAction.ts:160`, mirrored at `SpeakerStatusLanes.tsx:907`] — a QUALITY_REVIEWED speaker whose session exists (`sessionId` set) but is **not yet time-slotted** (`session.startTime == null`) renders "✓ Slot assigned" in the Confirmed column, while the new Agenda "N of M need a slot" count (`!s.startTime`) counts that same session as needing a slot. The two surfaces disagree. This is the **pre-existing Epic 11 `hasAssignedSlot` definition** that Phase C was required to reuse unchanged — fixing only the kanban display would diverge it from the primary-action button. Reconcile the kanban / Agenda / `getPrimaryAction` notion of "slotted" (gate all three on `startTime`) in a focused follow-up. [Edge + Blind]
- **Pool sub-view renders blank when the speaker-pool query errors (no empty/error state)** [`web-frontend/src/components/organizer/EventPage/EventSpeakersTab.tsx` Pool branch `currentView === 'pool' && speakers &&`] — once loading settles, an `undefined` `speakers` (fetch error) yields a blank Pool with no message. Pre-existing guard (was `view === 'kanban' && speakers` before Phase C); not introduced by this story. Add an empty/error state. [Edge]
- **Speaker-status summary fetch failure shows fabricated zeros + disables the slot-capacity gate** [`EventSpeakersTab.tsx` summary `useQuery`] — no `isError` handling; on failure the summary bar shows a confident "0/12 · Need more speakers · 0%" and `maxSlots` is `undefined` so `computeSlotCapacity(..., undefined)` silently stops enforcing the kanban invite gate. Pre-existing. Surface an error/stale indicator. [Edge]
- **`focusSpeakerId` highlight is best-effort and often no-ops** [`DragDropSlotAssignment` focus match on `session.speakers[].username`; `EventSpeakersTab.handleAssignSessionSlotForSpeaker` forwards `speaker.username ?? speaker.id`] — a null-username speaker (forwards pool `id`, never matches a session username), a stale-bookmark `speakerId`, or an already-slotted speaker (excluded from the unassigned tray) all land on Slots with no visible highlight. Documented best-effort per the spec; improve by matching on a stable session identifier or auto-switching the tray filter. [Edge + Blind]
- **Drag halo marks slot-capacity-blocked columns as valid** [`SpeakerStatusLanes.tsx` drag-start `validTargets` uses `classifyDrop(...).kind !== 'illegal'`] — a READY card at slot capacity yields `legal-blocked-slot` (not `illegal`), so the forward column lights the green "valid" halo, yet the drop only produces a capacity-rejection toast. Misleading affordance; low severity. [Blind]
- **QUALITY_REVIEWED cards are draggable but have no valid forward target** [`SpeakerStatusLanes.tsx:818` `disabled: status === 'DECLINED'`] — dragging a Confirmed card dims every other column (no legal target) then snaps back. Making QR non-draggable is the UX fix but would remove the currently-tested backward-snap-back path; revisit alongside the FR20 backward-drag decision. [Edge]

## Epic 14 Phase D (Registrations) — review 2026-06-14
- **Muted Registrations tab-count contrast (NFR7, LOW):** the count renders in `text.secondary` small text on the tab rail (`EventPage.tsx` muted variant). Verify ≥4.5:1 WCAG 2.1 AA contrast during the beta visual-QA pass; bump emphasis if it falls short. Not caused by this change beyond introducing the count; no functional impact.

## Deferred from: spec-14-e-communications-tab (Epic 14 Phase E, 2026-06-14)
- **Speakers audience: consistent template + language + preview + bulk-send surface (backend-inclusive follow-up).** The shipped Speakers audience (`SpeakerBulkComms`) is **deliberately NOT** the template→language→preview→send pattern the other three audiences use. It surfaces only the existing 1:1 `useSendReminder` endpoint (`POST /events/{code}/speaker-pool/{id}/send-reminder`, `reminderType: RESPONSE|CONTENT`), which **auto-selects** a fixed template by convention (`speaker-reminder-{response|content}-tier{1,2,3}`, per-speaker language, auto tier) — so the organizer's only knob is RESPONSE vs CONTENT. There is **no** speaker analog of `registrantNoticeService.preview()`/`.send(templateKey)` (no bulk-send-by-template, no speaker preview endpoint). Reviewed live by Nissim (2026-06-14): **decision = keep the reminder surface for now** (functional + frontend-only), accept the divergence. To make Speakers consistent with Newsletter/Registrant/Venue, build the **additive** backend in `event-management-service` (mirror registrant-notice): (1) a speaker-notice **preview** endpoint (render a chosen `SPEAKER`-category template in a chosen language), (2) a **bulk speaker-send** endpoint (send a chosen template to a recipient set). Then rebuild `SpeakerBulkComms` as template+language+preview+send. This is additive/backward-compatible (AR9-safe), but **not frontend-only** → deploys to prod before any beta preview. **This story subsumes the FR32 "logistics & arrival" + "thank-you" speaker comms** (they become just more SPEAKER templates the organizer can pick). [Phase E scope decision — Nissim, 2026-06-14]
- **`tabBadges` `COMMS_TASK_PATTERN` → first-class category (epic post-impl follow-up M3):** the Communications overdue-dot still derives from the name-heuristic regex in `web-frontend/src/components/organizer/EventPage/tabBadges.ts`, so an overdue `Venue Booking`/`Catering Coordination` task does not raise the dot even though Venue & Caterer is now a Communications audience. Re-derive the dot from a first-class task category and drop the regex. Listed in the epic's "Post-Implementation Follow-ups"; explicitly out of scope for this spec to keep it contained.

## Deferred from: code review of spec-14-e-communications-tab (Epic 14 Phase E, 2026-06-14)
- **Retry-failed re-sends a stale `result.failed` snapshot** [`web-frontend/src/components/organizer/EventPage/SpeakerBulkComms.tsx` retry button → `runSend(result.failed)`] — the failed `SpeakerPoolEntry` snapshots are captured at the original send; a speaker who changed status since (e.g. an INVITED speaker who has now accepted) is still re-sent the original reminder type on retry, because `result.failed` is not re-filtered against the live pool / current `reminderType`. Mitigated (not eliminated) by clearing `result` on type-change. Re-filter `result.failed` against the current pool before retrying. [Edge — MED]
- **Per-recipient send failures bucket opaquely** [`SpeakerBulkComms.tsx` `runSend` catch] — a speaker with no resolvable email (backend `sendReminder` throws) or a bad `id` lands in `failed` with no per-recipient reason surfaced; the organizer sees only an aggregate "failed N". Surface per-recipient failure reasons (mirrors the registrant-notice "N had no email on file (skipped)" treatment). [Edge — LOW]

## Deferred from: spec-14-f-1-wrapup-tab (Epic 14 Story 14.F.1, 2026-06-14)
- **Dead i18n keys after the sub-tab removal (LOW):** `eventPage.tabs.photos` and `eventPage.tabs.appreciation` (×10 locales) were the Wrap-up sub-tab labels; the stacked layout no longer uses them. Left in place (the children's own `photos.title`/`appreciation.title` headers are unaffected). Sweep with the Story 10.9 unused-key analyzer (`web-frontend/scripts/i18n/analyze-unused.py`) rather than a bespoke 10-locale edit.

## Deferred from: spec-14-f-4-publishing-settings (Epic 14 Story 14.F.4, 2026-06-14)
- **Settings "Cancel event" is a `console.log` stub** [`web-frontend/src/components/organizer/EventPage/EventSettingsTab.tsx` `handleCancelEvent`] — FR43 specifies "Cancel event (notifies registrants)", but the handler only logs. Pre-existing (not touched by 14.F.4, which was Publishing-layout-only). Wire it to the real cancel-event flow + registrant notification in a focused follow-up. [verify]

## Deferred from: code review of story 15-7 (2026-06-20)

- Q&A digest water-mark uses strict `createdAt > :since`; two top-level questions persisted at the identical microsecond could drop one. Negligible for human-posted questions at TIMESTAMPTZ microsecond precision. If ever a concern, switch to a (createdAt, id) tie-breaker or `>=` + notified-id dedup. [services/event-management-service/.../repository/SessionQnaPostRepository.java + QnaNotificationService.notifyRecipient]

## Deferred from: code review of 15-2-per-event-editable-event-type-config (2026-06-21)
- Even-split break distribution silently emits fewer breaks than requested when breakSlots ≥ maxSlots or positions collide (TimetableService). Acceptable-by-design; realistic values unaffected; consider a dialog max / validation later.
- Admin template live-preview (EventTypeConfigurationForm/SchedulePreview.tsx + scheduleTimeline.ts) not updated for apéro/even-split — global-template editor preview diverges from backend. Authoritative backend timetable is correct.
- EditEventTypeDialog number fields: clearing a field yields NaN → confusing zod error instead of defaulting to min. Minor UX.
- Doc-drift: only docs/architecture/03-data-architecture.md updated; 06-backend-architecture.md + user-guide/entity-management/events.md (mapped) not touched, no [no-doc]. May trip weekly auditor.
- i18n: 7 locales (fr/it/rm/es/fi/nl/ja) hold English placeholders for slotAssignment.editEventType.* pending hand-translation (if Nissim accepts placeholders).

## Deferred from: code review of 15-9-owasp-llm-agentic-security-checklist (2026-06-21)

- Frontend `dangerouslySetInnerHTML` guard (`web-frontend/src/components/__tests__/aiOutputEscaping.test.ts`) is a hardcoded 5-file allowlist. A newly-added AI-output render site (e.g. the Story 15.8 LinkedIn-draft preview) or an indirect HTML path (`react-markdown` + `rehype-raw`, `innerHTML`) is not covered. Right fix: an ESLint rule or repo-wide lint that flags `dangerouslySetInnerHTML` in components consuming AI output, rather than a static file list.
