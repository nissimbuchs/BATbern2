// Event Management Service
// Common configuration is in root build.gradle
// This file contains only service-specific dependencies and configuration

plugins {
    // Core plugins applied from root build.gradle configure block
    // Declaring here to ensure proper evaluation order with pluginManagement
    id 'java'
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
    id 'org.flywaydb.flyway'
    id 'org.openapi.generator'
    id 'jacoco'
    id 'checkstyle'
    // Service-specific plugin
    id 'com.gorylenko.gradle-git-properties' version '2.5.7'
}

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

repositories {
    mavenCentral()
}

// Git properties generation
gitProperties {
    keys = ['git.branch', 'git.commit.id', 'git.commit.id.abbrev', 'git.commit.time']
    dateFormat = "yyyy-MM-dd'T'HH:mm:ssZ"
    dateFormatTimeZone = "UTC"
    failOnNoGitDirectory = false
    extProperty = 'gitProps'
}

dependencies {
    // Shared test infrastructure (PostgreSQL Testcontainers base class)
    testImplementation testFixtures(project(':shared-kernel'))

    // Service-specific: Caching (Caffeine in-memory cache)
    implementation 'com.github.ben-manes.caffeine:caffeine:3.2.3'
    implementation 'org.springframework.boot:spring-boot-starter-cache'

    // Service-specific: AWS SDK for EventBridge, S3, CloudFront, and SES
    implementation platform('software.amazon.awssdk:bom:2.39.0')
    implementation 'software.amazon.awssdk:eventbridge'
    implementation 'software.amazon.awssdk:s3'
    implementation 'software.amazon.awssdk:cloudfront'
    implementation 'software.amazon.awssdk:ses'  // BAT-7: Email notifications
    implementation 'software.amazon.awssdk:url-connection-client'
    // Note: AWS SDK v2 uses software.amazon.awssdk:ses, but EmailService uses legacy v1 API
    // Adding AWS SDK v1 SES for compatibility with existing EmailService implementation
    implementation 'com.amazonaws:aws-java-sdk-ses:1.12.772'

    // Service-specific: Thymeleaf for email templates (BAT-7)
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'

    // Service-specific: WebSocket for real-time push notifications (BAT-7)
    implementation 'org.springframework.boot:spring-boot-starter-websocket'

    // Service-specific: Hypersistence Utils for JSONB support (BAT-7).
    // Epic 13 (SB4): the artifact is Hibernate-version-specific; SB 4.0.7 manages Hibernate
    // 7.2, so -hibernate-63 (Hibernate 6.3) is replaced by -hibernate-71 (Hibernate 7.1+).
    // The 6.x JsonBinaryType referenced the moved org.hibernate...BindableType -> "cannot access".
    implementation 'io.hypersistence:hypersistence-utils-hibernate-71:3.15.3'

    // Service-specific: JWT for email confirmation tokens
    implementation 'io.jsonwebtoken:jjwt-api:0.13.0'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.13.0'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.13.0'

    // Service-specific: ShedLock for distributed scheduled tasks (GAP-2)
    implementation 'net.javacrumbs.shedlock:shedlock-spring:5.10.0'
    implementation 'net.javacrumbs.shedlock:shedlock-provider-jdbc-template:5.10.0'

    // Story 10.17: Spring Cloud AWS SQS for inbound email @SqsListener
    // Epic 13 (SB4): 3.3.0 targets Spring Boot 3.x and crashes on SB4 — its SqsAutoConfiguration
    // calls the removed PropertyMapper.alwaysApplyingWhenNonNull() (NoSuchMethodError at context
    // load). The 4.0.x line is the Spring Boot 4 / Spring Framework 7 aligned release.
    implementation platform('io.awspring.cloud:spring-cloud-aws-dependencies:4.0.2')
    implementation 'io.awspring.cloud:spring-cloud-aws-starter-sqs'

    // Story 10.17: jakarta.mail-api for MIME parsing of inbound emails
    // (transitive via Thymeleaf but must be declared explicitly for compile-time use)
    implementation 'jakarta.mail:jakarta.mail-api:2.1.3'
    runtimeOnly 'org.eclipse.angus:angus-mail:2.0.3'

    // Spec spec-auto-participant-email-aliases-excel-export: name-badge XLSX export.
    // Same version as partner-coordination-service to keep the POI footprint aligned.
    implementation 'org.apache.poi:poi-ooxml:5.4.0'
}

// Flyway configuration - service-specific table name
flyway {
    table = 'flyway_schema_history_event_management'
}

// OpenAPI Generator — events-core (default task). Carved from events-api in API consolidation Phase 6.
// The monolithic events-api spec was split into 9 per-domain specs (events-core + 8 siblings),
// each generating into its own ch.batbern.events.<domain>.{api,dto}.generated package.
openApiGenerate {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/events-core-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-core".toString()
    apiPackage = 'ch.batbern.events.core.api.generated'
    modelPackage = 'ch.batbern.events.core.dto.generated'
    invokerPackage = 'ch.batbern.events.core.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse',
        'VenueRole': 'ch.batbern.events.dto.venuecoordination.VenueCoordinationConfig.Role',
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'Session': 'ch.batbern.events.sessions.dto.generated.Session',
        'SessionSpeaker': 'ch.batbern.events.sessions.dto.generated.SessionSpeaker'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'Session': 'ch.batbern.events.sessions.dto.generated.Session',
        'SessionSpeaker': 'ch.batbern.events.sessions.dto.generated.SessionSpeaker',
        'VenueRole': 'ch.batbern.events.dto.venuecoordination.VenueCoordinationConfig.Role'
    ]
}

// The core Event aggregate embeds Session/SessionSpeaker (owned by event-sessions-api),
// referenced via schemaMappings. (openapi-generator 7.2.0 bug #17647 emitted an illegal
// `@Valid <fully.qualified.Name>` for this cross-package mapping and required an
// importMappings + doLast-delete workaround; resolved by the 7.14.0 upgrade.)

// OpenAPI Generator (sessions) — carved from events-api in API consolidation Phase 6
task openApiGenerateSessions(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-sessions-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-sessions".toString()
    apiPackage = 'ch.batbern.events.sessions.api.generated'
    modelPackage = 'ch.batbern.events.sessions.dto.generated'
    invokerPackage = 'ch.batbern.events.sessions.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse',
        'ConflictType': 'ch.batbern.events.service.slotassignment.ConflictType',
        'ConflictSeverity': 'ch.batbern.events.service.slotassignment.ConflictSeverity'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'ConflictType': 'ch.batbern.events.service.slotassignment.ConflictType',
        'ConflictSeverity': 'ch.batbern.events.service.slotassignment.ConflictSeverity'
    ]
}

// OpenAPI Generator (speakers) — carved from events-api in API consolidation Phase 6
task openApiGenerateSpeakers(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-speakers-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-speakers".toString()
    apiPackage = 'ch.batbern.events.speakers.api.generated'
    modelPackage = 'ch.batbern.events.speakers.dto.generated'
    invokerPackage = 'ch.batbern.events.speakers.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse',
        'SpeakerWorkflowState': 'ch.batbern.shared.types.SpeakerWorkflowState',
        'SpeakerResponseType': 'ch.batbern.shared.types.SpeakerResponseType',
        'SpeakerResponsePreferences': 'ch.batbern.events.dto.SpeakerResponsePreferences'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'SpeakerWorkflowState': 'ch.batbern.shared.types.SpeakerWorkflowState',
        'SpeakerResponseType': 'ch.batbern.shared.types.SpeakerResponseType',
        'SpeakerResponsePreferences': 'ch.batbern.events.dto.SpeakerResponsePreferences'
    ]
}

// OpenAPI Generator (registrations) — carved from events-api in API consolidation Phase 6
task openApiGenerateRegistrations(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-registrations-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-registrations".toString()
    apiPackage = 'ch.batbern.events.registrations.api.generated'
    modelPackage = 'ch.batbern.events.registrations.dto.generated'
    invokerPackage = 'ch.batbern.events.registrations.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator (newsletter) — carved from events-api in API consolidation Phase 6
task openApiGenerateNewsletter(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-newsletter-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-newsletter".toString()
    apiPackage = 'ch.batbern.events.newsletter.api.generated'
    modelPackage = 'ch.batbern.events.newsletter.dto.generated'
    invokerPackage = 'ch.batbern.events.newsletter.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator (media) — carved from events-api in API consolidation Phase 6
task openApiGenerateMedia(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-media-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-media".toString()
    apiPackage = 'ch.batbern.events.media.api.generated'
    modelPackage = 'ch.batbern.events.media.dto.generated'
    invokerPackage = 'ch.batbern.events.media.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator (ai) — carved from events-api in API consolidation Phase 6
task openApiGenerateAi(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-ai-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-ai".toString()
    apiPackage = 'ch.batbern.events.ai.api.generated'
    modelPackage = 'ch.batbern.events.ai.dto.generated'
    invokerPackage = 'ch.batbern.events.ai.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator (analytics) — carved from events-api in API consolidation Phase 6
task openApiGenerateAnalytics(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-analytics-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-analytics".toString()
    apiPackage = 'ch.batbern.events.analytics.api.generated'
    modelPackage = 'ch.batbern.events.analytics.dto.generated'
    invokerPackage = 'ch.batbern.events.analytics.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator (watch) — carved from events-api in API consolidation Phase 6
task openApiGenerateWatch(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-watch-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-watch".toString()
    apiPackage = 'ch.batbern.events.watch.api.generated'
    modelPackage = 'ch.batbern.events.watch.dto.generated'
    invokerPackage = 'ch.batbern.events.watch.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator - Generate Topics API DTOs (Story 1: Topics API Migration - ADR-003/ADR-006)
// This task generates DTOs from topics-api.openapi.yml with topicCode identifiers
task openApiGenerateTopics(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/topics-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-topics".toString()
    apiPackage = 'ch.batbern.events.api.generated.topics'
    modelPackage = 'ch.batbern.events.dto.generated.topics'
    validateSpec = false  // Skip validation for custom license fields

    // Import mappings - use shared-kernel types instead of generating
    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    // Type mappings - map OpenAPI schemas to shared-kernel classes
    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',              // Phase 7: generate interfaces (TopicsApi) for controllers to implement
        skipDefaultInterface: 'true',
        useTags: 'true',                    // Phase 7: one interface per tag (TopicsApi, EventTopicsApi)
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',           // Generate builder pattern for DTOs (as requested)
        containerDefaultToNull: 'true'      // collections default to null (not empty list) — preserves
                                            // pre-7.14 wire behaviour: an unset list is absent, not `[]`
                                            // (required for the ?include= sparse-fieldset/embed contract)
    ]

    // Phase 7: generate both DTO models and API interfaces (per-tag) for controller wiring
    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    // Skip generation of shared-kernel types
    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator - Generate Event Tasks API (Phase 7: Configurable Task System, Story 5.5)
task openApiGenerateTasks(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/event-tasks-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-tasks".toString()
    apiPackage = 'ch.batbern.events.tasks.api.generated'
    modelPackage = 'ch.batbern.events.tasks.dto.generated'
    invokerPackage = 'ch.batbern.events.tasks.api.invoker'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'true',
        skipDefaultInterface: 'true',
        useTags: 'true',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        delegatePattern: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'
    ]

    globalProperties = [
        models: '',
        apis: '',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// OpenAPI Generator - Generate User API client DTOs (Story 2.2a: Cross-service types)
task openApiGenerateUsersClient(type: org.openapitools.generator.gradle.plugin.tasks.GenerateTask) {
    generatorName = 'spring'
    inputSpec = "$rootDir/docs/api/users-api.openapi.yml".toString()
    outputDir = "$buildDir/generated-users-client".toString()
    apiPackage = 'ch.batbern.events.api.generated.users'
    modelPackage = 'ch.batbern.events.dto.generated.users'
    validateSpec = false

    importMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata',
        'PaginatedResponse': 'ch.batbern.shared.dto.PaginatedResponse'
    ]

    typeMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]

    configOptions = [
        dateLibrary: 'java8',
        interfaceOnly: 'false',
        skipDefaultInterface: 'true',
        useTags: 'false',
        useSpringBoot3: 'true',
        documentationProvider: 'none',
        openApiNullable: 'false',
        hideGenerationTimestamp: 'true',
        useBeanValidation: 'true',
        performBeanValidation: 'true',
        useOptional: 'false',
        useJakartaEe: 'true',
        generateBuilders: 'true',
        containerDefaultToNull: 'true'  // unset collections serialize as absent, not `[]` (pre-7.14 contract)
    ]

    globalProperties = [
        models: '',
        apis: 'false',
        supportingFiles: 'ApiUtil.java'
    ]

    schemaMappings = [
        'ErrorResponse': 'ch.batbern.shared.dto.ErrorResponse',
        'PaginationMetadata': 'ch.batbern.shared.api.PaginationMetadata'
    ]
}

// Story 15.9 — advisory OWASP-LLM security test subset.
// Runs ONLY @Tag("ai-security") tests (BatbernAiSecurityTest, AiAssistControllerSecurityTest).
// The root `test` task has no tag filter, so these also run there and gate normally; this task
// exists so the advisory `ai-security` CI workflow can run the focused subset on its own.
tasks.register('aiSecurityTest', Test) {
    description = 'Runs the @Tag("ai-security") OWASP-LLM test subset (Story 15.9).'
    group = 'verification'
    useJUnitPlatform {
        includeTags 'ai-security'
    }
    testClassesDirs = sourceSets.test.output.classesDirs
    classpath = sourceSets.test.runtimeClasspath
    shouldRunAfter tasks.named('test')
}

// Make compileJava depend on all OpenAPI generation tasks
compileJava.dependsOn tasks.openApiGenerate, tasks.openApiGenerateSessions, tasks.openApiGenerateSpeakers, tasks.openApiGenerateRegistrations, tasks.openApiGenerateNewsletter, tasks.openApiGenerateMedia, tasks.openApiGenerateAi, tasks.openApiGenerateAnalytics, tasks.openApiGenerateWatch, tasks.openApiGenerateUsersClient, tasks.openApiGenerateTopics, tasks.openApiGenerateTasks

// Add generated sources to source sets
sourceSets {
    main {
        java {
            srcDir "$buildDir/generated-core/src/main/java"
            srcDir "$buildDir/generated-sessions/src/main/java"
            srcDir "$buildDir/generated-speakers/src/main/java"
            srcDir "$buildDir/generated-registrations/src/main/java"
            srcDir "$buildDir/generated-newsletter/src/main/java"
            srcDir "$buildDir/generated-media/src/main/java"
            srcDir "$buildDir/generated-ai/src/main/java"
            srcDir "$buildDir/generated-analytics/src/main/java"
            srcDir "$buildDir/generated-watch/src/main/java"
            srcDir "$buildDir/generated-users-client/src/main/java"
            srcDir "$buildDir/generated-topics/src/main/java"
            srcDir "$buildDir/generated-tasks/src/main/java"
        }
    }
}

// Clean generated sources when running clean
clean {
    delete "$buildDir/generated-core"
    delete "$buildDir/generated-sessions"
    delete "$buildDir/generated-speakers"
    delete "$buildDir/generated-registrations"
    delete "$buildDir/generated-newsletter"
    delete "$buildDir/generated-media"
    delete "$buildDir/generated-ai"
    delete "$buildDir/generated-analytics"
    delete "$buildDir/generated-watch"
    delete "$buildDir/generated-users-client"
    delete "$buildDir/generated-topics"
    delete "$buildDir/generated-tasks"
}
