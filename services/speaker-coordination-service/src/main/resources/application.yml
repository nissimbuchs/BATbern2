server:
  port: 8080

spring:
  application:
    name: speaker-coordination-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
