{
  "name": "batbern-infrastructure",
  "version": "1.0.0",
  "description": "Infrastructure as Code for BATbern Platform",
  "bin": {
    "infrastructure": "bin/infrastructure.js",
    "batbern-infrastructure": "bin/batbern-infrastructure.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest --watch",
    "cdk": "cdk",
    "synth": "cdk synth",
    "synth:dev": "cdk synth --context environment=development",
    "synth:staging": "cdk synth --context environment=staging",
    "synth:prod": "cdk synth --context environment=production",
    "deploy": "cdk deploy",
    "deploy:dev": "cdk deploy --all --context environment=development",
    "deploy:staging": "cdk deploy --all --context environment=staging",
    "deploy:prod": "cdk deploy --all --context environment=production --require-approval broadening",
    "diff": "cdk diff",
    "diff:dev": "cdk diff --all --context environment=development",
    "diff:staging": "cdk diff --all --context environment=staging",
    "diff:prod": "cdk diff --all --context environment=production"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.5",
    "aws-cdk": "^2.110.0",
    "jest": "^29.7.0",
    "js-yaml": "^4.1.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.110.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "jest": {
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/test"
    ],
    "testMatch": [
      "**/*.test.ts"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    }
  }
}
